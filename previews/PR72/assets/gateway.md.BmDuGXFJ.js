import{_ as t,o as a,c as o,ao as s}from"./chunks/framework.BnU3br60.js";const u=JSON.parse('{"title":"Multi-GPU Gateway","description":"","frontmatter":{},"headers":[],"relativePath":"gateway.md","filePath":"gateway.md","lastUpdated":null}'),n={name:"gateway.md"};function i(r,e,d,l,c,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="Multi-GPU-Gateway" tabindex="-1">Multi-GPU Gateway <a class="header-anchor" href="#Multi-GPU-Gateway" aria-label="Permalink to &quot;Multi-GPU Gateway {#Multi-GPU-Gateway}&quot;">​</a></h1><p>The gateway is a gRPC reverse proxy that fronts several ReactantServer.jl workers behind one KServe V2 gRPC endpoint. It is pure Julia, lives in its own package <code>ReactantServerGateway</code> (<a href="/ReactantServer.jl/previews/PR72/api#ReactantServerGateway.serve_gateway"><code>serve_gateway</code></a>), and reuses <code>ReactantServerCore</code>&#39;s node and config parsing plus the generated KServe protobuf. Because it builds only on <code>ReactantServerCore</code> and the gRPC layer, the gateway carries no Reactant dependency. The documented surface, <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerGateway.serve_gateway"><code>serve_gateway</code></a> and <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerGateway.probe_worker_ready"><code>probe_worker_ready</code></a> plus the <code>ReactantServerGateway.RunningGateway</code> handle <code>serve_gateway(blocking=false)</code> returns, is collected on the <a href="./api">API</a> page.</p><p>You do not start the gateway yourself. When a node has two or more workers, the supervisor (<a href="/ReactantServer.jl/previews/PR72/api#ReactantServerNode.supervise-Tuple{AbstractString}"><code>ReactantServerNode.supervise</code></a>, the node&#39;s default entry point) runs the gateway as an embedded child and synthesizes its worker endpoint list from the node file; a single-worker node skips it entirely, because one worker already serves the full KServe V2 API. See <a href="./deployment">Deployment</a> for the node; this page describes what its embedded gateway does.</p><p>Clients connect to a single gRPC endpoint. The gateway extracts the model name from each <code>ModelInferRequest</code> and forwards the raw protobuf bytes over gRPC to the worker that hosts that model. The KServe V2 protobuf wire format is identical end to end, so the gateway is a gRPC-to-gRPC pass-through that never re-marshals the body.</p><h2 id="One-endpoint-whether-you-run-1-GPU-or-8" tabindex="-1">One endpoint whether you run 1 GPU or 8 <a class="header-anchor" href="#One-endpoint-whether-you-run-1-GPU-or-8" aria-label="Permalink to &quot;One endpoint whether you run 1 GPU or 8 {#One-endpoint-whether-you-run-1-GPU-or-8}&quot;">​</a></h2><p>The key scaling fact: nothing about your model, client, or external interface changes as you add GPUs. You run the same launcher (or the same <code>supervise</code> call), the supervisor puts one worker on each GPU, and it fronts them with the embedded gateway, so clients still send one request to one endpoint and it is routed to a worker that serves the model. <code>gpus: auto</code> in the node file already means &quot;one worker per visible GPU&quot;, so the single-GPU node file scales as-is; you only edit it to do something non-default (see <a href="./node_config">Node Configuration</a>).</p><p>The supervisor decides whether a gateway exists at all: a single-worker node serves the full KServe V2 API directly on the public ports with no gateway, and a node with two or more workers embeds the gateway in front of them, each worker binding <code>base_port + i</code> and the gateway binding the public 8001/8002. The supervisor synthesizes the gateway&#39;s worker list from the node file, so the gateway needs no configuration of its own; <a href="./deployment">Deployment</a> owns that decision. The external interface (8001 for gRPC, 8002 for metrics/health) is therefore identical whether you run 1 GPU or 8, which is why your client and the public ports never change as you scale; see <a href="./client">Client Usage</a> for the client side. The <code>REACTANT_ROLE</code> environment variable can split the default <code>all</code> role into <code>workers</code> only or <code>gateway</code> only for deployments that span machines; that topology is beyond the scope of these guides.</p><p>One scrape on 8002 still covers the whole node: the embedded gateway serves its own <code>gateway_*</code> series and merges in every worker&#39;s <code>/metrics</code>. Each worker tags its series with <code>worker</code> and <code>gpu</code> labels itself (the <code>gpu</code> value is the physical device behind its <code>CUDA_VISIBLE_DEVICES</code>), so per-GPU and per-worker breakdowns need no Prometheus relabeling, for example <code>sum by (gpu) (rate(worker_dispatch_total[1m]))</code>. See <a href="./deployment">Deployment</a> for the scrape config.</p><h2 id="What-the-gateway-does" tabindex="-1">What the gateway does <a class="header-anchor" href="#What-the-gateway-does" aria-label="Permalink to &quot;What the gateway does {#What-the-gateway-does}&quot;">​</a></h2><ul><li><p><strong>Single endpoint:</strong> clients reach all workers through one gRPC listener.</p></li><li><p><strong>Model-name routing by autodiscovery:</strong> the gateway is given a flat list of worker <code>endpoints:</code> in its own <code>gateway.yml</code> and queries each worker&#39;s <code>RepositoryIndex</code> RPC (every 10s) to learn which models it currently serves. The discovered model-to-workers routing table is rebuilt and swapped in atomically on each probe, so a control-plane pin/unpin or a worker restart flips routing on the next probe.</p></li><li><p><strong>Replica scheduling:</strong> a model served by more than one worker is load-balanced across those workers, either uniformly (<code>round_robin</code>, the default) or by packing each model onto a fixed, operator-configured number of GPUs with coalescing-aware routing (<code>lpt_packing</code>); see <a href="/ReactantServer.jl/previews/PR72/gateway#scheduling-modes">Scheduling modes</a> below. A third mode, <code>least_outstanding</code>, spreads by live occupancy. Either way, a request fails over to the remaining replicas when a worker returns <code>NotFound</code> or <code>Unavailable</code>.</p></li><li><p><strong>Readiness probe:</strong> a background loop calls each worker&#39;s KServe <code>ServerReady</code> RPC; <code>/readyz</code> is ready when at least one worker reports ready.</p></li><li><p><strong>Raw passthrough:</strong> the <code>ModelInfer</code> hot path never decodes or re-marshals the protobuf body. The request and response types are <code>Vector{UInt8}</code> end to end (gRPCServer.jl and gRPCClient.jl support raw byte messages natively). To route, the gateway decodes a partial schema declaring only <code>model_name</code> (field 1) and <code>id</code> (field 3). Under <code>lpt_packing</code> a second partial decode reads one named tensor&#39;s shape to size the request in items (see the fill quantum below). ProtoBuf seeks past everything else, including every tensor payload; shapes precede the data on the wire, so neither decode touches it.</p></li><li><p><strong>SHM broadcast:</strong> <code>SystemSharedMemoryRegister</code> / <code>Unregister</code> are fanned out to every worker. POSIX SHM regions are host-local; every worker attaches via <code>shm_open</code> independently. Register succeeds only if all workers succeed (it rolls back partial success); unregister succeeds if any worker does.</p></li><li><p><strong>SHM namespace probe:</strong> <code>IsSameIPCNamespace</code> is fanned out to every worker and the gateway returns <code>true</code> only if all of them can see the client&#39;s region (any worker may service a later <code>ModelInfer</code>). A worker that errors or does not implement the RPC counts as <code>false</code>.</p></li><li><p><strong>Observability:</strong> structured logs, Prometheus metrics, <code>/healthz</code>, and <code>/readyz</code> on a separate admin HTTP port.</p></li></ul><h2 id="Scheduling-modes" tabindex="-1">Scheduling modes <a class="header-anchor" href="#Scheduling-modes" aria-label="Permalink to &quot;Scheduling modes {#Scheduling-modes}&quot;">​</a></h2><p>The gateway routes each model&#39;s requests across its replicas according to <code>scheduling.mode</code> in <code>gateway.yml</code>. <code>round_robin</code> is the predictable default and needs no measurements; <code>least_outstanding</code> spreads by live occupancy; <code>lpt_packing</code> concentrates each model&#39;s traffic on few GPUs for deep coalescing batches and is the mode whose placement the gateway manages. Every setting in the block below except <code>mode</code> can also be changed while the gateway runs, through its own gRPC control plane; see <a href="/ReactantServer.jl/previews/PR72/gateway#runtime-scheduling-control">Runtime scheduling control</a>.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">scheduling</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">lpt_packing</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             # round_robin (default) | least_outstanding | lpt_packing</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  work_basis</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">compute</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # compute (default) | items | requests  (what &quot;least busy&quot; measures,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                #   for least_outstanding and for fill_least; see below)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  rebalance_compute_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">300</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # fleet GPU-seconds consumed that triggers a repack</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  first_rebalance_compute_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # smaller budget for the first repack (0 = use rebalance_compute_seconds)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  ema_halflife_compute_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # demand-EMA halflife in fleet compute-seconds (0 = track rebalance_compute_seconds)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  hysteresis</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               # extra improvement required before a model moves workers (0 = move on any gain)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  default_replicas</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # GPUs per model unless overridden below (a number, or &quot;all&quot;)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  routing_fill_factor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # fill quantum as a multiple of max batch size (lpt_packing only)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  routing_policy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">fill_least</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # fill_least (default) | fill_rr  (lpt_packing only)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  routing_fill_mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # run (default) | spread | inflight  (what the quantum counts;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                #   \`inflight\` can park a model on one GPU, see the warning below)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  forbid_memory_oversubscription</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # never strand a model on-demand when it could fit resident (default on)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  compaction_mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">eager</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # eager (default) | off | scheduled  (defragment workers after a repack)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  compaction_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # repacks between compactions; 0 disables  (see On-demand Weights)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    big-model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      replicas</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               # this model is placed on 2 distinct GPUs (a number, or &quot;all&quot;)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      fill_mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">spread</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # optional per-model override of routing_fill_mode</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      fill_factor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # optional per-model override of routing_fill_factor</span></span></code></pre></div><h3 id="round_robin" tabindex="-1">round_robin <a class="header-anchor" href="#round_robin" aria-label="Permalink to &quot;round_robin {#round_robin}&quot;">​</a></h3><p><code>round_robin</code> (the default) spreads each model&#39;s requests uniformly across its replicas. It is fully predictable from the config file and needs no measurements, at the cost of thin per-worker queues: when every model is on every worker, each worker sees a slice of every model&#39;s traffic, so coalesced batches rarely fill.</p><h3 id="leastoutstanding-and-workbasis" tabindex="-1">least_outstanding and work_basis <a class="header-anchor" href="#leastoutstanding-and-workbasis" aria-label="Permalink to &quot;least_outstanding and work_basis {#leastoutstanding-and-workbasis}&quot;">​</a></h3><p><code>least_outstanding</code> sends each request to the replica carrying the least in-flight work, spreading by live occupancy rather than blindly. Like <code>round_robin</code> it needs no preconditions and does not concentrate traffic, so it favors even spreading over batch coalescing; prefer it over <code>round_robin</code> when a model&#39;s replicas have uneven or unpredictable per-request latency, so a slow replica stops attracting new work instead of accumulating a backlog.</p><p><code>scheduling.work_basis</code> decides what &quot;work&quot; means. It is fleet-wide: the same setting denominates <code>least_outstanding</code>&#39;s per-replica counter and <code>lpt_packing</code>&#39;s <code>fill_least</code> per-worker load, so there is one answer to &quot;what does this fleet call busy&quot;.</p><ul><li><p><strong><code>compute</code></strong> (the default) counts in-flight GPU-seconds: each request charges its item count times the model&#39;s worker-measured cost per item. This is the only basis that is comparable across models, where 32 items of a cheap model are not 32 items of an expensive one.</p></li><li><p><strong><code>items</code></strong> counts in-flight items, so a client that pre-batches is weighed by how much it sent. Correct without any cost measurement when a fleet&#39;s models cost about the same per item. Weakest under <code>fill_least</code>, which compares one worker&#39;s load across <em>all</em> models, so dropping the cost weighting makes a cheap model&#39;s items look as expensive as a costly model&#39;s.</p></li><li><p><strong><code>requests</code></strong> is the per-request denominator each mechanism used before this knob existed: a raw count for <code>least_outstanding</code>, and the model&#39;s measured cost per <em>request</em> for <code>fill_least</code> (which keeps its cross-model cost weighting). Blind to batch size either way, so a pre-batching client defeats the balancing. Set this to pin the pre-knob behavior exactly.</p></li></ul><p><code>compute</code> and <code>items</code> need to know where each model&#39;s batch axis is and what an item costs, which the gateway learns from a <code>ModelControlStatus</code> poll on each health-probe round. Under <code>least_outstanding</code> that is one extra RPC per worker per tick (<code>requests</code> polls nothing); <code>lpt_packing</code> already polls.</p><p>Under <code>lpt_packing</code> the basis is also runtime-mutable, like every other knob in the block: change it with <code>tools/gateway_ctl.jl set-policy work_basis=items</code> and read it back from <code>GetSchedulingStatus</code>, so a fleet can be switched and rolled back without a restart. Under <code>least_outstanding</code> it is fixed at startup, and the startup log line states which basis is in force.</p><h3 id="Models-with-no-batch-axis,-and-metas" tabindex="-1">Models with no batch axis, and metas <a class="header-anchor" href="#Models-with-no-batch-axis,-and-metas" aria-label="Permalink to &quot;Models with no batch axis, and metas {#Models-with-no-batch-axis,-and-metas}&quot;">​</a></h3><p>Both work bases degrade to one unit per request rather than misreporting, so no model ever routes on zeros:</p><ul><li><p><strong>A model that declares no batch axis</strong> (<code>wire_batch_spec</code> finds none: an unbatched bundle, a model whose inputs are all fixed-shape) counts as one item per request. Its leading dimension names an axis, not a count, so there is nothing to read; under <code>compute</code> it still charges its own measured cost, which for such a model is a cost per request.</p></li><li><p><strong>A model the gateway has not polled yet</strong> counts as one item until the first successful poll, and under <code>compute</code> borrows the fleet-mean cost per item, so a cold model weighs like an average one instead of looking free and attracting every request.</p></li><li><p><strong>A meta</strong> (<code>kind: meta</code>) deliberately reports no batch axis and counts one row per request, even when its own <code>client_inputs</code> declare a batch axis. The two halves go together: rows equal requests, so a meta&#39;s cost per item equals its cost per request, and a work-routing gateway charges a meta its whole measured cost. Reporting an axis without also counting rows would charge N times the per-request cost; counting rows without reporting an axis would divide the cost by N and then charge one of them.</p></li></ul><p>A meta is also the clearest argument for <code>compute</code> over <code>items</code>. Its work is data-dependent (a detection meta&#39;s ROI count varies per image, and its Julia glue is not GPU time at all), so no item count predicts it, while under <code>items</code> the most expensive thing on the fleet would charge one unit and a cheap batched model would charge 32. Note that a meta&#39;s <code>total_compute</code> counts its sub-call GPU time only, and its sub-models are folded into it and hidden from the gateway, so that time is counted once rather than double counted.</p><h3 id="lpt_packing" tabindex="-1">lpt_packing <a class="header-anchor" href="#lpt_packing" aria-label="Permalink to &quot;lpt_packing {#lpt_packing}&quot;">​</a></h3><p><code>lpt_packing</code> places each model on a fixed number of distinct GPUs and routes its requests to preserve batch fill. A model&#39;s replica count is operator-controlled: <code>default_replicas</code> (default 1, the single-GPU case that coalesces best), overridable per model under <code>scheduling.models.&lt;name&gt;.replicas</code>. Both accept a positive integer or <code>all</code>, which places the model on every ready worker (so <code>default_replicas: all</code> replicates the whole model set across all GPUs without listing each model, and tracks the fleet as workers come and go). The count never grows <em>automatically</em> under load, so a hot model relies on its worker&#39;s queue and coalescing rather than fanning out on its own; an operator can promote one at any time with <code>SetModelPlacement</code>, effective at the next repack.</p><div class="warning custom-block"><p class="custom-block-title">Replication is the operator&#39;s responsibility</p><p>The gateway does not check that a replica count is feasible for your hardware. Replicating a model charges its full weight footprint to every GPU it lands on, so <code>replicas: 2</code> (or <code>default_replicas: all</code>) only makes sense when those weights actually fit on each card alongside everything else placed there. If the assigned footprint exceeds a worker&#39;s on-demand weight budget, the weights cannot all stay resident and the worker thrashes, loading and evicting weights on nearly every request, which destroys throughput. Size replica counts against your GPU memory. The gateway logs a <code>weight footprint exceeds the worker&#39;s on-demand budget</code> warning at each repack when a placement is oversubscribed, so watch for it.</p></div><p>The packer chooses which GPUs host each model&#39;s replicas by balancing two live measurements: compute demand (the gateway-measured arrival rate times the true per-request compute cost the workers report over the control plane) and resident weight footprint against each worker&#39;s weight-memory budget, placing models heaviest-first onto the least pressured workers, where pressure is whichever of compute or memory is closer to full. Packing by memory keeps each GPU&#39;s resident weight set bounded so evictions stay rare. With <code>forbid_memory_oversubscription</code> enabled (the default), this becomes a hard guarantee: a model is placed only on a worker where its weights still fit whenever some worker can hold it, so a feasible fully-resident placement is never passed over for one that strands a model on-demand (the worker LRU evicting a placed model). The packer falls back to the unconstrained choice, and logs the oversubscription warning above, only when the weights genuinely exceed every worker&#39;s budget.</p><p>Placement is kept stable mainly by smoothing the demand signal rather than by a large dead band: the arrival rate and per-request cost are each tracked with an exponential moving average whose halflife is measured in fleet compute-seconds (<code>ema_halflife_compute_seconds</code>, defaulting to one <code>rebalance_compute_seconds</code> interval, so the signal decays about half per repack and ages with how busy the fleet is rather than with wall-clock). <code>hysteresis</code> adds an optional dead band on top: a single-replica model moves only when the move improves its resulting pressure by more than <code>hysteresis</code> (default <code>0.0</code>, i.e. move on any improvement), because batching depends on traffic staying where the queues are. (<code>max_worker_share</code> is accepted but advisory only; load no longer determines a model&#39;s GPU count.)</p><p>Repacks are driven by accumulated compute, not wall-clock: the gateway polls the workers every probe round and recomputes the placement once the fleet has consumed <code>rebalance_compute_seconds</code> GPU-seconds since the last repack. This compute is <strong>cumulative across every GPU</strong>: the gateway sums each worker&#39;s consumed GPU-seconds, so a fleet of N busy GPUs accrues the budget about N times as fast as one (the accrual rate is the sum of the per-GPU utilizations). The budget is therefore in GPU-work, not real time, and the wall-clock gap between repacks shrinks as the fleet gets busier or larger and stretches out when it is idle (<code>rebalance_compute_seconds</code> divided by the sum of utilizations). The same cumulative clock drives <code>ema_halflife_compute_seconds</code>, so the demand signal also ages in GPU-work rather than wall-clock. The first traffic-driven repack can use a smaller <code>first_rebalance_compute_seconds</code> budget so an early rebalance corrects the cold placement quickly, then later repacks use the larger steady-state budget to limit memory churn (<code>0</code> means the first repack uses <code>rebalance_compute_seconds</code> like the rest). An idle fleet does not repack until traffic resumes. Gateway-driven compaction of worker memory follows placement changes; see <a href="./on_demand_weights">On-demand Weights</a> for <code>compaction_mode</code> and <code>compaction_interval</code>.</p><h4 id="The-fill-quantum" tabindex="-1">The fill quantum <a class="header-anchor" href="#The-fill-quantum" aria-label="Permalink to &quot;The fill quantum {#The-fill-quantum}&quot;">​</a></h4><p>For a model with more than one replica, the gateway concentrates its requests on one replica at a time so the workers receive deep same-model groupings to coalesce (the coalescing itself stays at the worker; see <a href="./scheduling">Scheduling</a>). The <strong>fill quantum</strong> <code>Q</code> is the model&#39;s worker-reported max batch size scaled by <code>routing_fill_factor</code>, and it is denominated in <strong>items</strong>, not requests. One request carrying a batch of 32 and 32 requests carrying one item each are the same amount of work, and both spend a quantum of 32.</p><p>The gateway does not guess where a request&#39;s items are counted. Each worker reports, per model, the <strong>name</strong> of the wire-facing input carrying the batch axis and that tensor&#39;s 1-based axis, taken from the bundle manifest&#39;s reserved <code>n</code>/<code>b</code> shape marker. The gateway matches that name against the request&#39;s own name-addressed inputs and reads that one dimension, without decoding the payload. This has to come from the manifest: the axis is per tensor and varies between bundles (an image model&#39;s <code>whcn</code> puts it last, a tokenized <code>nc</code> puts it first), the first input need not carry one at all (a cross-encoder&#39;s <code>query</code> is unbatched while its <code>keys</code> are batched), and it has moved between exports of the same model. A model that declares no batch axis counts one item per request.</p><p>That matters most for a client that batches on its own behalf. If your client already fills requests to the model&#39;s max batch size, a request-denominated quantum would hold one replica for 32 <em>batches</em> and your bursts would land on a single GPU; the item denomination makes one such request close the run. Models the worker does not batch (a meta, or a model compiled unbatched, both reporting a max batch of 0 or 1) charge one unit per request, since their leading dimension names an axis rather than a count.</p><p><code>routing_fill_mode</code> decides what <code>Q</code> counts against:</p><ul><li><p><strong><code>run</code></strong> (default) counts items <strong>routed</strong> to the current replica. A run spends <code>Q</code> items on one replica, then the next run opens elsewhere, so a model&#39;s GPUs serve it in turn: batches stay deep and every replica gets an even share (to within one run) at any concurrency. A run also ends when the model has nothing in flight, so a low-rate model rotates per request instead of committing <code>Q</code> in a row, and when a replica falls a whole quantum behind the least-backed-up one, so a slow replica loses its turn rather than accumulating a queue.</p></li><li><p><strong><code>spread</code></strong> equalizes <strong>in-flight items</strong> across the replica set, so all <code>k</code> GPUs serve the model at once. This is the right mode when your client already batches to the max batch size, because then the worker has nothing left to coalesce and concentration only idles GPUs. It is also what promotion means for a latency-bound model whose client concurrency sits well below its max batch; the cost is coalescing depth, since each GPU now batches roughly <code>1/k</code> as many requests. Prefer it per model rather than fleet-wide.</p></li><li><p><strong><code>inflight</code></strong> is the legacy basis: <code>Q</code> counts requests <strong>in flight</strong>, so a replica keeps receiving the model&#39;s traffic until it holds a full quantum at once.</p></li></ul><div class="warning custom-block"><p class="custom-block-title"><code>inflight</code> can park a model on one GPU indefinitely</p><p>Because <code>inflight</code> compares the quantum against in-flight requests, a replica keeps winning until it holds <code>Q</code> of them <em>simultaneously</em>. A model whose in-flight concurrency stays between 2 and <code>Q</code> and never drains to zero is therefore served by <strong>one</strong> replica for the life of the process, and its other replicas receive no traffic at all. This is not a warm-up transient: with <code>max_batch = 32</code> and a client holding 4 requests in flight, the second GPU receives zero requests, forever. Closed-loop clients (a fixed-size worker pool, a benchmark harness) hit this reliably, so promoting such a model to more GPUs under <code>inflight</code> buys nothing.</p><p>Diagnose it with <code>gateway_replica_routed_total</code>: a flat series on one replica of a multi-replica model is the signature (<code>gateway_replica_outstanding</code> cannot show it, because a starved replica reads 0 either way). Use <code>inflight</code> only to reproduce the behavior that predated <code>run</code>.</p></div><p><code>routing_fill_factor</code> is the direct trade between the two concerns: a model&#39;s share is even to within one quantum, so a smaller factor balances more finely at the cost of splitting more batches, and a larger one commits longer to each replica for deeper batches.</p><p><code>routing_policy</code> (lpt_packing only) decides <em>which</em> replica each run opens on:</p><ul><li><p><strong><code>fill_least</code></strong> (default) opens each run on the replica whose GPU currently carries the least in-flight work, measured across <em>all</em> models and denominated by <code>work_basis</code> (in-flight GPU-seconds by default). This is what you want whenever replicas share GPUs with other models: a model&#39;s runs open on whichever of its GPUs is least busy rather than always the same one.</p></li><li><p><strong><code>fill_rr</code></strong> rotates the opening replica across the model&#39;s set, so successive runs of the same model spread evenly over its GPUs. Deliberately load-blind, which is right only when every model on the fleet costs about the same and no GPU is shared with anything else.</p></li></ul><p>Both policies are consulted at every run boundary, and exact ties rotate rather than falling back to the worker name, so an idle fleet warms every replica instead of pinning to the lowest-named one. Spreading every request without concentrating it at all is the separate <code>least_outstanding</code> scheduling mode above, not a fill mode. A single-replica model is the degenerate case: all its requests go to its one GPU (and still count toward that GPU&#39;s load for the <code>fill_least</code> decisions of models that share it).</p><h4 id="Preconditions-and-startup" tabindex="-1">Preconditions and startup <a class="header-anchor" href="#Preconditions-and-startup" aria-label="Permalink to &quot;Preconditions and startup {#Preconditions-and-startup}&quot;">​</a></h4><p><code>lpt_packing</code> has two preconditions, verified at gateway startup: every worker must run the <code>fifo</code> or <code>edf</code> scheduler discipline, not <code>fair</code> (placement decisions move to the gateway, so workers should not re-order against it; see <code>scheduler.discipline</code> in <a href="./node_config">Node Configuration</a>), and every worker must serve the identical model set. Because a worker compiles and warms up every model before its control plane answers, the workers are usually not up when the gateway starts, so the gateway waits for all of them before serving rather than failing, logging which workers are still pending. Under the node supervisor (the embedded gateway) this wait is enabled automatically; for a standalone gateway set <code>REACTANT_GATEWAY_STARTUP_WAIT_SECONDS</code> (a number of seconds, or <code>inf</code> to wait indefinitely; the default <code>0</code> fails fast). Each poll is watchdog-bounded, and if the gRPC client stack wedges during the long warmup (a known libcurl failure mode) the gateway exits so the supervisor restarts it with a fresh stack; this self-heals and you may see one such restart before it serves. Once all workers are up, a wrong discipline or differing model set is a hard error.</p><p>Runtime drift after startup degrades gracefully: a model temporarily missing from some workers is routed uniformly over its actual replicas with a warning until the fleet converges, and a worker that drops out is excluded from placement, its traffic failing over to the remaining replicas.</p><h4 id="Observability" tabindex="-1">Observability <a class="header-anchor" href="#Observability" aria-label="Permalink to &quot;Observability {#Observability}&quot;">​</a></h4><p>The placement is observable: <code>gateway_model_replicas</code> reports each model&#39;s replica count, <code>gateway_placement_weight</code> reports its per-worker weight, <code>gateway_replica_outstanding</code> reports the in-flight <strong>items</strong> per replica (summed batch sizes, matching the quantum&#39;s denomination; equal to a request count when every request carries one item), <code>gateway_replica_routed_total</code> counts the <strong>requests</strong> routed to each replica since start (the series that shows whether every replica is actually being used), <code>gateway_model_fill_quantum</code> reports each model&#39;s effective quantum, <code>gateway_repacks_total</code> counts repacks by what triggered them, and <code>gateway_model_utilization</code> reports each model&#39;s estimated demand in GPU-seconds per second.</p><p><code>gateway_worker_inflight_work</code> reports the per-worker load the router actually compares when it picks a replica, so a routing decision can be explained after the fact. It is exported by both work-routing mechanisms (<code>fill_least</code> under <code>lpt_packing</code>, and the <code>least_outstanding</code> mode) and carries a <code>basis</code> label, because the unit follows <code>work_basis</code>: GPU-seconds under <code>compute</code>, items under <code>items</code>, and a per-request cost under <code>requests</code>. Read it live at scrape time from the scheduler&#39;s counters, so it never lags a prober tick, and a worker that no longer hosts anything stops being emitted rather than freezing at its last value. <code>round_robin</code> exports nothing here, since it tracks no load.</p><p>The bundled Grafana dashboards use it: <strong>Scheduling &amp; Placement</strong> plots the per-worker load, the spread across workers (<code>max - min</code>, which is what <code>fill_least</code> exists to shrink), and the basis in force as the running gateway reports it, and <strong>Fleet Overview</strong> carries the per-worker load alongside readiness and device memory. For whether the routing actually produced an even result, as opposed to what the router intended, the <strong>GPU Work Balance</strong> dashboard is the one to read. It is built on <code>rate(worker_compute_seconds_total)</code> per worker, the device-seconds each GPU really consumed, and leads with the spread of that rate across devices (0% is even). Its planned-vs-actual panel is the diagnostic that separates a skewed <em>plan</em> (a repack or a replica count is the lever) from routing not following an even plan. A persistently high spread usually means a model&#39;s replicas cannot absorb the imbalance (too few replicas, or a single-replica model pinning one GPU) rather than a misbehaving policy, so read it next to the placement table.</p><h2 id="Runtime-scheduling-control" tabindex="-1">Runtime scheduling control <a class="header-anchor" href="#Runtime-scheduling-control" aria-label="Permalink to &quot;Runtime scheduling control {#Runtime-scheduling-control}&quot;">​</a></h2><p>The gateway answers its own gRPC service, <code>reactant_control.GatewayControlService</code>, on the same port it serves inference. It exposes the live scheduling state and lets an operator retune it without a restart:</p><table tabindex="0"><thead><tr><th style="text-align:right;">RPC</th><th style="text-align:right;">Purpose</th></tr></thead><tbody><tr><td style="text-align:right;"><code>GetSchedulingStatus</code></td><td style="text-align:right;">The mode, every runtime knob, repack bookkeeping, and one row per model and per worker (placement, in-flight and routed counts, resolved fill mode and quantum, measured demand and cost, assigned weights against each worker&#39;s budget). Answers in every scheduling mode.</td></tr><tr><td style="text-align:right;"><code>SetSchedulingPolicy</code></td><td style="text-align:right;">Change any knob in the <code>scheduling:</code> block except <code>mode</code>. <code>update_mask</code> names the fields to apply; one invalid value rejects the whole request and nothing changes.</td></tr><tr><td style="text-align:right;"><code>SetModelPlacement</code></td><td style="text-align:right;">Set, change, or clear one model&#39;s <code>replicas</code>, <code>fill_mode</code>, and <code>fill_factor</code>. The three are independent, so one call can promote a model and change how it uses its GPUs. Effective at the next repack.</td></tr><tr><td style="text-align:right;"><code>Repack</code></td><td style="text-align:right;">Repack now, bypassing the accumulated-compute budget, optionally waiting a bounded time for it to land.</td></tr></tbody></table><p><code>tools/gateway_ctl.jl</code> is the operator front end, run against the gateway&#39;s gRPC port:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>julia --project=packages/ReactantServerGateway tools/gateway_ctl.jl --gateway HOST:8001 status</span></span>
<span class="line"><span>... set-replicas big-model 2 --fill-mode spread</span></span>
<span class="line"><span>... repack --wait 30</span></span>
<span class="line"><span>... set-policy hysteresis=0.15 routing_policy=fill_least compaction_interval=4</span></span></code></pre></div><p><code>grpcurl -plaintext -proto proto_src/reactant_control_v1.proto ...</code> works too, with no Julia installed.</p><div class="warning custom-block"><p class="custom-block-title">Runtime changes are not persisted, and there is no authentication</p><p>Every override lives in memory only: a gateway restart reverts it to <code>gateway.yml</code> plus the environment, and restart is not hypothetical (the gateway deliberately exits on a wedged client stack, expecting its supervisor to bring it back). Treat these RPCs as a way to try a setting or respond to an incident, and write anything you want to keep into <code>gateway.yml</code>. <code>SchedulingPolicy.generation</code> reads 0 while every knob still matches the config file and is bumped by each accepted change, so you can tell a tuned gateway from a fresh one, and each accepted change is logged with its before and after values, which is the only durable record of it. Like the rest of the control plane these RPCs are unauthenticated (see <a href="./deployment">Deployment</a>), so the gateway&#39;s gRPC port must not be exposed outside the trusted network.</p></div><h2 id="What-the-gateway-does-not-do" tabindex="-1">What the gateway does not do <a class="header-anchor" href="#What-the-gateway-does-not-do" aria-label="Permalink to &quot;What the gateway does not do {#What-the-gateway-does-not-do}&quot;">​</a></h2><ul><li><p>Streaming RPCs.</p></li><li><p>The repository / model-config / statistics / trace / log RPCs in the Triton spec, plus <code>ServerLive</code>, <code>ServerReady</code>, <code>ModelMetadata</code>, and <code>RepositoryIndex</code> for clients. Of the KServe data plane only <code>ModelInfer</code>, the two SHM register/unregister RPCs, and <code>IsSameIPCNamespace</code> are proxied; everything else returns <code>UNIMPLEMENTED</code>. On the control plane the gateway answers <code>ControlService/CompactMemory</code> (fanned out to every worker) and all four <code>GatewayControlService</code> RPCs itself (see <a href="/ReactantServer.jl/previews/PR72/gateway#runtime-scheduling-control">Runtime scheduling control</a>); the other <code>ControlService</code> RPCs are worker-only.</p></li><li><p>TLS: parsed but not yet enforced; the listener and the worker back-hop are cleartext h2c.</p></li><li><p>CUDA shared memory.</p></li><li><p>Dynamic worker membership: the worker endpoint list is fixed at startup (from <code>gateway.yml</code> or <code>REACTANT_GATEWAY_WORKERS</code>). Which models each worker serves is rediscovered continuously, but adding or removing workers requires a gateway restart.</p></li></ul><h2 id="Configuration" tabindex="-1">Configuration <a class="header-anchor" href="#Configuration" aria-label="Permalink to &quot;Configuration {#Configuration}&quot;">​</a></h2><p>The supervisor configures the embedded gateway for you: it synthesizes the worker endpoint list (and the worker metrics list) from the node file into <code>REACTANT_GATEWAY_WORKERS</code> / <code>REACTANT_GATEWAY_WORKER_METRICS</code>, and binds the gateway to the public ports (8001/8002). Nothing about model placement is configured on the gateway; it autodiscovers which models each worker serves via <code>RepositoryIndex</code> and refreshes its routing table periodically.</p><p>To tune the gateway, provide a <code>gateway.yml</code> and point <code>REACTANT_GATEWAY_FILE</code> at it (or leave it at the conventional <code>/etc/reactantserver/gateway.yml</code>, which the supervisor picks up automatically; or, for the embedded gateway, set the <code>REACTANT_GATEWAY_*</code> environment below); it carries the gateway&#39;s own settings (listen addresses, message limits, logging, and the <code>scheduling:</code> block above). Settings can also be overridden by environment with the prefix <code>REACTANT_GATEWAY_</code> and the dotted path uppercased with underscores, for example <code>REACTANT_GATEWAY_LOGGING_LEVEL=debug</code> or <code>REACTANT_GATEWAY_SCHEDULING_MODE=lpt_packing</code>.</p><div class="tip custom-block"><p class="custom-block-title">A mounted <code>gateway.yml</code> owns the endpoint list</p><p>Per-model settings (<code>scheduling.models.&lt;name&gt;</code>) are the one part of the config with no environment equivalent, since the overrides only reach scalar keys, so promoting a single model requires a file. When the supervisor finds one it stops synthesizing the worker lists, because the file is now the authority: the file must therefore carry <code>endpoints:</code> itself, plus <code>metrics_endpoints:</code> and <code>worker_names:</code> if you want the aggregated <code>/metrics</code> and the <code>worker0..N</code> labels the Grafana dashboards join on. The startup wait is <em>not</em> suppressed (it describes the supervisor&#39;s own co-launched workers, not gateway configuration), so an lpt_packing node still waits for its workers to finish compiling rather than crash-looping.</p></div><p>For a standalone deployment, the package also ships <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerGateway.probe_worker_ready"><code>probe_worker_ready</code></a>, the worker container&#39;s healthcheck (a Julia replacement for the former Go <code>reactant-healthprobe</code>): it resolves a worker&#39;s port from the node file and calls its KServe <code>ServerReady</code> on localhost, returning whether it reported ready.</p><h2 id="Operational-notes" tabindex="-1">Operational notes <a class="header-anchor" href="#Operational-notes" aria-label="Permalink to &quot;Operational notes {#Operational-notes}&quot;">​</a></h2><ul><li><p>The gateway is a single point of failure. Each Julia worker stays reachable on its own KServe V2 gRPC endpoint during a gateway outage, so a client can fall back to addressing a worker directly.</p></li><li><p>The routing table is rebuilt every 10s from each worker&#39;s <code>RepositoryIndex</code> and swapped in atomically. If a worker dies, its routes persist until the next successful probe (up to ~10s); in the gap, requests to its models fail over to the remaining replicas (on <code>NotFound</code> or <code>Unavailable</code>), and a model with no live replica returns <code>NotFound</code>. The worker-side readiness probe (<code>ServerReady</code>, same 10s loop) drives <code>/readyz</code> and the <code>gateway_worker_ready</code> metric.</p></li><li><p>Under <code>lpt_packing</code>, the gateway polls the workers on every 10s probe round to refresh routing metadata and accumulate consumed compute, but recomputes the placement only once the fleet has consumed <code>scheduling.rebalance_compute_seconds</code> GPU-seconds (the first repack uses <code>scheduling.first_rebalance_compute_seconds</code> when set). Each repack logs a <code>lpt_packing: repack</code> line with the number of models placed, how many <code>moved</code> workers, how many were <code>held_by_hysteresis</code>, the largest available <code>max_improvement</code> against the <code>hysteresis</code> threshold, and the <code>compute_seconds</code>/<code>wall_seconds</code> since the last repack, useful for watching placement churn and the trigger cadence.</p></li><li><p>If a probe to a worker hangs (times out, rather than failing fast), the gateway drops and recreates that worker&#39;s gRPC connection before the next attempt. This recovers from a half-open connection (for example one caught during a worker&#39;s brief silent-accept window at startup) that would otherwise be reused and stall every later request to that worker, the per-worker equivalent of a restart, without dropping HTTP/2 multiplexing for healthy workers.</p></li><li><p>Successful <code>ModelInfer</code> requests are not logged (to keep the hot path quiet); worker errors and a model with no live replica are logged, and per-request latency and gRPC status are exported as Prometheus metrics. Logs contain no tensor data.</p></li></ul>`,65)])])}const g=t(n,[["render",i]]);export{u as __pageData,g as default};
