import{_ as a,o as t,c as s,ao as o}from"./chunks/framework.BnU3br60.js";const g=JSON.parse('{"title":"On-demand Weights","description":"","frontmatter":{},"headers":[],"relativePath":"on_demand_weights.md","filePath":"on_demand_weights.md","lastUpdated":null}'),i={name:"on_demand_weights.md"};function n(r,e,d,h,c,l){return t(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="On-demand-Weights" tabindex="-1">On-demand Weights <a class="header-anchor" href="#On-demand-Weights" aria-label="Permalink to &quot;On-demand Weights {#On-demand-Weights}&quot;">​</a></h1><p>Because only one model executes at a time, the GPU does not need every model&#39;s weights resident simultaneously. A GPU sized for a handful of models can therefore serve a much larger catalog, paying a small transfer cost only when a cold model is first called. This page is the operational guide to the weight memory system: the two memory tiers, the self-sizing probe that budgets them, residency control, node-shared host weights, BFC fragmentation and compaction, and the observability that confirms it all. Dispatch and batching are the <a href="./scheduling">Scheduler</a>&#39;s job; the reverse proxy and its placement policy live on <a href="./gateway">Multi-GPU Gateway</a>.</p><h2 id="Two-tiers-of-weight-memory" tabindex="-1">Two tiers of weight memory <a class="header-anchor" href="#Two-tiers-of-weight-memory" aria-label="Permalink to &quot;Two tiers of weight memory {#Two-tiers-of-weight-memory}&quot;">​</a></h2><ul><li><p><strong>Host RAM (resident by default).</strong> With the on-demand cache enabled, every model&#39;s weights default to system-pinned (<code>residency: system</code>): materialized once from disk into host RAM at startup and kept there. Host memory is plentiful and cheap relative to GPU memory, which removes disk from the hot path. A model can opt out with <code>residency: unpinned</code>, paying an mmap re-materialization on each on-demand load instead. Materializing the host floor is the expensive step, a layout copy at host-memory speed (<a href="/ReactantServer.jl/previews/PR72/api#ReactantServerCore.materialize_host_weights!-Tuple{PrivateWeightStore, Vararg{Any, 4}}"><code>materialize_host_weights!</code></a>), and it happens exactly once at startup.</p></li><li><p><strong>GPU (managed working set).</strong> Pinned models keep their weights on the GPU for the server&#39;s lifetime. Every other model is loaded onto the GPU on demand when a request arrives, kept resident afterward so repeat requests are free, and evicted under a configured GPU byte budget using a least-recently-used policy. Eviction frees the device memory immediately through an explicit PJRT buffer release (<code>ReactantServer.free_weights!</code>).</p></li></ul><p>Because the weights are already in RAM, an on-demand GPU load is a single host-to-device transfer (<code>ReactantServer.transfer_to_device</code>) rather than a reload from disk: tens of milliseconds even for the largest models, the same order of magnitude as a single inference.</p><h2 id="Enabling-it" tabindex="-1">Enabling it <a class="header-anchor" href="#Enabling-it" aria-label="Permalink to &quot;Enabling it {#Enabling-it}&quot;">​</a></h2><p>There is one knob: <code>runtime.weight_cache_fraction</code>, the fraction of the BFC arena (<code>mem_fraction * device memory</code>) devoted to all weights, pinned plus on-demand. It defaults to <strong>1.0</strong> (use the whole arena, GPU only), and <code>0</code> disables the cache so every model&#39;s weights stay resident. You normally do not set it at all.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  runtime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    backend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cuda</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    weight_cache_fraction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # default: use the whole arena, self-sized (see below)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    weight_cache_wiggle_fraction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.1</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # default: keep 10% of the arena free as headroom</span></span></code></pre></div><p>These are the <code>weight_cache_fraction</code> / <code>weight_cache_wiggle_fraction</code> fields of <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerCore.RuntimeConfig"><code>RuntimeConfig</code></a>, also settable via <code>INFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_FRACTION</code> and <code>INFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_WIGGLE_FRACTION</code> environment variables. The cache is GPU-only; on CPU there is no arena, so weights stay resident regardless.</p><h3 id="Self-sizing:-pinned-scratch-wiggle" tabindex="-1">Self-sizing: pinned + scratch + wiggle <a class="header-anchor" href="#Self-sizing:-pinned-scratch-wiggle" aria-label="Permalink to &quot;Self-sizing: pinned + scratch + wiggle {#Self-sizing:-pinned-scratch-wiggle}&quot;">​</a></h3><p>The fraction is a ceiling on weight memory, not a promise it all fits as cache: each execution also needs transient device memory (activations, conv/GEMM workspace, IO) on top of resident weights, and pinned models occupy the arena too. The worker resolves the on-demand budget for you at startup with <code>ReactantServer.weight_budget</code>. It probes each model once in isolation (loads and runs it), reads the allocator&#39;s peak device usage to measure the worst-case scratch, and sets</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>on_demand_budget = min(fraction*arena, (1-wiggle)*arena - max_scratch) - pinned_weights</span></span></code></pre></div><p>so pinned weights reserve their share, the measured scratch and the wiggle slack are held back, and the rest is the on-demand cache. When everything fits, the cache simply holds all weights resident (no eviction); when it does not, it is memory-safe. The probe is bounded one-time startup work and doubles as an execution smoke test; the resolved budget is logged. Pinned weights or scratch large enough to leave no room are logged as a warning, since no sizing can fix that.</p><h2 id="Pinning-hot-models" tabindex="-1">Pinning hot models <a class="header-anchor" href="#Pinning-hot-models" aria-label="Permalink to &quot;Pinning hot models {#Pinning-hot-models}&quot;">​</a></h2><p>A model that must never pay the on-demand transfer cost can be pinned to stay GPU-resident for the server&#39;s lifetime. Pinned models are exempt from eviction; the budget bounds only the unpinned working set.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">scheduler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    resnet50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      residency</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">device</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # pin_to_gpu: true is a back-compat alias</span></span></code></pre></div><p>This is the <code>residency</code> field of <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerCore.ModelSchedConfig"><code>ModelSchedConfig</code></a> (<code>unpinned</code>, <code>system</code>, or <code>device</code>; unspecified models default to <code>system</code> when the cache is enabled, see <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerCore.ResidencyState"><code>ResidencyState</code></a>). Pin the latency-sensitive or highest-traffic models, and let the long tail load on demand.</p><h2 id="Sharing-host-weights-across-same-node-workers" tabindex="-1">Sharing host weights across same-node workers <a class="header-anchor" href="#Sharing-host-weights-across-same-node-workers" aria-label="Permalink to &quot;Sharing host weights across same-node workers {#Sharing-host-weights-across-same-node-workers}&quot;">​</a></h2><p>With several workers on one node, <code>runtime.shared_host_weights: true</code> backs each system-pinned model&#39;s host copy with a node-shared POSIX shared-memory region so the workers share one copy. The default <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerCore.PrivateWeightStore"><code>PrivateWeightStore</code></a> gives each worker its own copy; <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerCore.SharedWeightStore"><code>SharedWeightStore</code></a> places one content-addressed region per model in <code>/dev/shm</code>, coordinated peer-to-peer through an advisory <code>flock</code>, so same-node workers attach one copy and each transfers it to its own device. <code>host_materialize</code> routes through the configured store, which is what makes this a configuration decision rather than a code change.</p><p>The regions and their lock files are created with mode <code>666</code> by default so containers running as unrelated UIDs can share them; that is world-writable, so set <code>runtime.shared_host_weights_mode: &quot;660&quot;</code> on production and multi-user systems (the server warns at startup when the shared store runs with the <code>666</code> default). Both fields belong to <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerCore.RuntimeConfig"><code>RuntimeConfig</code></a>, with <code>INFERENCE_SERVER_RUNTIME_SHARED_HOST_WEIGHTS</code> and <code>INFERENCE_SERVER_RUNTIME_SHARED_HOST_WEIGHTS_MODE</code> environment overrides.</p><h2 id="Memory-fragmentation-and-compaction" tabindex="-1">Memory fragmentation and compaction <a class="header-anchor" href="#Memory-fragmentation-and-compaction" aria-label="Permalink to &quot;Memory fragmentation and compaction {#Memory-fragmentation-and-compaction}&quot;">​</a></h2><p>When you size a GPU to squeeze the largest possible working set onto it, fragmentation becomes a real constraint. The device pool is one BFC (best-fit-with-coalescing) arena, claimed up front when <code>runtime.preallocate: true</code>. As the on-demand cache loads and evicts models of different sizes over time, the freed regions return to the arena&#39;s free list but do not always sit next to each other, so the arena can hold plenty of free bytes in total yet have no single gap large enough for the next model&#39;s weights. The load then fails or forces extra eviction even though the memory is technically there. The tighter you pack the GPU, the sooner this bites.</p><p>Pinned models are not the problem here: they are loaded once at startup, before any on-demand traffic, so they sit at the base of the arena and never move. Fragmentation accumulates only in the on-demand working set above them. Compaction targets exactly that region. It frees every resident on-demand weight buffer at once, so the allocator coalesces the now-contiguous free space back into one large region above the pinned base. Pinned models are left in place: they are never freed and never re-read from disk. Host floors (the system-pinned RAM copies) are also left untouched, so when an on-demand model reloads it is a fast host-to-device transfer rather than a disk re-materialization (<code>ReactantServer.compact!</code> implements this on the worker).</p><p>Whatever drives compaction frees the on-demand region the same way; what differs is <em>when</em> it fires and <em>what</em> gets reloaded eagerly. Underneath, compaction is a worker control RPC, <code>CompactMemory</code> on the <code>ControlService</code>, that frees the on-demand region and reloads a list of models (empty means free only); both the standalone trigger and the gateway use it. There are two ways to drive it, matched to the two deployment shapes.</p><h3 id="Standalone-worker-no-gateway" tabindex="-1">Standalone worker (no gateway) <a class="header-anchor" href="#Standalone-worker-no-gateway" aria-label="Permalink to &quot;Standalone worker (no gateway) {#Standalone-worker-no-gateway}&quot;">​</a></h3><p>A worker runs compaction itself on a cadence counted in <strong>weight-cache loads</strong>, set under <code>scheduler:</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">scheduler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  compaction_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">200</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # compact every 200 on-demand loads (0 disables, the default)</span></span></code></pre></div><p>A load is what places a variable-size weight block on the device, so loads are what fragment the arena; a dispatch to an already-resident model does not, which is why the cadence counts loads rather than requests or time. The worker trigger is always eager: it frees the on-demand region and lets it refill lazily as requests arrive. It is <strong>off by default</strong>, and a gateway-fronted worker should leave it off, so the gateway is the sole authority on compaction in a gateway deployment (next section). It is the <code>compaction_interval</code> field of <a href="/ReactantServer.jl/previews/PR72/api#ReactantServerCore.SchedulerConfig"><code>SchedulerConfig</code></a>, also exposed as <code>INFERENCE_SERVER_SCHEDULER_COMPACTION_INTERVAL</code>.</p><h3 id="Behind-a-gateway" tabindex="-1">Behind a gateway <a class="header-anchor" href="#Behind-a-gateway" aria-label="Permalink to &quot;Behind a gateway {#Behind-a-gateway}&quot;">​</a></h3><p>In a gateway deployment the gateway owns compaction; workers leave <code>scheduler.compaction_interval</code> at <code>0</code>. The gateway ties compaction to <strong>placement changes</strong>, the fleet-level event that churns worker memory: when the <code>lpt_packing</code> scheduler repacks and moves a model from one worker to another, the new worker loads it and the old worker&#39;s copy goes cold. Two settings under the gateway&#39;s <code>scheduling:</code> block control it:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">scheduling</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">lpt_packing</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  compaction_mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">eager</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # eager (default) | off | scheduled</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  compaction_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # every placement-changing repack (see below)</span></span></code></pre></div><p><code>compaction_mode</code> selects what each affected worker reloads eagerly after the free:</p><ul><li><p><strong><code>eager</code></strong> (the default) frees the on-demand region on each worker whose placement changed and lets it refill with live traffic. Models the worker no longer serves are dropped immediately; the ones it still serves reload on their next request.</p></li><li><p><strong><code>off</code></strong> disables gateway-driven compaction.</p></li><li><p><strong><code>scheduled</code></strong> also reloads the set of models the repack just assigned to that worker, so the worker&#39;s new placement is warm right away instead of cold-loading on first request. The gateway computes that per-worker list from the placement, which is why this is a gateway concept.</p></li></ul><p><code>compaction_interval</code> is the cadence in repacks (default <code>1</code>, so every placement-changing repack compacts). Because the demand signal is smoothed, most repacks recompute the same placement and do nothing; the counter advances on every repack but the fan-out only fires on the first placement-changing repack at or after the interval, so it can land a little later than exactly N. Both settings are also exposed as <code>REACTANT_GATEWAY_SCHEDULING_COMPACTION_MODE</code> and <code>REACTANT_GATEWAY_SCHEDULING_COMPACTION_INTERVAL</code>. A single client <code>CompactMemory</code> call to the gateway also fans out to every worker on demand, independent of the repack cadence, for a one-off fleet defragment.</p><h3 id="Cost-and-when-to-enable" tabindex="-1">Cost and when to enable <a class="header-anchor" href="#Cost-and-when-to-enable" aria-label="Permalink to &quot;Cost and when to enable {#Cost-and-when-to-enable}&quot;">​</a></h3><p>With the on-demand cache disabled (<code>weight_cache_fraction: 0</code>) every model is permanently resident from startup and nothing churns, so compaction has no working set to defragment and is a no-op. When it does run it has a cost: freeing the on-demand region drops models that were warm, so they pay a host-to-device reload (lazily on next request for <code>eager</code>, or up front for <code>scheduled</code>).</p><p>The default is <code>eager</code> on every placement-changing repack (<code>compaction_interval: 1</code>), which keeps fragmentation from ever accumulating: whenever placement shifts, the affected workers defragment and refill from live traffic. The trade is a one-time reload on the first request per moved model, which the relatively infrequent repack cadence (<code>rebalance_compute_seconds</code>, 300 GPU-seconds by default) keeps modest. To cut reloads further, raise <code>compaction_interval</code> so it fires less often, switch to <code>scheduled</code> to warm the new placement up front instead of lazily, or set <code>compaction_mode: off</code> to disable it and watch for allocation failures under memory pressure (the signal that fragmentation has become the bottleneck).</p><h2 id="Observability" tabindex="-1">Observability <a class="header-anchor" href="#Observability" aria-label="Permalink to &quot;Observability {#Observability}&quot;">​</a></h2><p>The <code>ReactantServer.Scheduler</code> exposes weight-cache residency and load/evict counters alongside its dispatch metrics; read them with <code>ReactantServer.scheduler_metrics</code> (<code>ReactantServer.weight_cache_stats</code> is the underlying snapshot). Use them to confirm that hot models stay resident and that the eviction rate is acceptable for your budget. Coalescing (packing many requests into one execution) amortizes a one-time on-demand transfer across every item in the batch, so on-demand loading and batching reinforce each other.</p><p>Each compaction also logs a <code>memory compacted</code> line with the device free space and on-demand budget before and after, and the weight cache tracks a <code>compactions</code> counter. Compare the before/after device free figures to confirm that compaction actually recovered contiguous space on your hardware: it relies on the allocator returning freed buffers to the arena and coalescing them, which the log lets you verify rather than assume.</p><p>The worker also exports the BFC allocator&#39;s live numbers as Prometheus gauges (aggregated through the gateway&#39;s <code>/metrics</code>): <code>worker_device_memory_in_use_bytes</code>, <code>worker_device_memory_free_bytes</code>, <code>worker_device_memory_peak_in_use_bytes</code> (the session high-water, your empirical scratch + resident ceiling), and <code>worker_device_memory_pool_bytes</code>. The peak gauge is what the startup auto-sizing measured; a rising peak over a long uptime is the signal to revisit the budget. Note the GPU BFC allocator does not report a largest-contiguous-free-block figure, so fragmentation is not directly observable here; in practice it surfaces as an allocation failing despite ample reported free bytes, which is what compaction (above) addresses.</p>`,41)])])}const m=a(i,[["render",n]]);export{g as __pageData,m as default};
