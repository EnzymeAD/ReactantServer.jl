import{_ as t,o as a,c as s,ao as n}from"./chunks/framework.BG36LRd2.js";const u=JSON.parse('{"title":"Deployment","description":"","frontmatter":{},"headers":[],"relativePath":"deployment.md","filePath":"deployment.md","lastUpdated":null}'),o={name:"deployment.md"};function i(r,e,d,l,c,p){return a(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="Deployment" tabindex="-1">Deployment <a class="header-anchor" href="#Deployment" aria-label="Permalink to &quot;Deployment {#Deployment}&quot;">​</a></h1><p>A node runs as a single supervisor process (<code>ReactantServerNode</code>): it detects every visible GPU, spawns one single-GPU worker subprocess per device, multiplexes their logs onto one stream with <code>[name]</code> line prefixes, and restarts children that die. With two or more workers it also runs an embedded gateway on the public ports; with a single worker it binds that worker to the public ports directly. The external interface is the same either way: KServe V2 gRPC on <code>:8001</code>, health and metrics on <code>:8002</code> (<code>/readyz</code>, <code>/healthz</code>, <code>/metrics</code>), matching Triton&#39;s ports.</p><p>The supported deployment is <strong>native</strong> (no containers): the supervisor uses the host&#39;s NVIDIA driver directly. A container image is also provided as an alternative and runs the same node with the same interface; see <a href="/ReactantServer.jl/previews/PR71/deployment#docker-container-deployment">Docker (container) deployment</a> below.</p><h2 id="Audience-and-mission" tabindex="-1">Audience and mission <a class="header-anchor" href="#Audience-and-mission" aria-label="Permalink to &quot;Audience and mission {#Audience-and-mission}&quot;">​</a></h2><p>The project targets small and mid-size labs and engineering organizations that need big-tech-level efficiency without big-tech scale: startups and small companies serving production ML where each GPU is a meaningful fraction of infrastructure spend, scientific and research groups operating within bounded compute budgets, and on-premise or cloud deployments where GPU-hours are the dominant cost line. The audience is engineers and operators who understand what is happening inside their systems; the project gives them tools rather than hiding the system&#39;s behavior behind convenient defaults. Larger organizations can use the project too, as a component of a larger system; the difference is what ships in the box, not what is possible.</p><p>The mission is to make serving compiled (non-LLM) models elegant: a hackable, Julia-first inference stack that maximizes the economic efficiency of GPU-based inference. GPU memory is roughly two thirds of GPU cost, so serving infrastructure that wastes memory wastes money; the concrete goal is serving the largest number of models per GPU at a given quality of service. The whole stack is plain Julia, legible end to end, adoptable off the shelf, and open to being bent toward a workload nobody anticipated.</p><p>The project&#39;s explicit non-goals, from its design:</p><ul><li><p><strong>Hyperscale platform requirements.</strong> Multi-tenant isolation, complex traffic shaping, and deep integration with bespoke internal platforms are not built into the core, where every smaller deployment would pay for them. Large deployments are supported through the control-plane seam described under <a href="/ReactantServer.jl/previews/PR71/deployment#multi-node-bring-your-own-control-plane">Multi-node</a>.</p></li><li><p><strong>LLM serving at scale.</strong> vLLM, TGI, TensorRT-LLM, and similar projects are purpose-built for that domain and do it well; this project does not compete there.</p></li><li><p><strong>A packaged, managed solution.</strong> This is infrastructure for builders, not a hosted service. Users who do not want to think about the underlying architecture should choose a managed inference service.</p></li><li><p><strong>Multi-framework serving.</strong> A model must be lowered by Reactant to a device executable first (today via StableHLO/XLA). Teams that need to serve PyTorch, TensorFlow, and ONNX models side by side without converting them are better served by Triton or similar.</p></li></ul><h2 id="Deployment-shapes" tabindex="-1">Deployment shapes <a class="header-anchor" href="#Deployment-shapes" aria-label="Permalink to &quot;Deployment shapes {#Deployment-shapes}&quot;">​</a></h2><p>The deployment decision comes down to how many GPUs you have and, with more than one, whether your constraint is memory or compute:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Situation</th><th style="text-align:right;">Shape</th><th style="text-align:right;">Optimizes for</th></tr></thead><tbody><tr><td style="text-align:right;">One GPU, many models</td><td style="text-align:right;">Single GPU</td><td style="text-align:right;">Fitting many models on limited hardware</td></tr><tr><td style="text-align:right;">Several GPUs, models do not all fit everywhere</td><td style="text-align:right;">Multi-GPU distributed</td><td style="text-align:right;">Serving more models than any one GPU holds</td></tr><tr><td style="text-align:right;">Several GPUs, models fit everywhere, need throughput</td><td style="text-align:right;">Multi-GPU replicated</td><td style="text-align:right;">Spreading compute load across replicas</td></tr><tr><td style="text-align:right;">Many machines</td><td style="text-align:right;">Multi-node</td><td style="text-align:right;">Whatever your control plane decides</td></tr></tbody></table><p>Each shape is selected by a configuration value, not a different architecture, and each is strictly additive: a single-GPU deployment never pays for the gateway in dependencies or runtime cost, and a gateway deployment never requires a control plane.</p><h3 id="Single-GPU" tabindex="-1">Single GPU <a class="header-anchor" href="#Single-GPU" aria-label="Permalink to &quot;Single GPU {#Single-GPU}&quot;">​</a></h3><p>A single worker is the entire deployment. The worker speaks the full KServe V2 gRPC surface itself: one Julia process, one YAML file, nothing else to operate. This is the recommended starting point for small labs and the conceptual foundation for the distributed multi-GPU case, which scales the same idea across cards. The <a href="./scheduling">Scheduling</a> page covers the fair scheduler and batch coalescing; <a href="./on_demand_weights">On-demand Weights</a> covers the weight cache that serves a catalog larger than device memory.</p><h3 id="Multi-GPU,-distributed-without-replication" tabindex="-1">Multi-GPU, distributed without replication <a class="header-anchor" href="#Multi-GPU,-distributed-without-replication" aria-label="Permalink to &quot;Multi-GPU, distributed without replication {#Multi-GPU,-distributed-without-replication}&quot;">​</a></h3><p>You have several cards, but your model library is large enough that it cannot fit on every GPU. Your constraint is memory. The gateway uses LPT (longest-processing-time) packing to place each model on one GPU, balancing memory footprint against compute load; spreading models across cards by their load keeps any one card from being monopolized. The workers switch to the simpler FIFO discipline and the placement intelligence moves upstream to the gateway. There is still no external infrastructure to stand up: the gateway is one more Julia process and one more YAML file, and there is no placement file to maintain.</p><h3 id="Multi-GPU,-replicated" tabindex="-1">Multi-GPU, replicated <a class="header-anchor" href="#Multi-GPU,-replicated" aria-label="Permalink to &quot;Multi-GPU, replicated {#Multi-GPU,-replicated}&quot;">​</a></h3><p>You have enough memory that your models fit on more than one card. Your constraint is compute: you want to spread a model&#39;s request load across replicas for throughput. The gateway gives a replicated model a replica count, places it on that many distinct GPUs, and routes its requests to fill one replica&#39;s batch before moving to the next, so batch coalescing is preserved across replicas. Size replica counts for the model&#39;s expected concurrency, and make sure the memory is there: replicating a model puts its full weight footprint on every GPU it lands on, and an over-subscribed GPU thrashes, loading and evicting weights on nearly every request. See <a href="./gateway">Multi-GPU Gateway</a> for the scheduling modes and knobs.</p><h3 id="Multi-node-bring-your-own-control-plane" tabindex="-1">Multi-node (bring your own control plane) <a class="header-anchor" href="#Multi-node-bring-your-own-control-plane" aria-label="Permalink to &quot;Multi-node (bring your own control plane) {#Multi-node-bring-your-own-control-plane}&quot;">​</a></h3><p>Beyond a single host, the project deliberately does not ship a multi-node control plane: anyone who needs multi-node coordination is usually at a scale where a generic control plane would not fit. Instead, each node exposes the interface a control plane integrates against, so you can build or adapt your own coordination layer on top of ReactantServer nodes. The endpoint contract is the KServe V2 gRPC data plane (<code>ModelInfer</code>, <code>RepositoryIndex</code>, <code>ServerReady</code>) plus the worker control RPCs (<code>ModelControlStatus</code>, <code>SetModelResidency</code>, <code>SetModelPolicy</code> for residency and policy, <code>CompactMemory</code> to defragment device memory), and an admin HTTP port serving <code>/healthz</code>, <code>/readyz</code>, and Prometheus <code>/metrics</code>. A node&#39;s embedded gateway additionally answers <code>GatewayControlService</code> for its own scheduling state. Your control plane discovers which models a node serves via <code>RepositoryIndex</code> and routes <code>ModelInfer</code> to a node that reports the model ready. The project supplies the seam; the organization supplies the tooling that encodes needs only it can know.</p><p>The simplest multi-node setup reuses the standalone gateway pointed at every node&#39;s worker endpoints; a larger deployment replaces it with your own control plane speaking the same gRPC interface. See <a href="./gateway">Multi-GPU Gateway</a> for the standalone gateway&#39;s configuration.</p><h2 id="The-node-supervisor" tabindex="-1">The node supervisor <a class="header-anchor" href="#The-node-supervisor" aria-label="Permalink to &quot;The node supervisor {#The-node-supervisor}&quot;">​</a></h2><p>The supervisor&#39;s job on startup is to turn &quot;this host plus this node file&quot; into a concrete set of child processes. It does so in three steps.</p><p><strong>1. Detect the devices.</strong> The first of these that yields a non-empty answer wins (<code>ReactantServerNode.detect_gpus</code>): 2. <code>REACTANT_GPUS</code> environment variable: a count (<code>2</code>) or an explicit list (<code>0,2</code> or GPU UUIDs); <code>0</code> means a CPU node.</p><ol start="3"><li><p>the node file&#39;s <code>gpus:</code> key (<code>auto</code>, a count, or a list).</p></li><li><p>a <code>CUDA_VISIBLE_DEVICES</code> already set on the host.</p></li><li><p><code>nvidia-smi</code> enumeration.</p></li><li><p><code>/dev/nvidiaN</code> device nodes.</p></li></ol><p>For a CUDA node that finds no devices, startup fails with guidance (run with <code>--gpus all</code>, set <code>REACTANT_GPUS</code>, or set <code>backend: cpu</code>).</p><p><strong>2. Materialize the workers</strong> (<code>ReactantServerCore.materialize_node!</code>). With no <code>workers:</code> list, one worker is synthesized per detected device (<code>worker0..workerN-1</code>); an explicit <code>workers:</code> list wins and assigns each worker a device positionally, or by its <code>gpu:</code> key. Either way each worker is pinned to exactly one device through its own <code>CUDA_VISIBLE_DEVICES</code>, so inside the worker the device is always ordinal 0 and the single-GPU worker code runs unchanged. The materialized node file is written to <code>/run/reactantserver/node.yaml</code> for inspection.</p><p>Each worker&#39;s compute-thread pool is sized to its share of the host, <code>min(CPU_THREADS ÷ workers, 16)</code> threads, plus one interactive thread for the GPU dispatch loop. This avoids the oversubscription that <code>--threads=auto</code> would cause: with several workers on one node, each <code>auto</code> worker (and its GC and host library pools) would size itself to the whole machine and the workers would fight for every core under load. The cap keeps a very large box from handing any one worker an unhelpfully huge pool. Set <code>REACTANT_WORKER_THREADS</code> to override the computed value; it is used verbatim, bypassing the split and the cap.</p><p><strong>3. Decide on the gateway</strong> by worker count, in the default <code>all</code> role:</p><ul><li><p><strong>One worker, no gateway.</strong> A lone worker already serves the full KServe V2 API, so the supervisor binds it directly to the public ports (8001/8002) and starts no gateway. No extra process, no extra hop.</p></li><li><p><strong>Two or more workers, workers plus the embedded gateway.</strong> Each worker binds <code>base_port + i</code> (and <code>metrics_base_port + i</code>), and the gateway binds the public 8001/8002. The supervisor synthesizes the gateway&#39;s worker list (and worker metrics list) from the node file, so the gateway needs no config of its own.</p></li></ul><p>The external interface is therefore identical whether you run 1 GPU or 8, which is why your client and the public ports never change as you scale. <code>REACTANT_ROLE</code> selects what the supervisor runs; the default <code>all</code> (workers plus the embedded gateway on one host) is the documented deployment. The <code>workers</code> and <code>gateway</code> roles exist in the code to split a deployment across machines, but multi-node is not a shipped example.</p><p>One <a href="/ReactantServer.jl/previews/PR71/api#ReactantServerNode.supervise-Tuple{AbstractString}"><code>ReactantServerNode.supervise</code></a> call on the multi-GPU host does the whole fan-out, just as it does for one GPU:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ReactantServerNode</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ReactantServerNode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">supervise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;node.yaml&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># one worker per GPU + the embedded gateway</span></span></code></pre></div><p>This single parent process spawns one <a href="/ReactantServer.jl/previews/PR71/api#ReactantServer.serve-Tuple{AbstractString}"><code>ReactantServer.serve</code></a> worker subprocess per GPU and the gateway as another subprocess, multiplexes their logs onto its stdout with <code>[worker0]</code> / <code>[gateway]</code> prefixes, and restarts any child that dies. (You can still run each worker and the gateway by hand, as separate <code>serve</code> / <code>serve_gateway</code> processes, but the supervisor is the intended path and the one the launcher uses.) <code>ReactantServerNode.main()</code> is the container entrypoint: it reads <code>REACTANT_NODE_FILE</code> for the node config and runs the supervisor to completion.</p><h3 id="Watch-it-without-a-GPU" tabindex="-1">Watch it without a GPU <a class="header-anchor" href="#Watch-it-without-a-GPU" aria-label="Permalink to &quot;Watch it without a GPU {#Watch-it-without-a-GPU}&quot;">​</a></h3><p>To see the decision logic and the prefixed logs on a machine with no GPU, run the supervisor as a CPU node with two synthetic workers, with <code>backend: cpu</code> in the node file:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>REACTANT_GPUS=0 REACTANT_CPU_WORKERS=2 \\</span></span>
<span class="line"><span>  julia --project=packages/ReactantServerNode -e &#39;using ReactantServerNode; ReactantServerNode.supervise(&quot;node.yaml&quot;)&#39;</span></span></code></pre></div><p>You will see <code>worker0</code>, <code>worker1</code>, and <code>gateway</code> start, their logs interleaved with <code>[name]</code> prefixes, and the gateway serving on 8001/8002, exactly the multi-worker shape it takes on a multi-GPU host.</p><h3 id="The-node-file-is-unchanged-when-you-scale" tabindex="-1">The node file is unchanged when you scale <a class="header-anchor" href="#The-node-file-is-unchanged-when-you-scale" aria-label="Permalink to &quot;The node file is unchanged when you scale {#The-node-file-is-unchanged-when-you-scale}&quot;">​</a></h3><p><code>gpus: auto</code> already means &quot;one worker per visible GPU&quot;, so the single-GPU node file from the <a href="./tutorial">Tutorial</a> scales as-is: give the host more GPUs and the supervisor runs more workers. You only edit the node file to do something non-default:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">model_repo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/var/lib/reactantserver/models</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">base_port</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8080</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # worker i listens on base_port + i (8080, 8081, ...)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">metrics_base_port</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9100</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # and metrics on metrics_base_port + i (9100, 9101, ...)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">gpus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">auto</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                # or an integer count, or an explicit device list</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  runtime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    backend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cuda</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  endpoints</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    host</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0.0.0</span></span></code></pre></div><p>A model listed on more than one worker is replicated, and the gateway load-balances requests for it across those workers. See <a href="./node_config">Node Configuration</a> for the <code>models:</code> map and <a href="./on_demand_weights">On-demand Weights</a> for fitting more models than GPU memory holds. The node is described by one YAML node file; the commented templates under <code>config/</code> (<code>node.default.yaml</code>, <code>node.yaml</code>) are reference configs, and gateway scheduling (<code>round_robin</code> or <code>lpt_packing</code>) is covered on the <a href="./gateway">Multi-GPU Gateway</a> page.</p><h2 id="Running-natively" tabindex="-1">Running natively <a class="header-anchor" href="#Running-natively" aria-label="Permalink to &quot;Running natively {#Running-natively}&quot;">​</a></h2><p>Instantiate the workspace once, then run the node supervisor. <code>ReactantServerNode.main()</code> reads <code>REACTANT_NODE_FILE</code> for the node config and honors the standard environment overrides:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># once, to resolve and precompile the workspace (selects the CUDA build via REACTANT_GPU_*):</span></span>
<span class="line"><span>REACTANT_GPU=cuda REACTANT_GPU_VERSION=13.1 \\</span></span>
<span class="line"><span>  julia --project=. -e &#39;using Pkg; Pkg.instantiate(); Pkg.precompile()&#39;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># run the supervisor across four GPUs, serving a bundle directory:</span></span>
<span class="line"><span>CUDA_VISIBLE_DEVICES=0,1,2,3 \\</span></span>
<span class="line"><span>INFERENCE_SERVER_MODEL_DIRS=/path/to/bundles \\</span></span>
<span class="line"><span>REACTANT_NODE_FILE=config/node.default.yaml \\</span></span>
<span class="line"><span>  julia --handle-signals=no --project=packages/ReactantServerNode \\</span></span>
<span class="line"><span>    -e &#39;using ReactantServerNode; ReactantServerNode.main()&#39;</span></span></code></pre></div><p><code>--handle-signals=no</code> lets the supervisor&#39;s own handler run so it shuts its worker children down on SIGTERM. <code>REACTANT_GPU_VERSION</code> selects the Reactant CUDA build (<code>12.9</code> or <code>13.1</code>) and must be set before <code>instantiate</code>. <code>INFERENCE_SERVER_MODEL_DIRS</code> overrides the node file&#39;s model repository (colon-separated); the runtime tunables under <a href="./node_config">Node Configuration</a> take <code>INFERENCE_SERVER_*</code> overrides the same way. For an always-on service, run this command under a process manager such as systemd, with <code>Restart=on-failure</code> and a SIGTERM-based graceful stop (<code>KillMode=mixed</code> pairs with <code>--handle-signals=no</code>).</p><p>Every model compiles to a device executable on every worker before the gRPC plane accepts traffic, so first startup is slow (minutes to hours for a large model set). Watch readiness with <code>curl -sf http://127.0.0.1:8002/readyz</code>, not the process state.</p><h2 id="Running-under-systemd" tabindex="-1">Running under systemd <a class="header-anchor" href="#Running-under-systemd" aria-label="Permalink to &quot;Running under systemd {#Running-under-systemd}&quot;">​</a></h2><p>For an always-on node, run the supervisor from a system service. Put the tunables in an <code>EnvironmentFile</code> and let the unit run the same command as above. Adjust the user, the checkout path, and the GPU list for your host.</p><p><code>/etc/reactantserver/reactantserver.env</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CUDA_VISIBLE_DEVICES=0,1,2,3</span></span>
<span class="line"><span>INFERENCE_SERVER_MODEL_DIRS=/path/to/bundles</span></span>
<span class="line"><span>REACTANT_NODE_FILE=config/node.default.yaml</span></span>
<span class="line"><span>REACTANT_GPU=cuda</span></span>
<span class="line"><span>REACTANT_GPU_VERSION=13.1</span></span></code></pre></div><p><code>/etc/systemd/system/reactantserver.service</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[Unit]</span></span>
<span class="line"><span>Description=ReactantServer node supervisor</span></span>
<span class="line"><span>After=network-online.target</span></span>
<span class="line"><span>Wants=network-online.target</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[Service]</span></span>
<span class="line"><span>Type=exec</span></span>
<span class="line"><span>User=YOUR_DEPLOY_USER</span></span>
<span class="line"><span>WorkingDirectory=/path/to/ReactantServer.jl</span></span>
<span class="line"><span>EnvironmentFile=/etc/reactantserver/reactantserver.env</span></span>
<span class="line"><span># Absolute path to julia: systemd does not source your shell rc, so a juliaup install under the</span></span>
<span class="line"><span># user&#39;s home is not on PATH. \`julia --version\` in a login shell shows the binary to use here.</span></span>
<span class="line"><span>ExecStart=/home/YOUR_DEPLOY_USER/.juliaup/bin/julia --handle-signals=no --project=packages/ReactantServerNode -e &#39;using ReactantServerNode; ReactantServerNode.main()&#39;</span></span>
<span class="line"><span>Restart=on-failure</span></span>
<span class="line"><span>RestartSec=10</span></span>
<span class="line"><span># First boot compiles every model on every worker (minutes to hours) AFTER the unit is already</span></span>
<span class="line"><span># active; systemd cannot gate that (the supervisor sends no sd_notify). Check readiness with</span></span>
<span class="line"><span># \`curl -sf http://127.0.0.1:8002/readyz\`, not \`systemctl is-active\`.</span></span>
<span class="line"><span>TimeoutStartSec=infinity</span></span>
<span class="line"><span># Graceful stop: SIGTERM to the supervisor only, which drains its workers; pairs with</span></span>
<span class="line"><span># --handle-signals=no. Anything still alive after TimeoutStopSec is SIGKILLed.</span></span>
<span class="line"><span>KillMode=mixed</span></span>
<span class="line"><span>KillSignal=SIGTERM</span></span>
<span class="line"><span>TimeoutStopSec=45</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[Install]</span></span>
<span class="line"><span>WantedBy=multi-user.target</span></span></code></pre></div><p>Enable and watch it:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sudo systemctl daemon-reload</span></span>
<span class="line"><span>sudo systemctl enable --now reactantserver.service</span></span>
<span class="line"><span>journalctl -u reactantserver -f</span></span>
<span class="line"><span>until curl -sf http://127.0.0.1:8002/readyz; do sleep 15; done; echo READY</span></span></code></pre></div><p><code>sudo systemctl stop reactantserver</code> sends SIGTERM to the supervisor, which drains its workers within <code>TimeoutStopSec</code> before exiting. Run the workspace <code>Pkg.instantiate()</code> once (as in the previous section) before enabling the unit, so the first start is not also resolving dependencies.</p><h2 id="Docker-container-deployment" tabindex="-1">Docker (container) deployment <a class="header-anchor" href="#Docker-container-deployment" aria-label="Permalink to &quot;Docker (container) deployment {#Docker-container-deployment}&quot;">​</a></h2><p>A container image is an alternative to running the supervisor directly. It runs the same node (supervisor + workers + embedded gateway) with the same <code>:8001</code>/<code>:8002</code> interface:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>git submodule update --init lib/gRPCServer.jl</span></span>
<span class="line"><span>REACTANT_GPU=cuda REACTANT_GPU_VERSION=13.1 julia --project=. -e &#39;using Pkg; Pkg.instantiate()&#39;  # Manifest.toml is gitignored</span></span>
<span class="line"><span>make image        # or: docker build -f docker/Dockerfile -t reactantserver .</span></span>
<span class="line"><span>REACTANTSERVER_MODELS=/path/to/bundles docker compose up</span></span></code></pre></div><p><code>make image</code> builds through the configured engine (<code>ENGINE</code>, default <code>podman</code>). The equivalent without compose is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>docker run --gpus all --ipc=host -p 8001:8001 -p 8002:8002 \\</span></span>
<span class="line"><span>  -v /path/to/bundles:/var/lib/reactantserver/models:ro reactantserver</span></span></code></pre></div><p>The image is a multi-stage build on <code>julia:1.12.6-trixie</code> that copies only <code>libnvJitLink.so.13</code> (the one CUDA userspace library Reactant needs but does not bundle) from an official CUDA 13.1 image; it deliberately does not bring the rest of the CUDA userspace, so the base cannot shadow Reactant&#39;s bundled cuDNN/NCCL. Why that one library matters: Reactant statically links cuBLASLt into <code>libReactantExtra.so</code>, but CUDA-13 cuBLASLt dlopens <code>libnvJitLink.so.13</code> to JIT its GEMM kernels, and the library is neither in the Reactant artifact nor injected by the NVIDIA container runtime (which adds only <code>libcuda.so.1</code>). A bare Julia image without it aborts at the first GPU compile with cuBLASLt&#39;s &quot;Invalid handle was passed to cublasLtCreate&quot;; the copied library fixes that.</p><p>The container needs the host NVIDIA Container Toolkit for GPU access, and the compose file mounts the model repository read-only plus a persistent volume for the Reactant compile cache (autotune results), so tuned kernels survive container recreation. The container shares the host IPC namespace (<code>ipc: host</code>) so POSIX shared-memory regions created by a client are visible to the workers. The autotune knobs are settable as container env (<code>INFERENCE_SERVER_RUNTIME_AUTOTUNE</code>, <code>INFERENCE_SERVER_RUNTIME_AUTOTUNE_CACHE</code>, <code>INFERENCE_SERVER_RUNTIME_AUTOTUNE_CACHE_DIR</code>); the baked default node file sits at <code>/etc/reactantserver/node.yaml</code> and can be overridden by mounting your own over that path. See <code>docker/README.md</code> for details.</p><h2 id="Metrics" tabindex="-1">Metrics <a class="header-anchor" href="#Metrics" aria-label="Permalink to &quot;Metrics {#Metrics}&quot;">​</a></h2><p>One scrape on <code>:8002</code> covers everything. With multiple workers, the embedded gateway serves its own <code>gateway_*</code> series and fans out to each worker&#39;s metrics endpoint, merging them into a single exposition; with a single worker, <code>:8002</code> is that worker&#39;s own <code>/metrics</code>. Each worker tags its series with <code>worker</code> and <code>gpu</code> labels itself (the <code>gpu</code> value is the physical device behind its <code>CUDA_VISIBLE_DEVICES</code>), so per-GPU and per-worker breakdowns need no Prometheus relabeling, for example <code>sum by (gpu) (rate(worker_dispatch_total[1m]))</code>.</p><p>A ready-to-run Prometheus + Grafana stack lives under <code>config/monitoring/</code> with a seven-dashboard suite. Because the node runs natively (not in a container), that stack&#39;s Prometheus scrapes the host at <code>host.docker.internal:8002</code> rather than over a Docker network; if the node listens on a different host, edit the target in <code>prometheus.yml</code> to that host&#39;s <code>address:8002</code>. Grafana is at <code>http://&lt;host&gt;:3000</code> (anonymous viewing on; <code>admin</code> / <code>admin</code> to edit) and Prometheus at <code>http://&lt;host&gt;:9090</code>. See <code>config/monitoring/README.md</code> for the compose commands and dashboards.</p><h2 id="Security" tabindex="-1">Security <a class="header-anchor" href="#Security" aria-label="Permalink to &quot;Security {#Security}&quot;">​</a></h2><p>ReactantServer is designed to run on a trusted network behind your own perimeter. Be aware of the following before exposing any endpoint:</p><ul><li><p><strong>Cleartext h2c.</strong> All gRPC traffic (worker and gateway) is cleartext h2c. TLS settings are parsed by the gateway config but not yet enforced; a configured cert triggers a startup warning.</p></li><li><p><strong>No authentication or authorization</strong> on the KServe data plane, the worker control-plane RPCs (residency and policy), the gateway&#39;s own scheduling control plane (<code>GatewayControlService</code>, which can repack the fleet and change a model&#39;s replica count and routing at runtime), or the Prometheus metrics listener (which binds <code>0.0.0.0:8002</code> by default). The control plane is mutating, so treat access to the gRPC ports as administrative access.</p></li><li><p><strong>Trusted bundles.</strong> A bundle&#39;s optional <code>model.jl</code> executes arbitrary Julia in the server process, so bundles are trusted input. Only serve bundles you built or audited; see <a href="./bundles">Bundles</a>.</p></li><li><p><strong>Shared memory is a local trust boundary.</strong> Client-registered regions and the optional node-shared host-weight store live in <code>/dev/shm</code>; the shared weight regions default to mode <code>666</code> (world-writable) for friction-free sharing. Set <code>runtime.shared_host_weights_mode: &quot;660&quot;</code> on production or multi-user systems.</p></li></ul>`,69)])])}const g=t(o,[["render",i]]);export{u as __pageData,g as default};
