import{_ as a,o as s,c as t,ao as i}from"./chunks/framework.COW0S1sA.js";const u=JSON.parse('{"title":"Scheduling & Coalescing","description":"","frontmatter":{},"headers":[],"relativePath":"scheduling.md","filePath":"scheduling.md","lastUpdated":null}'),o={name:"scheduling.md"};function n(r,e,d,h,l,c){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="Scheduling-and-Coalescing" tabindex="-1">Scheduling &amp; Coalescing <a class="header-anchor" href="#Scheduling-and-Coalescing" aria-label="Permalink to &quot;Scheduling &amp; Coalescing {#Scheduling-and-Coalescing}&quot;">​</a></h1><p>The worker&#39;s scheduler decides which model runs next whenever the GPU is free, and how many queued requests one execution should serve. It is a deficit-weighted, cost-aware, coalescing dispatch engine: concurrent requests land on per-model queues, a single dispatch loop runs exactly one GPU execution at a time, same-model requests are merged into one batched execution at a compiled size, and GPU time is shared by relative model weight and a learned per-batch-size cost estimate. It holds the model registry, the backend, the device memory pool, the <a href="/ReactantServer.jl/previews/PR70/api#ReactantServerCore.SchedulerConfig"><code>SchedulerConfig</code></a>, per-model dispatch state, and the optional on-demand <code>WeightCache</code>. Submit work with <code>ReactantServer.infer</code> and read observability counters with <code>ReactantServer.scheduler_metrics</code>.</p><p>Because it serializes the GPU, the scheduler is the component that turns a burst of concurrent requests into a steady stream of full batches. This page covers that worker-side engine and the coalescing it performs. Routing a model&#39;s requests across several workers, including the gateway scheduling modes (<code>round_robin</code>, <code>least_outstanding</code>, <code>lpt_packing</code>), is the gateway&#39;s job; see <a href="./gateway">Gateway</a>. Where model weights live and how on-demand loading works is covered in <a href="./on_demand_weights">On-demand Weights</a>.</p><h2 id="How-a-request-flows-through-the-worker" tabindex="-1">How a request flows through the worker <a class="header-anchor" href="#How-a-request-flows-through-the-worker" aria-label="Permalink to &quot;How a request flows through the worker {#How-a-request-flows-through-the-worker}&quot;">​</a></h2><p>Each gRPC request arrives on its own task. The request runs its <code>preprocess</code> hook on that task, then is queued on its model&#39;s queue (FIFO, so the front is always the oldest request). The dispatch loop wakes when work is available, selects a model, coalesces that model&#39;s queued requests into one execution at a compiled batch size, runs it, and hands each caller back its raw output slice; the caller&#39;s task runs the <code>postprocess</code> hook on that slice. The loop runs no <code>model.jl</code> code itself, so user hooks never serialize against the GPU.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>       gRPC requests (one task each)</span></span>
<span class="line"><span>                 |</span></span>
<span class="line"><span>   preprocess hook runs on the caller&#39;s task</span></span>
<span class="line"><span>                 |</span></span>
<span class="line"><span>        per-model queues (FIFO, one per model)</span></span>
<span class="line"><span>                 |</span></span>
<span class="line"><span>       +---------+---------+</span></span>
<span class="line"><span>       |   dispatch loop   |   one GPU execution at a time</span></span>
<span class="line"><span>       | select -&gt; coalesce -&gt; run -&gt; slice</span></span>
<span class="line"><span>       +---------+---------+</span></span>
<span class="line"><span>                 |</span></span>
<span class="line"><span>        per-request output slices</span></span>
<span class="line"><span>                 |</span></span>
<span class="line"><span>   postprocess hook runs on the caller&#39;s task</span></span>
<span class="line"><span>                 |</span></span>
<span class="line"><span>          replies to clients</span></span></code></pre></div><p>A request becomes visible to the dispatch loop only after its <code>preprocess</code> has returned, so the loop never executes a request whose hook work has not finished. Because the hooks run on the per-request tasks rather than on the loop, the CPU-side pre/post work of many requests proceeds in parallel and overlaps the single, serialized GPU execution: while the loop runs one model on the GPU, other requests are being pre/post-processed on other threads.</p><p>A single <code>Threads.Condition</code> guards all mutable scheduler state (the per-model queues and the EMA/cost fields) and signals the dispatch loop when work arrives. The loop holds the lock only to select and dequeue; the actual execution runs outside the lock, so producers keep enqueuing during GPU work, and the lock is re-taken briefly afterwards to record the dispatch. Only one execution runs at a time by design, which keeps memory and concurrency reasoning simple and is what makes the single-resident-model weight strategy safe.</p><h3 id="The-dispatch-loop-runs-on-its-own-thread" tabindex="-1">The dispatch loop runs on its own thread <a class="header-anchor" href="#The-dispatch-loop-runs-on-its-own-thread" aria-label="Permalink to &quot;The dispatch loop runs on its own thread {#The-dispatch-loop-runs-on-its-own-thread}&quot;">​</a></h3><p>The supervisor starts each worker with <code>--threads=&lt;compute_threads&gt;,1</code>: a default pool sized to the worker&#39;s share of the host for the per-request pre/post tasks, plus one interactive thread. The scheduler pins its GPU dispatch loop to the interactive thread when one exists, so the GPU dispatch is scheduled promptly and is never starved by the hook tasks that saturate the default pool. With no interactive thread the default pool is used; correctness is unaffected, only the overlap.</p><h2 id="Decision-order" tabindex="-1">Decision order <a class="header-anchor" href="#Decision-order" aria-label="Permalink to &quot;Decision order {#Decision-order}&quot;">​</a></h2><p>Each time the GPU frees up, the scheduler decides in this order: 2. <strong>Dispatch a committed meta sub-call first, if one is pending.</strong> A meta model&#39;s orchestration runs off the dispatch loop and re-enters the scheduler for each sub-call (see <a href="./meta_models">Meta Models</a>); those continuations are committed and jump ahead of the discipline scan, at most one per in-flight meta, so a meta already in progress is not stalled behind newly arrived work.</p><ol start="3"><li><p><strong>Otherwise select the next model</strong>, according to the configured <code>discipline</code>: <code>fair</code> (deficit-weighted and cost-aware, the default), <code>fifo</code> (oldest queued request), or <code>edf</code> (soonest deadline).</p></li><li><p><strong>Coalesce the winner.</strong> The selected model&#39;s queued requests are taken in FIFO order and packed into the largest compiled batch size that fits, padding a partial batch up to the smallest size when needed and always making forward progress on at least the oldest request. The remainder stays queued for the next round.</p></li></ol><h2 id="Disciplines" tabindex="-1">Disciplines <a class="header-anchor" href="#Disciplines" aria-label="Permalink to &quot;Disciplines {#Disciplines}&quot;">​</a></h2><p>The discipline selects which model is dispatched next. Coalescing applies underneath all three: whichever model wins, its queue is coalesced in FIFO order.</p><h3 id="fair-default" tabindex="-1">fair (default) <a class="header-anchor" href="#fair-default" aria-label="Permalink to &quot;fair (default) {#fair-default}&quot;">​</a></h3><p>Each model has a relative <code>weight</code> that defines its share of compute: <code>share = weight / sum(weights)</code>. The scheduler tracks a decaying exponential moving average of how much GPU time each model has recently consumed (decayed to &quot;now&quot; with a half-life of <code>ema_halflife_seconds</code> before every read or write), and scores every model with a non-empty queue by how far below its share it is, divided by its estimated cost:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>priority = clamp(share - recent_compute_ema, -cap, cap) / effective_cost</span></span></code></pre></div><p>A model that has consumed less than its share recently scores higher. Dividing by cost stops an expensive model from blocking cheaper ones on a marginal fairness edge. The clamp (<code>recency_penalty_cap</code>, default 0.25) bounds both lockout and domination: a quiet model cannot run away with the worker, and a saturated one cannot be starved entirely. When every model&#39;s recent-compute EMA is zero (quiet or newly loaded models), the deficit equals the share, so those models stay schedulable rather than normalizing to zero.</p><h3 id="fifo" tabindex="-1">fifo <a class="header-anchor" href="#fifo" aria-label="Permalink to &quot;fifo {#fifo}&quot;">​</a></h3><p>Under <code>fifo</code>, per-model weights are ignored. The model whose oldest queued request is the oldest overall is served first, with ties broken by model name for determinism. This is the discipline to use when the worker sits behind a gateway that is already doing placement; see &quot;Which discipline to use&quot; below.</p><h3 id="edf" tabindex="-1">edf <a class="header-anchor" href="#edf" aria-label="Permalink to &quot;edf {#edf}&quot;">​</a></h3><p>Under <code>edf</code> (earliest-deadline-first), the model whose most-urgent queued request has the soonest deadline is served first. The deadline comes from the request&#39;s remaining-budget timeout; a request with no deadline is treated as least urgent. Ties break by the model&#39;s oldest queued request and then by name, so when all queued requests share a deadline (the common case) <code>edf</code> is exactly <code>fifo</code>. It diverges only to promote a model carrying a request with less budget left, so a meta&#39;s sub-call, which inherits the meta&#39;s deadline, is served ahead of fresher regular work. Queues stay arrival-ordered (never reordered), and coalescing still draws FIFO from the front, so a request deep behind the batch size advances over successive dispatches of its model rather than jumping the batch.</p><p><code>edf</code> also sheds work it cannot finish in time. Before GPU work begins, a request whose deadline has already passed is dropped under every discipline; under <code>edf</code> a laxity check is added: a request is also dropped if it cannot finish within this dispatch&#39;s learned compute cost, so the GPU is not spent on work that will miss anyway (the classic EDF overload failure mode). Dropped requests receive a <code>DeadlineExceeded</code> error (gRPC <code>DEADLINE_EXCEEDED</code> upstream); only feasible requests run, and when none are feasible the execution is skipped entirely. The cost estimate is learned from prior dispatches and unseeded pairs default small, so the laxity drop is a no-op until a model has run once. Committed meta sub-calls skip the laxity drop (their earlier stages already spent GPU time) but still honor the hard deadline check.</p><p>Note that because <code>edf</code> derives urgency solely from the deadline, issuing different per-client deadlines for the same model reorders that model&#39;s service and therefore affects fairness across clients; keep deadlines uniform to retain <code>fifo</code>-like fairness.</p><h3 id="Which-discipline-to-use" tabindex="-1">Which discipline to use <a class="header-anchor" href="#Which-discipline-to-use" aria-label="Permalink to &quot;Which discipline to use {#Which-discipline-to-use}&quot;">​</a></h3><p><code>fair</code> is for workers with no upstream placement authority: a single-GPU worker, or a fleet behind the round-robin gateway, where the worker itself must stop one model from crowding out the rest. Behind a gateway running <code>lpt_packing</code>, the gateway is the fairness and placement authority (see <a href="./gateway">Gateway</a>), and the worker must run <code>fifo</code> or <code>edf</code> so the two do not fight; <code>fair</code> would double-book GPU time against the gateway&#39;s placement decisions. <code>edf</code> is for deadline-sensitive serving, and it degrades gracefully to <code>fifo</code> when clients use uniform deadlines.</p><h2 id="Coalescing" tabindex="-1">Coalescing <a class="header-anchor" href="#Coalescing" aria-label="Permalink to &quot;Coalescing {#Coalescing}&quot;">​</a></h2><p>Coalescing merges same-model requests into one GPU execution. The selected model&#39;s queue is scanned in FIFO order, the requests that fit are concatenated along the batch axis into a single compiled-batch-size dispatch, the execution runs once, and the outputs are sliced back per caller (zeros padding a partial fill are dropped). The remainder stays queued for the next round. The batch sizes themselves come from the bundle&#39;s manifest batching spec: a bundle may carry several compiled batch sizes that share one set of weights, and the scheduler picks the executable that matches each coalesced batch without duplicating parameters.</p><h3 id="When-a-model-can-coalesce" tabindex="-1">When a model can coalesce <a class="header-anchor" href="#When-a-model-can-coalesce" aria-label="Permalink to &quot;When a model can coalesce {#When-a-model-can-coalesce}&quot;">​</a></h3><p>A model coalesces multiple requests only when every executable input and every output carries a batch axis (an all-or-nothing requirement that mirrors the loader&#39;s manifest check). Inputs are then concatenated along that axis and outputs split back per request. Otherwise the model serves one request per dispatch. Single unbatched modules (the batch-0 executable) are never coalesced. Inputs without a batch axis are taken from the first request, since they do not vary across the batch.</p><p>Only requests that resolve to the same input-shape variant coalesce together. A model compiled for several input shapes (aspect-ratio variants) keeps one executable set per variant over a single shared set of weights, and different shapes cannot be concatenated along the batch axis. The coalescing window is the leading run of same-variant requests at the front of the queue, which keeps the taken set a contiguous prefix (so it can be removed in one step) and preserves FIFO order. A request whose shape has no compiled executable dispatches alone so that the caller gets the precise &quot;no compiled program for input shape&quot; error.</p><h3 id="Sizing-the-batch" tabindex="-1">Sizing the batch <a class="header-anchor" href="#Sizing-the-batch" aria-label="Permalink to &quot;Sizing the batch {#Sizing-the-batch}&quot;">​</a></h3><p>The scheduler chooses the largest compiled batch size that the queued rows can fill; a partial fill pads up to the smallest compiled size (the compiled shape may therefore exceed the queued rows, and the dispatch runs padded). Coalescing uses only what is queued now; there is no look-ahead.</p><p>A per-model <code>max_batch_size</code> caps how many rows are coalesced into one dispatch. The cap does not change compiled shapes: a partial fill still pads up to the smallest compiled size, and a single request larger than the cap is never split, so it dispatches alone (growing the chosen size to the smallest compiled batch that fits it when necessary). Forward progress is guaranteed: an indivisible front request that exceeds the batch size or the cap still dispatches alone rather than being held back, so the oldest request is always served within a bounded number of rounds. A lone request that already fills the dispatch passes its inputs straight through with no concatenation or padding.</p><h3 id="Why-coalescing-is-the-throughput-lever" tabindex="-1">Why coalescing is the throughput lever <a class="header-anchor" href="#Why-coalescing-is-the-throughput-lever" aria-label="Permalink to &quot;Why coalescing is the throughput lever {#Why-coalescing-is-the-throughput-lever}&quot;">​</a></h3><p>Packing many requests into one execution amortizes the fixed per-launch overhead and, for a model that had to be loaded on demand, the one-time weight transfer, across every item in the batch. On a representative compute-heavy model, per-image latency drops by roughly three times from batch 1 to batch 32 while images per second more than triples. Coalescing is therefore the reason a worker can absorb bursts: concurrent requests pile onto a model&#39;s queue and the loop converts them into a small number of full dispatches instead of paying launch cost once per request.</p><h2 id="Cost-learning" tabindex="-1">Cost learning <a class="header-anchor" href="#Cost-learning" aria-label="Permalink to &quot;Cost learning {#Cost-learning}&quot;">​</a></h2><p>The scheduler measures the real GPU time of each execution and refines a per-batch-size cost estimate with an exponential moving average (<code>cost_ema_alpha</code>, default 0.2). The estimate initializes to the first measurement, so it converges immediately rather than drifting up from zero. It feeds the fair discipline (the cost divisor, so an expensive model does not crowd out cheaper ones on a marginal fairness edge) and the <code>edf</code> laxity check.</p><p>Two things keep the estimate honest:</p><ul><li><p><strong>Warmup seeds it.</strong> At startup, under the fair discipline, each compiled size is run once on zero-filled inputs and the measured time becomes the estimate. Failures are non-fatal: the default cost covers any unseeded (model, batch size) pair until the first real dispatch measures it. While every candidate is unseeded they all share the default, so the deficit term decides. When the on-demand weight cache is enabled with auto-sizing, the startup isolation probe doubles as this warmup.</p></li><li><p><strong>Weight load time is excluded.</strong> The measured <code>compute_time</code> covers only the model execution, not the on-demand weight load that preceded it, so the estimate reflects steady-state (resident) execution rather than cold-call latency.</p></li></ul><p>Coalescing is also priced fairly: a <code>coalescing_discount</code> (default 0.10) is applied to the effective cost of a coalesced batch, so a larger dispatch is not penalized for being larger when it competes against an unbatched one.</p><h2 id="Per-model-configuration" tabindex="-1">Per-model configuration <a class="header-anchor" href="#Per-model-configuration" aria-label="Permalink to &quot;Per-model configuration {#Per-model-configuration}&quot;">​</a></h2><p>Most models need no tuning: with the default <code>fair</code> discipline, all-default weights yield uniform shares. When you want to change that, per-model overrides live under <code>scheduler.models</code> in the node file, which builds the <a href="/ReactantServer.jl/previews/PR70/api#ReactantServerCore.ModelSchedConfig"><code>ModelSchedConfig</code></a> entries:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  scheduler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      resnet50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        weight</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # relative compute share under fair (default 1.0)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        max_batch_size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # cap on rows coalesced per dispatch (default uncapped)</span></span></code></pre></div><p><code>weight</code> sets the model&#39;s fair share. <code>residency</code> is also settable per model (the initial residency floor); see <a href="./on_demand_weights">On-demand Weights</a> for what the floors mean. <code>max_batch_size</code> caps coalescing rows per dispatch, as described above; it does not change compiled shapes and never splits a request. The full node-file surface, including the global <code>scheduler:</code> block, is in <a href="./node_config">Node Configuration</a>.</p><p><code>max_queue_depth</code> caps each model&#39;s queue independently: a full model rejects new requests without affecting admission for the others, so one backlogged model cannot starve the rest of the worker.</p><h3 id="Live-policy-changes" tabindex="-1">Live policy changes <a class="header-anchor" href="#Live-policy-changes" aria-label="Permalink to &quot;Live policy changes {#Live-policy-changes}&quot;">​</a></h3><p>The control plane can adjust scheduling policy on a running worker. <code>ReactantServer.set_policy!</code> updates a model&#39;s live <code>weight</code> (consulted by the fair discipline; available in both residency modes, and it raises on an unknown model). <code>ReactantServer.set_residency!</code> moves a model to a target residency floor; it is only meaningful in externally-managed mode and the worker rejects it otherwise. Both run on the dispatch thread, the sole mutator of residency, and block until applied; control commands are drained ahead of dispatch selection, so a residency transition takes effect before the dispatch that may depend on it. <code>ReactantServer.compact!</code> frees the resident non-pinned weights so the allocator coalesces its free list, then reloads the listed models, defragmenting the device arena; see <a href="./on_demand_weights">On-demand Weights</a>.</p><h2 id="Observability" tabindex="-1">Observability <a class="header-anchor" href="#Observability" aria-label="Permalink to &quot;Observability {#Observability}&quot;">​</a></h2><p><code>ReactantServer.scheduler_metrics</code> snapshots per-model state: dispatch count, requests served, rows served, total compute, the current recent-compute EMA, queue depth, queue-wait P50/P99, the histogram of dispatched batch sizes, the effective max batch, and residency. The counters are cumulative over the worker&#39;s lifetime, so deltas between polls give rates; <code>dispatch_count</code> counts coalesced batch executions, <code>requests_served</code> counts individual requests (at least one per dispatch, more under coalescing), and <code>rows_served</code> counts batch-axis rows, the effective batch size actually executed.</p><p>The worker exports these as Prometheus series (<code>worker_dispatch_total</code>, <code>worker_compute_seconds_total</code>, <code>worker_queue_depth</code>, <code>worker_queue_wait_seconds</code>, <code>worker_requests_served_total</code>, <code>worker_rows_served_total</code>, <code>worker_model_max_batch_size</code>, and the residency gauges), tagged per model and per GPU, and the node&#39;s metrics endpoint aggregates them. See <a href="./deployment">Deployment</a> for the scrape configuration.</p><p><code>ReactantServer.control_status</code> is the control-plane view a gateway reads: the worker&#39;s residency mode and discipline, and for each model its weight, queue depth, cumulative serving counters, effective max batch, and the wire-facing batch axis (the input name and 1-based axis a gateway can count items on without decoding a request). A meta is reported as a single unit whose footprint is the sum of its sub-models, so the gateway packs and routes the group as one. See <a href="./meta_models">Meta Models</a> and <a href="./gateway">Gateway</a>.</p><h2 id="Configuration-reference" tabindex="-1">Configuration reference <a class="header-anchor" href="#Configuration-reference" aria-label="Permalink to &quot;Configuration reference {#Configuration-reference}&quot;">​</a></h2><p>The <code>scheduler:</code> block in the node file maps to <a href="/ReactantServer.jl/previews/PR70/api#ReactantServerCore.SchedulerConfig"><code>SchedulerConfig</code></a>, and entries under <code>scheduler.models</code> map to <a href="/ReactantServer.jl/previews/PR70/api#ReactantServerCore.ModelSchedConfig"><code>ModelSchedConfig</code></a>. The discipline enum is <a href="/ReactantServer.jl/previews/PR70/api#ReactantServerCore.SchedulingDiscipline"><code>SchedulingDiscipline</code></a>. Defaults and YAML keys:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  scheduler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    discipline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">fair</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               # fair | fifo | edf (fair is the default)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    ema_halflife_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # fair: half-life of the recent-compute EMA</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    recency_penalty_cap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.25</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # fair: clamp on the share-vs-EMA deficit</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    coalescing_discount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.10</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # fair: cost discount applied to coalesced batches</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    cost_ema_alpha</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.2</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # fair/edf: smoothing for the learned per-batch-size cost</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    max_queue_depth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1024</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # per-model queue cap; a full model rejects new requests</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    dispatch_timeout_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # per-request execution timeout</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    compaction_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # worker-local memory compaction every N on-demand loads</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {}                     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># per-model overrides (weight, residency, max_batch_size)</span></span></code></pre></div><p>Every field is also settable with an <code>INFERENCE_SERVER_SCHEDULER_*</code> environment variable (for example <code>INFERENCE_SERVER_SCHEDULER_DISCIPLINE=fifo</code>). See the <a href="./api">API Reference</a> for the full <a href="/ReactantServer.jl/previews/PR70/api#ReactantServerCore.SchedulerConfig"><code>SchedulerConfig</code></a> documentation.</p>`,57)])])}const m=a(o,[["render",n]]);export{u as __pageData,m as default};
