import{_ as s,o as a,c as i,ao as t}from"./chunks/framework.D4i3nfEM.js";const k=JSON.parse('{"title":"Bundles & model.jl","description":"","frontmatter":{},"headers":[],"relativePath":"bundles.md","filePath":"bundles.md","lastUpdated":null}'),n={name:"bundles.md"};function o(r,e,d,l,h,p){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="Bundles-and-model.jl" tabindex="-1">Bundles &amp; model.jl <a class="header-anchor" href="#Bundles-and-model.jl" aria-label="Permalink to &quot;Bundles &amp; model.jl {#Bundles-and-model.jl}&quot;">​</a></h1><p>A model is delivered to the server as a self-contained bundle: a directory holding a compiled model (an MLIR module Reactant compiles, a StableHLO program today), its weights, and a manifest. Bundles are produced offline by the conversion tooling and loaded at server startup.</p><h2 id="Bundle-layout" tabindex="-1">Bundle layout <a class="header-anchor" href="#Bundle-layout" aria-label="Permalink to &quot;Bundle layout {#Bundle-layout}&quot;">​</a></h2><p>A bundle is a directory containing:</p><ul><li><p><code>manifest.yaml</code> - the metadata parsed into a <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.Manifest"><code>Manifest</code></a>: I/O specs, dtypes, shapes, and the compiled batch sizes.</p></li><li><p><code>model.mlir</code> - the MLIR module Reactant compiles, currently a serialized StableHLO portable artifact (single batch size), or one module per size as <code>model.b{N}.mlir</code> sharing a single <code>weights.safetensors</code>.</p></li><li><p><code>weights.safetensors</code> - the model weights, memory-mapped at load time.</p></li><li><p><code>model.jl</code> - optional; registers custom pre/post-processing (see below).</p></li></ul><p>The directory name is the model name: renaming the directory renames the model, with no edits to the manifest or <code>model.jl</code> (a <code>name</code> declared in either is informational and ignored). Each immediate subdirectory of <code>model_repo</code> that contains a <code>manifest.yaml</code> is a bundle. In <code>dynamic</code> mode a renamed bundle directory (same contents) is detected by the watcher and renamed in place, keeping the compiled executables and resident weights; only new or changed bundles are compiled.</p><h2 id="Manifest-shape-encoding" tabindex="-1">Manifest shape encoding <a class="header-anchor" href="#Manifest-shape-encoding" aria-label="Permalink to &quot;Manifest shape encoding {#Manifest-shape-encoding}&quot;">​</a></h2><p>Each tensor&#39;s shape is an einsum-style string of single ASCII letters, one per axis, with a companion <code>dims:</code> map giving the size of every non-batch letter:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">executable_inputs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">input</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    dtype</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">f32</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    shape</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;chwn&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # channel, height, width, batch</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    dims</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      c</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      h</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">224</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      w</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">224</span></span></code></pre></div><p>The letters <code>n</code> and <code>b</code> are reserved batch markers (at most one occurrence per tensor). Other letters are tensor-scoped (no implicit cross-tensor equality) and must be unique within a single shape. A size of <code>-1</code> in <code>dims</code> marks a variable axis, a <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.Dim"><code>Dim</code></a> with kind <code>VARIABLE</code>: a dynamic non-batch axis. On <code>client_outputs</code> that pass through <code>model.jl</code> that is how a result with a data-dependent extent is declared, for example the detection count of a postprocessed detector. On executable inputs a variable axis is servable only when the compiled variants are enumerated in a top-level <code>input_shapes</code> block (see <code>write_bundle</code> below); the <a href="./object_detection">Object Detection</a> example compiles one weight set for several image shapes this way.</p><p>The per-input batch axis is derived from the position of <code>n</code>/<code>b</code>; at inference the request&#39;s size along that axis must equal one of <code>batching.compiled_batch_sizes</code>. Each tensor parses into a <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.TensorSpec"><code>TensorSpec</code></a> with a <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.Dim"><code>Dim</code></a> per axis, and the compiled sizes form the <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.BatchingSpec"><code>BatchingSpec</code></a>. The writer stamps <code>format_version: &quot;2.0&quot;</code>; the loader accepts <code>2.0</code> or <code>2</code>.</p><p>Shapes use the Julia column-major convention (the batch dimension is the last axis), which is the reverse of the row-major form a Python/XLA exporter would write. The wire codec handles the conversion, so KServe clients see canonical row-major shapes.</p><p>Datatypes are written as manifest tokens (<code>f32</code>, <code>bf16</code>, <code>i64</code>, <code>bool</code>, and so on); see <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.DType"><code>DType</code></a> for the full mapping between tokens, Julia types, and KServe wire strings. Client-facing tensors must use a dtype that has a KServe wire mapping (FP8 is executable-only).</p><h2 id="Custom-pre/post-processing-with-model.jl" tabindex="-1">Custom pre/post-processing with model.jl <a class="header-anchor" href="#Custom-pre/post-processing-with-model.jl" aria-label="Permalink to &quot;Custom pre/post-processing with model.jl {#Custom-pre/post-processing-with-model.jl}&quot;">​</a></h2><p>A bundle may include a <code>model.jl</code> that calls <a href="/ReactantServer.jl/previews/PR73/api#ReactantServer.register_model-Tuple{AbstractString}"><code>register_model</code></a> to attach <code>preprocess</code> and <code>postprocess</code> hooks. Both hooks receive and return a <code>Vector{NamedTensor}</code> (see <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.NamedTensor"><code>NamedTensor</code></a>); omitted hooks default to identity.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># model.jl, inside the bundle directory</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ReactantServer</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> normalize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(inputs)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # inputs :: Vector{NamedTensor}; transform and return a Vector{NamedTensor}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inputs</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> to_classes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(outputs)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # e.g. map logits to class ids</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> outputs</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">register_model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;resnet50&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; preprocess</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">normalize, postprocess</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">to_classes)</span></span></code></pre></div><p>The worker runs the hooks on each request&#39;s own task (preprocess before the request is queued, postprocess on the result), crossing the world-age boundary with <code>invokelatest</code>. This means the hooks for different requests run concurrently, on multiple threads, overlapping the GPU execution: keep them free of shared mutable state (or guard it yourself). When <code>model.jl</code> transforms the I/O, declare the client-facing tensors via <code>client_inputs</code> / <code>client_outputs</code> in the manifest; without a <code>model.jl</code> those keys are not permitted and the executable specs are the client-facing specs. See <a href="/ReactantServer.jl/previews/PR73/api#ReactantServer.register_model-Tuple{AbstractString}"><code>register_model</code></a> in the API reference for the exact hook signatures.</p><p>For a bundle whose <code>model.jl</code> chains several models with data-dependent logic rather than wrapping one executable, see <a href="./meta_models">Meta Models</a>.</p><h2 id="Producing-bundles" tabindex="-1">Producing bundles <a class="header-anchor" href="#Producing-bundles" aria-label="Permalink to &quot;Producing bundles {#Producing-bundles}&quot;">​</a></h2><p><code>ReactantServerExport</code> produces bundles offline and is kept out of the server&#39;s dependency graph. It is not part of the server runtime.</p><p>A project that owns a Lux model (or any Reactant-traceable function) uses <code>ReactantServerExport</code>; Lux itself is not a dependency of the package:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ReactantServerExport</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">export_bundle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:lux</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, model, ps, st, example_input;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    dir</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bundles/mlp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mlp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, batch_sizes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span></code></pre></div><p>The <code>:lux</code> frontend traces <code>model(x, ps, st)</code> at each requested batch size, taking the first return as the output; the batch dimension is the last Julia axis (the Lux convention) and the leading network axis. A multi-input / multi-output variant takes <code>example_inputs::Tuple</code> and <code>output_select</code> mapping the raw model output to the ordered tuple of arrays to export; per-tensor batch axes default to each array&#39;s last Julia axis, overridable with <code>input_batch_axes</code> / <code>output_batch_axes</code> (1-based Julia axes, or <code>nothing</code> to opt one tensor out of batching).</p><p>The <code>:reactant</code> frontend is the generic form: any Reactant-traceable <code>f(inputs..., weights...)</code> with explicit <code>name =&gt; array</code> weight pairs, producing a single unbatched <code>model.mlir</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">export_bundle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:reactant</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, f, (x,), [(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;w1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, w1), (</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;b1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, b1)];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    dir</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bundles/mlp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mlp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>A PyTorch project also loads <code>PythonCall</code>, which triggers the package extension driving <code>torch.export.export</code> and torchax:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ReactantServerExport, PythonCall</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">export_bundle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:pytorch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, model, (example_input,);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    dir</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bundles/mlp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mlp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, batch_sizes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span></code></pre></div><p>The <code>:lux</code> and <code>:pytorch</code> frontends trace once per requested batch size and write a server-loadable bundle. The batch dimension is the last Julia axis (the leading PyTorch axis after the row-major / column-major reversal). <code>export_bundle(:torchscript, ...)</code> exports a TorchScript artifact (a <code>.pt</code> file or a loaded <code>ScriptModule</code>) through the same extension; without <code>PythonCall</code> the <code>:pytorch</code> and <code>:torchscript</code> calls fail with a message directing you to load it.</p><h3 id="write_bundle-and-IOSpec" tabindex="-1">write_bundle and IOSpec <a class="header-anchor" href="#write_bundle-and-IOSpec" aria-label="Permalink to &quot;write_bundle and IOSpec {#write_bundle-and-IOSpec}&quot;">​</a></h3><p><code>write_bundle</code> is the low-level writer: it takes the executable <code>IOSpec</code>s, the StableHLO modules (one per batch size, or text or serialized bytes), and an ordered <code>weights</code> collection whose order becomes the safetensors <code>argument_order</code>, and emits <code>manifest.yaml</code>, the <code>model[.b{N}].mlir</code> files, and <code>weights.safetensors</code>. An <code>IOSpec</code> names a tensor, its network (row-major) shape, its dtype, the 0-based <code>batch_axis</code>, and optional explicit <code>letters</code> for the non-batch axes (the reserved markers <code>n</code>/<code>b</code> are rejected); a <code>-1</code> axis size encodes a variable axis. The bundle directory&#39;s basename must equal <code>name</code> (the identity rule above).</p><p>With <code>input_shapes</code> given, the writer marks the variable executable-input axes <code>-1</code> in the manifest and emits one module set per compiled variant as <code>model.v{i}[.b{N}].mlir</code>, all sharing the single <code>weights.safetensors</code>; each variant lists the variable-axis sizes in (input, axis) order, and the variants must share one set of batch sizes. <code>client_inputs</code> / <code>client_outputs</code> (each <code>nothing</code> or a <code>Vector{IOSpec}</code>) declare the wire-facing spec when it differs from the executable spec, for bundles whose <code>model.jl</code> transforms between the two; they are emitted only when given.</p><p><code>collect_provenance</code> captures best-effort reproducibility metadata for a bundle (repo remote, commit, tree, branch, dirty flag, Julia and exporter versions, and an <code>exported_at</code> timestamp). <code>export_bundle</code> merges it into the manifest&#39;s <code>provenance</code> block; on a dirty work tree the uncommitted diff is written to the bundle as <code>working_tree.patch</code> rather than embedded in the manifest, and <code>git_commit</code> plus <code>git apply --binary working_tree.patch</code> reconstructs the exported code exactly.</p><p>The test suite also builds small bundles directly; see <code>test/stablehlo_fixtures.jl</code>.</p><h2 id="Related-pages" tabindex="-1">Related pages <a class="header-anchor" href="#Related-pages" aria-label="Permalink to &quot;Related pages {#Related-pages}&quot;">​</a></h2><p>The manifest and boundary types are documented on the <a href="./api">API</a> page: <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.Manifest"><code>Manifest</code></a>, <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.TensorSpec"><code>TensorSpec</code></a>, <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.Dim"><code>Dim</code></a>, <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.BatchingSpec"><code>BatchingSpec</code></a>, <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.load_manifest-Tuple{AbstractString}"><code>load_manifest</code></a>, <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.DType"><code>DType</code></a>, and <a href="/ReactantServer.jl/previews/PR73/api#ReactantServerCore.NamedTensor"><code>NamedTensor</code></a>. <code>export_bundle</code>, <code>write_bundle</code>, <code>IOSpec</code>, and <code>collect_provenance</code> are documented in the <code>ReactantServerExport</code> docstrings. The <a href="./tutorial">Tutorial</a> walks the full export-to-serve path, and <a href="./node_config">Node Configuration</a> covers how the server loads and watches a repository of bundles.</p>`,35)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
