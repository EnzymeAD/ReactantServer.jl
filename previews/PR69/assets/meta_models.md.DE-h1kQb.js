import{_ as s,o as a,c as t,ao as i}from"./chunks/framework.Do-vtUTZ.js";const k=JSON.parse('{"title":"Meta Models","description":"","frontmatter":{},"headers":[],"relativePath":"meta_models.md","filePath":"meta_models.md","lastUpdated":null}'),n={name:"meta_models.md"};function l(h,e,o,r,p,d){return a(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="Meta-Models" tabindex="-1">Meta Models <a class="header-anchor" href="#Meta-Models" aria-label="Permalink to &quot;Meta Models {#Meta-Models}&quot;">​</a></h1><p>A meta model is a bundle whose <code>model.jl</code> orchestrates <em>other</em> models with ordinary Julia in between, rather than wrapping a single compiled executable. It exists for the logic <code>torch.export</code> cannot trace: data-dependent control flow, loops whose bounds depend on a tensor&#39;s contents, or a pipeline where stage two&#39;s inputs are computed from stage one&#39;s outputs. The server runs the orchestration as a normal inference request, so a meta model is addressable by clients under its own name exactly like any other model.</p><p>If your model is a single traced graph, use a plain bundle with optional pre/post hooks (see <a href="./bundles">Bundles</a>). Reach for a meta model only when the glue between sub-models is real program logic.</p><h2 id="Anatomy" tabindex="-1">Anatomy <a class="header-anchor" href="#Anatomy" aria-label="Permalink to &quot;Anatomy {#Anatomy}&quot;">​</a></h2><p>A meta bundle is a directory with a <code>manifest.yaml</code> and a <code>model.jl</code>, and no compiled artifact or weights of its own. The manifest declares <code>kind: meta</code>, lists the models the orchestration is allowed to call, and must carry <code>client_inputs</code>/<code>client_outputs</code> (there is no executable to infer the client-facing I/O from):</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">format_version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">two_stage</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">meta</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">meta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  calls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">two_stage_encode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">two_stage_score</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">client_inputs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - {</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">TOKENS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">dtype</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">i64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">shape</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">nb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">dims</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">b</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">client_outputs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - {</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">SCORES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">dtype</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">f32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">shape</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">nc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">dims</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">c</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">512</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}}</span></span></code></pre></div><p>The <code>model.jl</code> calls <a href="/ReactantServer.jl/previews/PR69/api#ReactantServer.register_meta_model-Tuple{AbstractString}"><code>register_meta_model</code></a> with the orchestration function:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">register_meta_model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;two_stage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; run </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (inputs, call)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    feats </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;two_stage_encode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, inputs)     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># stage one: encode</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    rows  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rank_rows</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(feats)                     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ordinary Julia, data-dependent</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    out   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;two_stage_score&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, rows)        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># stage two: score</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ReactantServer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NamedTensor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SCORES&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, out[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data)]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>run</code> has the form <code>run(inputs::Vector{NamedTensor}, call) -&gt; Vector{NamedTensor}</code>. The injected <code>call(model_name, inputs)</code> invokes another model and returns its outputs as <a href="/ReactantServer.jl/previews/PR69/api#ReactantServerCore.NamedTensor"><code>NamedTensor</code></a>s. The callee must appear in <code>meta.calls</code>; calling an undeclared model is a loud error. The loader also rejects a meta bundle whose <code>model.jl</code> calls <code>register_model</code> instead of <code>register_meta_model</code>.</p><h2 id="Execution:-local,-on-the-request-task,-one-meta-at-a-time" tabindex="-1">Execution: local, on the request task, one meta at a time <a class="header-anchor" href="#Execution:-local,-on-the-request-task,-one-meta-at-a-time" aria-label="Permalink to &quot;Execution: local, on the request task, one meta at a time {#Execution:-local,-on-the-request-task,-one-meta-at-a-time}&quot;">​</a></h2><p>A meta&#39;s <code>run(inputs, call)</code> executes on the gRPC request task, off the GPU dispatch loop, exactly like a model&#39;s pre/post hooks. Each <code>call(name, inputs)</code> re-enters the local scheduler in-process: the sub-model is dispatched on the loop and its result handed back to the orchestration. There is no gateway round-trip and no serialization between stages; a tensor handed from one stage to the next is passed by reference. The same <code>model.jl</code> runs unchanged on a single worker or in a fleet; the author never writes routing and never needs to know where anything is placed, because a meta only ever runs where its sub-models already live (see Placement, below).</p><p>Two rules shape how a meta shares the GPU with everything else on the worker:</p><ul><li><p><strong>A bounded number of GPU metas at a time.</strong> A per-worker gate admits <code>REACTANT_META_CONCURRENCY</code> meta orchestrations at once, default <strong>2</strong>. A meta holds its permit across the whole run, including the CPU glue between stages, but it does <strong>not</strong> hold the GPU during that glue. While a meta computes between stages, the dispatch loop is free and serves other models, so the GPU is not idle during the glue. The default of 2 lets two heavy pipelines overlap (one&#39;s glue hides behind another&#39;s GPU stage); at 1 two heavy metas serialize, while one holds the gate the other waits and can miss its deadline. Raising it further favors meta completion via more concurrency, but more metas share the serial GPU and the scratch pool (size it for that many concurrent metas). A <strong>compute-only</strong> meta (empty <code>meta.calls</code>, below) issues no sub-calls and bypasses the gate entirely, so a heavy pure-Julia meta never holds a permit a GPU meta needs.</p></li><li><p><strong>In-flight sub-calls jump the line.</strong> Once a meta holds the gate, each of its GPU stages is dispatched ahead of the queued regular work rather than waiting behind it. The number that can cut the line is tied to the gate: each in-flight meta contributes at most one pending sub-call, so the committed set is bounded by <code>REACTANT_META_CONCURRENCY</code>. At a setting of 1 exactly one meta cuts the line and the behavior is fully deterministic; raising the gate lets that many metas cut the line symmetrically (no contention for a single privileged slot), so the one knob sets both how many metas run and how many are prioritized: a single dial for how strongly to favor meta completion.</p></li></ul><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Client --infer(two_stage)--&gt; Gateway</span></span>
<span class="line"><span>  Gateway --routes the whole group to the one worker that holds it--&gt; Worker request task</span></span>
<span class="line"><span>    Worker: run(inputs, call), holds a meta-gate permit</span></span>
<span class="line"><span>            (up to REACTANT_META_CONCURRENCY, default 2)</span></span>
<span class="line"><span>      call(&quot;two_stage_encode&quot;, input)   committed, jumps the queue --&gt; dispatch loop / GPU</span></span>
<span class="line"><span>      feature maps &lt;---------------------------------------------------------------|</span></span>
<span class="line"><span>      Julia glue (the GPU is free; other models&#39; requests are dispatched)</span></span>
<span class="line"><span>      call(&quot;two_stage_score&quot;, feats)    by reference, committed --------------&gt; dispatch loop / GPU</span></span>
<span class="line"><span>      scores &lt;--------------------------------------------------------------------------|</span></span>
<span class="line"><span>      assembled outputs</span></span>
<span class="line"><span>  Worker --outputs--&gt; Gateway --outputs--&gt; Client</span></span></code></pre></div><h2 id="How-it-interacts-with-batch-scheduling" tabindex="-1">How it interacts with batch scheduling <a class="header-anchor" href="#How-it-interacts-with-batch-scheduling" aria-label="Permalink to &quot;How it interacts with batch scheduling {#How-it-interacts-with-batch-scheduling}&quot;">​</a></h2><p>A meta no longer holds the GPU for its whole run. Each stage is an ordinary dispatch on the loop, so the GPU is occupied only while a stage actually computes and is free during the meta&#39;s CPU glue, when the loop serves other models. The trade is the gate: while a meta holds its permit, a second meta on the same worker waits (its stages cannot start), though the GPU is not wasted because regular models run in the meantime. A stage runs at batch 1 for the meta unless other clients&#39; calls to the same sub-model happen to coalesce with it on the loop.</p><p>A meta carries a deadline like any request. It is dropped before it starts if the deadline has already passed or if it cannot take a gate permit in time, so a meta backlog sheds at admission rather than piling up, and no GPU is spent on a meta that will not run. Once a meta is in flight, its continuation sub-calls are <strong>committed</strong>: they skip the predictive laxity drop, so a stage is never shed merely because it might run long after earlier stages already consumed GPU. They still honor the base check, so a meta whose deadline genuinely elapses mid-pipeline stops at the next stage boundary (raising <code>DeadlineExceeded</code>) instead of running the rest. The net effect is that a meta either runs to completion or is shed at a clean boundary, never silently discarding a stage it already paid for. Under the <code>edf</code> discipline the committed sub-call inherits the meta&#39;s deadline, so it is ordered ahead of fresher work (see the discipline notes in <a href="./node_config">Node Configuration</a>).</p><h2 id="Placement:-the-group-travels-together" tabindex="-1">Placement: the group travels together <a class="header-anchor" href="#Placement:-the-group-travels-together" aria-label="Permalink to &quot;Placement: the group travels together {#Placement:-the-group-travels-together}&quot;">​</a></h2><p>A meta owns no weights or executable of its own, so it is not placed on a GPU by itself. Instead the meta and the sub-models it calls form a group that the gateway places as a single unit. The worker reports the meta to the gateway with a memory footprint equal to the sum of its sub-models&#39; weights and hides the sub-models from discovery, so the gateway packs and routes the group by the meta&#39;s own traffic and never sees the individual stages. When the group is placed on a GPU, the sub-models&#39; weights are kept resident there together, so the meta&#39;s in-process sub-calls always find their executables and weights local.</p><p>This is the inverse of routing each sub-call independently. The author still writes the orchestration with no knowledge of placement, but the cost of that abstraction is now paid once, at placement time (the group is co-located), rather than per sub-call at request time (a hop to wherever each stage happened to land). The sub-models are internal to the meta: they are never addressable by clients and never appear in the gateway&#39;s routing table.</p><h2 id="Reuse-buffers-with-call.scratch" tabindex="-1">Reuse buffers with <code>call.scratch</code> <a class="header-anchor" href="#Reuse-buffers-with-call.scratch" aria-label="Permalink to &quot;Reuse buffers with \`call.scratch\` {#Reuse-buffers-with-call.scratch}&quot;">​</a></h2><p>A meta&#39;s data-dependent glue often builds a large intermediate, for example a feature tensor of tens of megabytes handed from one stage to the next. Allocating that fresh on every request drives GC pressure. The injected <code>call</code> exposes a reuse-buffer allocator for this case:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">feats </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> call</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">scratch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">256</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">512</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), Float32)      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># from the worker&#39;s reuse pool (or a plain array)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">fill_features!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(feats, inputs)                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># write directly into it</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">out </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;two_stage_score&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [ReactantServer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NamedTensor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;FEATS&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, feats)])</span></span></code></pre></div><p><code>call.scratch</code> returns plain <code>Array</code>s aliasing the pool&#39;s backing (or, with no pool configured, fresh heap arrays), so a buffer can be handed straight to the next stage as a <code>NamedTensor</code> by reference; either way the <code>model.jl</code> is identical. The pool is plain memory, local to the worker, and never shared across processes. It exists purely to keep large intermediates off the per-request allocation path; because the sub-call runs in-process, the buffer is handed to the next stage by reference, with no copy. A meta that never calls <code>scratch</code> just allocates normally. The pool defaults to 1 GiB across 16 slots, configured per worker, and the acquired slots are released when the orchestration returns. Concurrent metas share the pool, so size it for that many in-flight metas; the deadline-bounded acquire degrades gracefully if it is ever starved. The client driver exposes the same <code>scratch</code> interface for staging inference inputs (see <a href="./client">Client Usage</a>).</p><p>Request every buffer in one <code>call.scratch</code> call (pass a vector of <code>dims =&gt; T</code> pairs to get several at once); calling it more than once per request is rejected, and a scratch buffer must reach the sub-call as a contiguous array (a reshape or contiguous prefix is fine).</p><h2 id="Constraints" tabindex="-1">Constraints <a class="header-anchor" href="#Constraints" aria-label="Permalink to &quot;Constraints {#Constraints}&quot;">​</a></h2><ul><li><p><strong>A meta model may not call another meta model.</strong> This is validated at load: a group is exactly one level deep.</p></li><li><p><strong>Compute-only metas are allowed.</strong> A meta may declare an empty <code>meta.calls</code> and do all its work in Julia, calling no sub-models at all. This is useful for logic that is awkward to express as a traced graph but needs no separate executable. A compute-only meta issues no GPU sub-calls, so it bypasses the meta gate and runs on its request task without taking a permit.</p></li><li><p><strong>Sub-models are internal.</strong> A model named in some meta&#39;s <code>meta.calls</code> is reachable only through that meta, never addressed by clients directly, and is hidden from the gateway&#39;s discovery and placement; the meta&#39;s group carries it.</p></li></ul><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><p><a href="./object_detection">Object Detection</a> for a worked end-to-end meta model: converting a torchvision Faster R-CNN into two StableHLO stages chained by data-dependent Julia glue</p></li><li><p><a href="./bundles">Bundles</a> for the plain (non-meta) bundle path and pre/post hooks</p></li><li><p><a href="./gateway">Multi-GPU Gateway</a> for how the gateway routes and places models</p></li><li><p><a href="./node_config">Node Configuration</a> for the scheduling disciplines, including <code>edf</code></p></li><li><p><a href="./client">Client Usage</a> for the client-side <code>scratch</code> interface</p></li><li><p><a href="/ReactantServer.jl/previews/PR69/api#ReactantServer.register_meta_model-Tuple{AbstractString}"><code>register_meta_model</code></a> in the API reference</p></li></ul>`,29)])])}const g=s(n,[["render",l]]);export{k as __pageData,g as default};
