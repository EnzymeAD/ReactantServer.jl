var documenterSearchIndex = {"docs":
[{"category":"section","location":"scheduling/#Scheduling-and-Coalescing","page":"Scheduling","text":"The worker's scheduler decides which model runs next whenever the GPU is free, and how many queued requests one execution should serve. It is a deficit-weighted, cost-aware, coalescing dispatch engine: concurrent requests land on per-model queues, a single dispatch loop runs exactly one GPU execution at a time, same-model requests are merged into one batched execution at a compiled size, and GPU time is shared by relative model weight and a learned per-batch-size cost estimate. It holds the model registry, the backend, the device memory pool, the SchedulerConfig, per-model dispatch state, and the optional on-demand WeightCache. Submit work with ReactantServer.infer and read observability counters with ReactantServer.scheduler_metrics.\n\nBecause it serializes the GPU, the scheduler is the component that turns a burst of concurrent requests into a steady stream of full batches. This page covers that worker-side engine and the coalescing it performs. Routing a model's requests across several workers, including the gateway scheduling modes (round_robin, least_outstanding, lpt_packing), is the gateway's job; see Gateway. Where model weights live and how on-demand loading works is covered in On-demand Weights.","title":"Scheduling & Coalescing"},{"category":"section","location":"scheduling/#How-a-request-flows-through-the-worker","page":"Scheduling","text":"Each gRPC request arrives on its own task. The request runs its preprocess hook on that task, then is queued on its model's queue (FIFO, so the front is always the oldest request). The dispatch loop wakes when work is available, selects a model, coalesces that model's queued requests into one execution at a compiled batch size, runs it, and hands each caller back its raw output slice; the caller's task runs the postprocess hook on that slice. The loop runs no model.jl code itself, so user hooks never serialize against the GPU.\n\n       gRPC requests (one task each)\n                 |\n   preprocess hook runs on the caller's task\n                 |\n        per-model queues (FIFO, one per model)\n                 |\n       +---------+---------+\n       |   dispatch loop   |   one GPU execution at a time\n       | select -> coalesce -> run -> slice\n       +---------+---------+\n                 |\n        per-request output slices\n                 |\n   postprocess hook runs on the caller's task\n                 |\n          replies to clients\n\nA request becomes visible to the dispatch loop only after its preprocess has returned, so the loop never executes a request whose hook work has not finished. Because the hooks run on the per-request tasks rather than on the loop, the CPU-side pre/post work of many requests proceeds in parallel and overlaps the single, serialized GPU execution: while the loop runs one model on the GPU, other requests are being pre/post-processed on other threads.\n\nA single Threads.Condition guards all mutable scheduler state (the per-model queues and the EMA/cost fields) and signals the dispatch loop when work arrives. The loop holds the lock only to select and dequeue; the actual execution runs outside the lock, so producers keep enqueuing during GPU work, and the lock is re-taken briefly afterwards to record the dispatch. Only one execution runs at a time by design, which keeps memory and concurrency reasoning simple and is what makes the single-resident-model weight strategy safe.","title":"How a request flows through the worker"},{"category":"section","location":"scheduling/#The-dispatch-loop-runs-on-its-own-thread","page":"Scheduling","text":"The supervisor starts each worker with --threads=<compute_threads>,1: a default pool sized to the worker's share of the host for the per-request pre/post tasks, plus one interactive thread. The scheduler pins its GPU dispatch loop to the interactive thread when one exists, so the GPU dispatch is scheduled promptly and is never starved by the hook tasks that saturate the default pool. With no interactive thread the default pool is used; correctness is unaffected, only the overlap.","title":"The dispatch loop runs on its own thread"},{"category":"section","location":"scheduling/#Decision-order","page":"Scheduling","text":"Each time the GPU frees up, the scheduler decides in this order:\n\nDispatch a committed meta sub-call first, if one is pending. A meta model's orchestration runs off the dispatch loop and re-enters the scheduler for each sub-call (see Meta Models); those continuations are committed and jump ahead of the discipline scan, at most one per in-flight meta, so a meta already in progress is not stalled behind newly arrived work.\nOtherwise select the next model, according to the configured discipline: fair (deficit-weighted and cost-aware, the default), fifo (oldest queued request), or edf (soonest deadline).\nCoalesce the winner. The selected model's queued requests are taken in FIFO order and packed into the largest compiled batch size that fits, padding a partial batch up to the smallest size when needed and always making forward progress on at least the oldest request. The remainder stays queued for the next round.","title":"Decision order"},{"category":"section","location":"scheduling/#Disciplines","page":"Scheduling","text":"The discipline selects which model is dispatched next. Coalescing applies underneath all three: whichever model wins, its queue is coalesced in FIFO order.","title":"Disciplines"},{"category":"section","location":"scheduling/#fair-(default)","page":"Scheduling","text":"Each model has a relative weight that defines its share of compute: share = weight / sum(weights). The scheduler tracks a decaying exponential moving average of how much GPU time each model has recently consumed (decayed to \"now\" with a half-life of ema_halflife_seconds before every read or write), and scores every model with a non-empty queue by how far below its share it is, divided by its estimated cost:\n\npriority = clamp(share - recent_compute_ema, -cap, cap) / effective_cost\n\nA model that has consumed less than its share recently scores higher. Dividing by cost stops an expensive model from blocking cheaper ones on a marginal fairness edge. The clamp (recency_penalty_cap, default 0.25) bounds both lockout and domination: a quiet model cannot run away with the worker, and a saturated one cannot be starved entirely. When every model's recent-compute EMA is zero (quiet or newly loaded models), the deficit equals the share, so those models stay schedulable rather than normalizing to zero.","title":"fair (default)"},{"category":"section","location":"scheduling/#fifo","page":"Scheduling","text":"Under fifo, per-model weights are ignored. The model whose oldest queued request is the oldest overall is served first, with ties broken by model name for determinism. This is the discipline to use when the worker sits behind a gateway that is already doing placement; see \"Which discipline to use\" below.","title":"fifo"},{"category":"section","location":"scheduling/#edf","page":"Scheduling","text":"Under edf (earliest-deadline-first), the model whose most-urgent queued request has the soonest deadline is served first. The deadline comes from the request's remaining-budget timeout; a request with no deadline is treated as least urgent. Ties break by the model's oldest queued request and then by name, so when all queued requests share a deadline (the common case) edf is exactly fifo. It diverges only to promote a model carrying a request with less budget left, so a meta's sub-call, which inherits the meta's deadline, is served ahead of fresher regular work. Queues stay arrival-ordered (never reordered), and coalescing still draws FIFO from the front, so a request deep behind the batch size advances over successive dispatches of its model rather than jumping the batch.\n\nedf also sheds work it cannot finish in time. Before GPU work begins, a request whose deadline has already passed is dropped under every discipline; under edf a laxity check is added: a request is also dropped if it cannot finish within this dispatch's learned compute cost, so the GPU is not spent on work that will miss anyway (the classic EDF overload failure mode). Dropped requests receive a DeadlineExceeded error (gRPC DEADLINE_EXCEEDED upstream); only feasible requests run, and when none are feasible the execution is skipped entirely. The cost estimate is learned from prior dispatches and unseeded pairs default small, so the laxity drop is a no-op until a model has run once. Committed meta sub-calls skip the laxity drop (their earlier stages already spent GPU time) but still honor the hard deadline check.\n\nNote that because edf derives urgency solely from the deadline, issuing different per-client deadlines for the same model reorders that model's service and therefore affects fairness across clients; keep deadlines uniform to retain fifo-like fairness.","title":"edf"},{"category":"section","location":"scheduling/#Which-discipline-to-use","page":"Scheduling","text":"fair is for workers with no upstream placement authority: a single-GPU worker, or a fleet behind the round-robin gateway, where the worker itself must stop one model from crowding out the rest. Behind a gateway running lpt_packing, the gateway is the fairness and placement authority (see Gateway), and the worker must run fifo or edf so the two do not fight; fair would double-book GPU time against the gateway's placement decisions. edf is for deadline-sensitive serving, and it degrades gracefully to fifo when clients use uniform deadlines.","title":"Which discipline to use"},{"category":"section","location":"scheduling/#Coalescing","page":"Scheduling","text":"Coalescing merges same-model requests into one GPU execution. The selected model's queue is scanned in FIFO order, the requests that fit are concatenated along the batch axis into a single compiled-batch-size dispatch, the execution runs once, and the outputs are sliced back per caller (zeros padding a partial fill are dropped). The remainder stays queued for the next round. The batch sizes themselves come from the bundle's manifest batching spec: a bundle may carry several compiled batch sizes that share one set of weights, and the scheduler picks the executable that matches each coalesced batch without duplicating parameters.","title":"Coalescing"},{"category":"section","location":"scheduling/#When-a-model-can-coalesce","page":"Scheduling","text":"A model coalesces multiple requests only when every executable input and every output carries a batch axis (an all-or-nothing requirement that mirrors the loader's manifest check). Inputs are then concatenated along that axis and outputs split back per request. Otherwise the model serves one request per dispatch. Single unbatched modules (the batch-0 executable) are never coalesced. Inputs without a batch axis are taken from the first request, since they do not vary across the batch.\n\nOnly requests that resolve to the same input-shape variant coalesce together. A model compiled for several input shapes (aspect-ratio variants) keeps one executable set per variant over a single shared set of weights, and different shapes cannot be concatenated along the batch axis. The coalescing window is the leading run of same-variant requests at the front of the queue, which keeps the taken set a contiguous prefix (so it can be removed in one step) and preserves FIFO order. A request whose shape has no compiled executable dispatches alone so that the caller gets the precise \"no compiled program for input shape\" error.","title":"When a model can coalesce"},{"category":"section","location":"scheduling/#Sizing-the-batch","page":"Scheduling","text":"The scheduler chooses the largest compiled batch size that the queued rows can fill; a partial fill pads up to the smallest compiled size (the compiled shape may therefore exceed the queued rows, and the dispatch runs padded). Coalescing uses only what is queued now; there is no look-ahead.\n\nA per-model max_batch_size caps how many rows are coalesced into one dispatch. The cap does not change compiled shapes: a partial fill still pads up to the smallest compiled size, and a single request larger than the cap is never split, so it dispatches alone (growing the chosen size to the smallest compiled batch that fits it when necessary). Forward progress is guaranteed: an indivisible front request that exceeds the batch size or the cap still dispatches alone rather than being held back, so the oldest request is always served within a bounded number of rounds. A lone request that already fills the dispatch passes its inputs straight through with no concatenation or padding.","title":"Sizing the batch"},{"category":"section","location":"scheduling/#Why-coalescing-is-the-throughput-lever","page":"Scheduling","text":"Packing many requests into one execution amortizes the fixed per-launch overhead and, for a model that had to be loaded on demand, the one-time weight transfer, across every item in the batch. On a representative compute-heavy model, per-image latency drops by roughly three times from batch 1 to batch 32 while images per second more than triples. Coalescing is therefore the reason a worker can absorb bursts: concurrent requests pile onto a model's queue and the loop converts them into a small number of full dispatches instead of paying launch cost once per request.","title":"Why coalescing is the throughput lever"},{"category":"section","location":"scheduling/#Cost-learning","page":"Scheduling","text":"The scheduler measures the real GPU time of each execution and refines a per-batch-size cost estimate with an exponential moving average (cost_ema_alpha, default 0.2). The estimate initializes to the first measurement, so it converges immediately rather than drifting up from zero. It feeds the fair discipline (the cost divisor, so an expensive model does not crowd out cheaper ones on a marginal fairness edge) and the edf laxity check.\n\nTwo things keep the estimate honest:\n\nWarmup seeds it. At startup, under the fair discipline, each compiled size is run once on zero-filled inputs and the measured time becomes the estimate. Failures are non-fatal: the default cost covers any unseeded (model, batch size) pair until the first real dispatch measures it. While every candidate is unseeded they all share the default, so the deficit term decides. When the on-demand weight cache is enabled with auto-sizing, the startup isolation probe doubles as this warmup.\nWeight load time is excluded. The measured compute_time covers only the model execution, not the on-demand weight load that preceded it, so the estimate reflects steady-state (resident) execution rather than cold-call latency.\n\nCoalescing is also priced fairly: a coalescing_discount (default 0.10) is applied to the effective cost of a coalesced batch, so a larger dispatch is not penalized for being larger when it competes against an unbatched one.","title":"Cost learning"},{"category":"section","location":"scheduling/#Per-model-configuration","page":"Scheduling","text":"Most models need no tuning: with the default fair discipline, all-default weights yield uniform shares. When you want to change that, per-model overrides live under scheduler.models in the node file, which builds the ModelSchedConfig entries:\n\nglobal:\n  scheduler:\n    models:\n      resnet50:\n        weight: 2.0           # relative compute share under fair (default 1.0)\n        max_batch_size: 8     # cap on rows coalesced per dispatch (default uncapped)\n\nweight sets the model's fair share. residency is also settable per model (the initial residency floor); see On-demand Weights for what the floors mean. max_batch_size caps coalescing rows per dispatch, as described above; it does not change compiled shapes and never splits a request. The full node-file surface, including the global scheduler: block, is in Node Configuration.\n\nmax_queue_depth caps each model's queue independently: a full model rejects new requests without affecting admission for the others, so one backlogged model cannot starve the rest of the worker.","title":"Per-model configuration"},{"category":"section","location":"scheduling/#Live-policy-changes","page":"Scheduling","text":"The control plane can adjust scheduling policy on a running worker. ReactantServer.set_policy! updates a model's live weight (consulted by the fair discipline; available in both residency modes, and it raises on an unknown model). ReactantServer.set_residency! moves a model to a target residency floor; it is only meaningful in externally-managed mode and the worker rejects it otherwise. Both run on the dispatch thread, the sole mutator of residency, and block until applied; control commands are drained ahead of dispatch selection, so a residency transition takes effect before the dispatch that may depend on it. ReactantServer.compact! frees the resident non-pinned weights so the allocator coalesces its free list, then reloads the listed models, defragmenting the device arena; see On-demand Weights.","title":"Live policy changes"},{"category":"section","location":"scheduling/#Observability","page":"Scheduling","text":"ReactantServer.scheduler_metrics snapshots per-model state: dispatch count, requests served, rows served, total compute, the current recent-compute EMA, queue depth, queue-wait P50/P99, the histogram of dispatched batch sizes, the effective max batch, and residency. The counters are cumulative over the worker's lifetime, so deltas between polls give rates; dispatch_count counts coalesced batch executions, requests_served counts individual requests (at least one per dispatch, more under coalescing), and rows_served counts batch-axis rows, the effective batch size actually executed.\n\nThe worker exports these as Prometheus series (worker_dispatch_total, worker_compute_seconds_total, worker_queue_depth, worker_queue_wait_seconds, worker_requests_served_total, worker_rows_served_total, worker_model_max_batch_size, and the residency gauges), tagged per model and per GPU, and the node's metrics endpoint aggregates them. See Deployment for the scrape configuration.\n\nReactantServer.control_status is the control-plane view a gateway reads: the worker's residency mode and discipline, and for each model its weight, queue depth, cumulative serving counters, effective max batch, and the wire-facing batch axis (the input name and 1-based axis a gateway can count items on without decoding a request). A meta is reported as a single unit whose footprint is the sum of its sub-models, so the gateway packs and routes the group as one. See Meta Models and Gateway.","title":"Observability"},{"category":"section","location":"scheduling/#Configuration-reference","page":"Scheduling","text":"The scheduler: block in the node file maps to SchedulerConfig, and entries under scheduler.models map to ModelSchedConfig. The discipline enum is SchedulingDiscipline. Defaults and YAML keys:\n\nglobal:\n  scheduler:\n    discipline: fair               # fair | fifo | edf (fair is the default)\n    ema_halflife_seconds: 30.0     # fair: half-life of the recent-compute EMA\n    recency_penalty_cap: 0.25      # fair: clamp on the share-vs-EMA deficit\n    coalescing_discount: 0.10      # fair: cost discount applied to coalesced batches\n    cost_ema_alpha: 0.2            # fair/edf: smoothing for the learned per-batch-size cost\n    max_queue_depth: 1024          # per-model queue cap; a full model rejects new requests\n    dispatch_timeout_seconds: 30.0 # per-request execution timeout\n    compaction_interval: 0         # worker-local memory compaction every N on-demand loads\n    models: {}                     # per-model overrides (weight, residency, max_batch_size)\n\nEvery field is also settable with an INFERENCE_SERVER_SCHEDULER_* environment variable (for example INFERENCE_SERVER_SCHEDULER_DISCIPLINE=fifo). See the API Reference for the full SchedulerConfig documentation.","title":"Configuration reference"},{"category":"section","location":"deployment/#Deployment","page":"Deployment","text":"A node runs as a single supervisor process (ReactantServerNode): it detects every visible GPU, spawns one single-GPU worker subprocess per device, multiplexes their logs onto one stream with [name] line prefixes, and restarts children that die. With two or more workers it also runs an embedded gateway on the public ports; with a single worker it binds that worker to the public ports directly. The external interface is the same either way: KServe V2 gRPC on :8001, health and metrics on :8002 (/readyz, /healthz, /metrics), matching Triton's ports.\n\nThe supported deployment is native (no containers): the supervisor uses the host's NVIDIA driver directly. A container image is also provided as an alternative and runs the same node with the same interface; see Docker (container) deployment below.","title":"Deployment"},{"category":"section","location":"deployment/#Audience-and-mission","page":"Deployment","text":"The project targets small and mid-size labs and engineering organizations that need big-tech-level efficiency without big-tech scale: startups and small companies serving production ML where each GPU is a meaningful fraction of infrastructure spend, scientific and research groups operating within bounded compute budgets, and on-premise or cloud deployments where GPU-hours are the dominant cost line. The audience is engineers and operators who understand what is happening inside their systems; the project gives them tools rather than hiding the system's behavior behind convenient defaults. Larger organizations can use the project too, as a component of a larger system; the difference is what ships in the box, not what is possible.\n\nThe mission is to make serving compiled (non-LLM) models elegant: a hackable, Julia-first inference stack that maximizes the economic efficiency of GPU-based inference. GPU memory is roughly two thirds of GPU cost, so serving infrastructure that wastes memory wastes money; the concrete goal is serving the largest number of models per GPU at a given quality of service. The whole stack is plain Julia, legible end to end, adoptable off the shelf, and open to being bent toward a workload nobody anticipated.\n\nThe project's explicit non-goals, from its design:\n\nHyperscale platform requirements. Multi-tenant isolation, complex traffic shaping, and deep integration with bespoke internal platforms are not built into the core, where every smaller deployment would pay for them. Large deployments are supported through the control-plane seam described under Multi-node.\nLLM serving at scale. vLLM, TGI, TensorRT-LLM, and similar projects are purpose-built for that domain and do it well; this project does not compete there.\nA packaged, managed solution. This is infrastructure for builders, not a hosted service. Users who do not want to think about the underlying architecture should choose a managed inference service.\nMulti-framework serving. A model must be lowered by Reactant to a device executable first (today via StableHLO/XLA). Teams that need to serve PyTorch, TensorFlow, and ONNX models side by side without converting them are better served by Triton or similar.","title":"Audience and mission"},{"category":"section","location":"deployment/#Deployment-shapes","page":"Deployment","text":"The deployment decision comes down to how many GPUs you have and, with more than one, whether your constraint is memory or compute:\n\nSituation Shape Optimizes for\nOne GPU, many models Single GPU Fitting many models on limited hardware\nSeveral GPUs, models do not all fit everywhere Multi-GPU distributed Serving more models than any one GPU holds\nSeveral GPUs, models fit everywhere, need throughput Multi-GPU replicated Spreading compute load across replicas\nMany machines Multi-node Whatever your control plane decides\n\nEach shape is selected by a configuration value, not a different architecture, and each is strictly additive: a single-GPU deployment never pays for the gateway in dependencies or runtime cost, and a gateway deployment never requires a control plane.","title":"Deployment shapes"},{"category":"section","location":"deployment/#Single-GPU","page":"Deployment","text":"A single worker is the entire deployment. The worker speaks the full KServe V2 gRPC surface itself: one Julia process, one YAML file, nothing else to operate. This is the recommended starting point for small labs and the conceptual foundation for the distributed multi-GPU case, which scales the same idea across cards. The Scheduling page covers the fair scheduler and batch coalescing; On-demand Weights covers the weight cache that serves a catalog larger than device memory.","title":"Single GPU"},{"category":"section","location":"deployment/#Multi-GPU,-distributed-without-replication","page":"Deployment","text":"You have several cards, but your model library is large enough that it cannot fit on every GPU. Your constraint is memory. The gateway uses LPT (longest-processing-time) packing to place each model on one GPU, balancing memory footprint against compute load; spreading models across cards by their load keeps any one card from being monopolized. The workers switch to the simpler FIFO discipline and the placement intelligence moves upstream to the gateway. There is still no external infrastructure to stand up: the gateway is one more Julia process and one more YAML file, and there is no placement file to maintain.","title":"Multi-GPU, distributed without replication"},{"category":"section","location":"deployment/#Multi-GPU,-replicated","page":"Deployment","text":"You have enough memory that your models fit on more than one card. Your constraint is compute: you want to spread a model's request load across replicas for throughput. The gateway gives a replicated model a replica count, places it on that many distinct GPUs, and routes its requests to fill one replica's batch before moving to the next, so batch coalescing is preserved across replicas. Size replica counts for the model's expected concurrency, and make sure the memory is there: replicating a model puts its full weight footprint on every GPU it lands on, and an over-subscribed GPU thrashes, loading and evicting weights on nearly every request. See Multi-GPU Gateway for the scheduling modes and knobs.","title":"Multi-GPU, replicated"},{"category":"section","location":"deployment/#Multi-node-(bring-your-own-control-plane)","page":"Deployment","text":"Beyond a single host, the project deliberately does not ship a multi-node control plane: anyone who needs multi-node coordination is usually at a scale where a generic control plane would not fit. Instead, each node exposes the interface a control plane integrates against, so you can build or adapt your own coordination layer on top of ReactantServer nodes. The endpoint contract is the KServe V2 gRPC data plane (ModelInfer, RepositoryIndex, ServerReady) plus the worker control RPCs (ModelControlStatus, SetModelResidency, SetModelPolicy for residency and policy, CompactMemory to defragment device memory), and an admin HTTP port serving /healthz, /readyz, and Prometheus /metrics. A node's embedded gateway additionally answers GatewayControlService for its own scheduling state. Your control plane discovers which models a node serves via RepositoryIndex and routes ModelInfer to a node that reports the model ready. The project supplies the seam; the organization supplies the tooling that encodes needs only it can know.\n\nThe simplest multi-node setup reuses the standalone gateway pointed at every node's worker endpoints; a larger deployment replaces it with your own control plane speaking the same gRPC interface. See Multi-GPU Gateway for the standalone gateway's configuration.","title":"Multi-node (bring your own control plane)"},{"category":"section","location":"deployment/#The-node-supervisor","page":"Deployment","text":"The supervisor's job on startup is to turn \"this host plus this node file\" into a concrete set of child processes. It does so in three steps.\n\n1. Detect the devices. The first of these that yields a non-empty answer wins (ReactantServerNode.detect_gpus):\n\nREACTANT_GPUS environment variable: a count (2) or an explicit list (0,2 or GPU UUIDs); 0 means a CPU node.\nthe node file's gpus: key (auto, a count, or a list).\na CUDA_VISIBLE_DEVICES already set on the host.\nnvidia-smi enumeration.\n/dev/nvidiaN device nodes.\n\nFor a CUDA node that finds no devices, startup fails with guidance (run with --gpus all, set REACTANT_GPUS, or set backend: cpu).\n\n2. Materialize the workers (ReactantServerCore.materialize_node!). With no workers: list, one worker is synthesized per detected device (worker0..workerN-1); an explicit workers: list wins and assigns each worker a device positionally, or by its gpu: key. Either way each worker is pinned to exactly one device through its own CUDA_VISIBLE_DEVICES, so inside the worker the device is always ordinal 0 and the single-GPU worker code runs unchanged. The materialized node file is written to /run/reactantserver/node.yaml for inspection.\n\nEach worker's compute-thread pool is sized to its share of the host, min(CPU_THREADS ÷ workers, 16) threads, plus one interactive thread for the GPU dispatch loop. This avoids the oversubscription that --threads=auto would cause: with several workers on one node, each auto worker (and its GC and host library pools) would size itself to the whole machine and the workers would fight for every core under load. The cap keeps a very large box from handing any one worker an unhelpfully huge pool. Set REACTANT_WORKER_THREADS to override the computed value; it is used verbatim, bypassing the split and the cap.\n\n3. Decide on the gateway by worker count, in the default all role:\n\nOne worker, no gateway. A lone worker already serves the full KServe V2 API, so the supervisor binds it directly to the public ports (8001/8002) and starts no gateway. No extra process, no extra hop.\nTwo or more workers, workers plus the embedded gateway. Each worker binds base_port + i (and metrics_base_port + i), and the gateway binds the public 8001/8002. The supervisor synthesizes the gateway's worker list (and worker metrics list) from the node file, so the gateway needs no config of its own.\n\nThe external interface is therefore identical whether you run 1 GPU or 8, which is why your client and the public ports never change as you scale. REACTANT_ROLE selects what the supervisor runs; the default all (workers plus the embedded gateway on one host) is the documented deployment. The workers and gateway roles exist in the code to split a deployment across machines, but multi-node is not a shipped example.\n\nOne ReactantServerNode.supervise call on the multi-GPU host does the whole fan-out, just as it does for one GPU:\n\nusing ReactantServerNode\nReactantServerNode.supervise(\"node.yaml\")   # one worker per GPU + the embedded gateway\n\nThis single parent process spawns one ReactantServer.serve worker subprocess per GPU and the gateway as another subprocess, multiplexes their logs onto its stdout with [worker0] / [gateway] prefixes, and restarts any child that dies. (You can still run each worker and the gateway by hand, as separate serve / serve_gateway processes, but the supervisor is the intended path and the one the launcher uses.) ReactantServerNode.main() is the container entrypoint: it reads REACTANT_NODE_FILE for the node config and runs the supervisor to completion.","title":"The node supervisor"},{"category":"section","location":"deployment/#Watch-it-without-a-GPU","page":"Deployment","text":"To see the decision logic and the prefixed logs on a machine with no GPU, run the supervisor as a CPU node with two synthetic workers, with backend: cpu in the node file:\n\nREACTANT_GPUS=0 REACTANT_CPU_WORKERS=2 \\\n  julia --project=packages/ReactantServerNode -e 'using ReactantServerNode; ReactantServerNode.supervise(\"node.yaml\")'\n\nYou will see worker0, worker1, and gateway start, their logs interleaved with [name] prefixes, and the gateway serving on 8001/8002, exactly the multi-worker shape it takes on a multi-GPU host.","title":"Watch it without a GPU"},{"category":"section","location":"deployment/#The-node-file-is-unchanged-when-you-scale","page":"Deployment","text":"gpus: auto already means \"one worker per visible GPU\", so the single-GPU node file from the Tutorial scales as-is: give the host more GPUs and the supervisor runs more workers. You only edit the node file to do something non-default:\n\nmodel_repo: /var/lib/reactantserver/models\nbase_port: 8080           # worker i listens on base_port + i (8080, 8081, ...)\nmetrics_base_port: 9100   # and metrics on metrics_base_port + i (9100, 9101, ...)\ngpus: auto                # or an integer count, or an explicit device list\n\nglobal:\n  runtime:\n    backend: cuda\n  endpoints:\n    host: 0.0.0.0\n\nA model listed on more than one worker is replicated, and the gateway load-balances requests for it across those workers. See Node Configuration for the models: map and On-demand Weights for fitting more models than GPU memory holds. The node is described by one YAML node file; the commented templates under config/ (node.default.yaml, node.yaml) are reference configs, and gateway scheduling (round_robin or lpt_packing) is covered on the Multi-GPU Gateway page.","title":"The node file is unchanged when you scale"},{"category":"section","location":"deployment/#Running-natively","page":"Deployment","text":"Instantiate the workspace once, then run the node supervisor. ReactantServerNode.main() reads REACTANT_NODE_FILE for the node config and honors the standard environment overrides:\n\n# once, to resolve and precompile the workspace (selects the CUDA build via REACTANT_GPU_*):\nREACTANT_GPU=cuda REACTANT_GPU_VERSION=13.1 \\\n  julia --project=. -e 'using Pkg; Pkg.instantiate(); Pkg.precompile()'\n\n# run the supervisor across four GPUs, serving a bundle directory:\nCUDA_VISIBLE_DEVICES=0,1,2,3 \\\nINFERENCE_SERVER_MODEL_DIRS=/path/to/bundles \\\nREACTANT_NODE_FILE=config/node.default.yaml \\\n  julia --handle-signals=no --project=packages/ReactantServerNode \\\n    -e 'using ReactantServerNode; ReactantServerNode.main()'\n\n--handle-signals=no lets the supervisor's own handler run so it shuts its worker children down on SIGTERM. REACTANT_GPU_VERSION selects the Reactant CUDA build (12.9 or 13.1) and must be set before instantiate. INFERENCE_SERVER_MODEL_DIRS overrides the node file's model repository (colon-separated); the runtime tunables under Node Configuration take INFERENCE_SERVER_* overrides the same way. For an always-on service, run this command under a process manager such as systemd, with Restart=on-failure and a SIGTERM-based graceful stop (KillMode=mixed pairs with --handle-signals=no).\n\nEvery model compiles to a device executable on every worker before the gRPC plane accepts traffic, so first startup is slow (minutes to hours for a large model set). Watch readiness with curl -sf http://127.0.0.1:8002/readyz, not the process state.","title":"Running natively"},{"category":"section","location":"deployment/#Running-under-systemd","page":"Deployment","text":"For an always-on node, run the supervisor from a system service. Put the tunables in an EnvironmentFile and let the unit run the same command as above. Adjust the user, the checkout path, and the GPU list for your host.\n\n/etc/reactantserver/reactantserver.env:\n\nCUDA_VISIBLE_DEVICES=0,1,2,3\nINFERENCE_SERVER_MODEL_DIRS=/path/to/bundles\nREACTANT_NODE_FILE=config/node.default.yaml\nREACTANT_GPU=cuda\nREACTANT_GPU_VERSION=13.1\n\n/etc/systemd/system/reactantserver.service:\n\n[Unit]\nDescription=ReactantServer node supervisor\nAfter=network-online.target\nWants=network-online.target\n\n[Service]\nType=exec\nUser=YOUR_DEPLOY_USER\nWorkingDirectory=/path/to/ReactantServer.jl\nEnvironmentFile=/etc/reactantserver/reactantserver.env\n# Absolute path to julia: systemd does not source your shell rc, so a juliaup install under the\n# user's home is not on PATH. `julia --version` in a login shell shows the binary to use here.\nExecStart=/home/YOUR_DEPLOY_USER/.juliaup/bin/julia --handle-signals=no --project=packages/ReactantServerNode -e 'using ReactantServerNode; ReactantServerNode.main()'\nRestart=on-failure\nRestartSec=10\n# First boot compiles every model on every worker (minutes to hours) AFTER the unit is already\n# active; systemd cannot gate that (the supervisor sends no sd_notify). Check readiness with\n# `curl -sf http://127.0.0.1:8002/readyz`, not `systemctl is-active`.\nTimeoutStartSec=infinity\n# Graceful stop: SIGTERM to the supervisor only, which drains its workers; pairs with\n# --handle-signals=no. Anything still alive after TimeoutStopSec is SIGKILLed.\nKillMode=mixed\nKillSignal=SIGTERM\nTimeoutStopSec=45\n\n[Install]\nWantedBy=multi-user.target\n\nEnable and watch it:\n\nsudo systemctl daemon-reload\nsudo systemctl enable --now reactantserver.service\njournalctl -u reactantserver -f\nuntil curl -sf http://127.0.0.1:8002/readyz; do sleep 15; done; echo READY\n\nsudo systemctl stop reactantserver sends SIGTERM to the supervisor, which drains its workers within TimeoutStopSec before exiting. Run the workspace Pkg.instantiate() once (as in the previous section) before enabling the unit, so the first start is not also resolving dependencies.","title":"Running under systemd"},{"category":"section","location":"deployment/#Docker-(container)-deployment","page":"Deployment","text":"A container image is an alternative to running the supervisor directly. It runs the same node (supervisor + workers + embedded gateway) with the same :8001/:8002 interface:\n\nREACTANT_GPU=cuda REACTANT_GPU_VERSION=13.1 julia --project=. -e 'using Pkg; Pkg.instantiate()'  # Manifest.toml is gitignored\nmake image        # or: docker build -f docker/Dockerfile -t reactantserver .\nREACTANTSERVER_MODELS=/path/to/bundles docker compose up\n\nmake image builds through the configured engine (ENGINE, default podman). The equivalent without compose is:\n\ndocker run --gpus all --ipc=host -p 8001:8001 -p 8002:8002 \\\n  -v /path/to/bundles:/var/lib/reactantserver/models:ro reactantserver\n\nThe image is a multi-stage build on julia:1.12.6-trixie that copies only libnvJitLink.so.13 (the one CUDA userspace library Reactant needs but does not bundle) from an official CUDA 13.1 image; it deliberately does not bring the rest of the CUDA userspace, so the base cannot shadow Reactant's bundled cuDNN/NCCL. Why that one library matters: Reactant statically links cuBLASLt into libReactantExtra.so, but CUDA-13 cuBLASLt dlopens libnvJitLink.so.13 to JIT its GEMM kernels, and the library is neither in the Reactant artifact nor injected by the NVIDIA container runtime (which adds only libcuda.so.1). A bare Julia image without it aborts at the first GPU compile with cuBLASLt's \"Invalid handle was passed to cublasLtCreate\"; the copied library fixes that.\n\nThe container needs the host NVIDIA Container Toolkit for GPU access, and the compose file mounts the model repository read-only plus a persistent volume for the Reactant compile cache (autotune results), so tuned kernels survive container recreation. The container shares the host IPC namespace (ipc: host) so POSIX shared-memory regions created by a client are visible to the workers. The autotune knobs are settable as container env (INFERENCE_SERVER_RUNTIME_AUTOTUNE, INFERENCE_SERVER_RUNTIME_AUTOTUNE_CACHE, INFERENCE_SERVER_RUNTIME_AUTOTUNE_CACHE_DIR); the baked default node file sits at /etc/reactantserver/node.yaml and can be overridden by mounting your own over that path. See docker/README.md for details.","title":"Docker (container) deployment"},{"category":"section","location":"deployment/#Metrics","page":"Deployment","text":"One scrape on :8002 covers everything. With multiple workers, the embedded gateway serves its own gateway_* series and fans out to each worker's metrics endpoint, merging them into a single exposition; with a single worker, :8002 is that worker's own /metrics. Each worker tags its series with worker and gpu labels itself (the gpu value is the physical device behind its CUDA_VISIBLE_DEVICES), so per-GPU and per-worker breakdowns need no Prometheus relabeling, for example sum by (gpu) (rate(worker_dispatch_total[1m])).\n\nA ready-to-run Prometheus + Grafana stack lives under config/monitoring/ with a seven-dashboard suite. Because the node runs natively (not in a container), that stack's Prometheus scrapes the host at host.docker.internal:8002 rather than over a Docker network; if the node listens on a different host, edit the target in prometheus.yml to that host's address:8002. Grafana is at http://<host>:3000 (anonymous viewing on; admin / admin to edit) and Prometheus at http://<host>:9090. See config/monitoring/README.md for the compose commands and dashboards.","title":"Metrics"},{"category":"section","location":"deployment/#Security","page":"Deployment","text":"ReactantServer is designed to run on a trusted network behind your own perimeter. Be aware of the following before exposing any endpoint:\n\nCleartext h2c. All gRPC traffic (worker and gateway) is cleartext h2c. TLS settings are parsed by the gateway config but not yet enforced; a configured cert triggers a startup warning.\nNo authentication or authorization on the KServe data plane, the worker control-plane RPCs (residency and policy), the gateway's own scheduling control plane (GatewayControlService, which can repack the fleet and change a model's replica count and routing at runtime), or the Prometheus metrics listener (which binds 0.0.0.0:8002 by default). The control plane is mutating, so treat access to the gRPC ports as administrative access.\nTrusted bundles. A bundle's optional model.jl executes arbitrary Julia in the server process, so bundles are trusted input. Only serve bundles you built or audited; see Bundles.\nShared memory is a local trust boundary. Client-registered regions and the optional node-shared host-weight store live in /dev/shm; the shared weight regions default to mode 666 (world-writable) for friction-free sharing. Set runtime.shared_host_weights_mode: \"660\" on production or multi-user systems.","title":"Security"},{"category":"section","location":"meta_models/#Meta-Models","page":"Meta Models","text":"A meta model is a bundle whose model.jl orchestrates other models with ordinary Julia in between, rather than wrapping a single compiled executable. It exists for the logic torch.export cannot trace: data-dependent control flow, loops whose bounds depend on a tensor's contents, or a pipeline where stage two's inputs are computed from stage one's outputs. The server runs the orchestration as a normal inference request, so a meta model is addressable by clients under its own name exactly like any other model.\n\nIf your model is a single traced graph, use a plain bundle with optional pre/post hooks (see Bundles). Reach for a meta model only when the glue between sub-models is real program logic.","title":"Meta Models"},{"category":"section","location":"meta_models/#Anatomy","page":"Meta Models","text":"A meta bundle is a directory with a manifest.yaml and a model.jl, and no compiled artifact or weights of its own. The manifest declares kind: meta, lists the models the orchestration is allowed to call, and must carry client_inputs/client_outputs (there is no executable to infer the client-facing I/O from):\n\nformat_version: \"2.0\"\nname: two_stage\nkind: meta\nmeta:\n  calls: [two_stage_encode, two_stage_score]\nclient_inputs:\n  - {name: TOKENS, dtype: i64, shape: nb, dims: {b: 32}}\nclient_outputs:\n  - {name: SCORES, dtype: f32, shape: nc, dims: {c: 512}}\n\nThe model.jl calls register_meta_model with the orchestration function:\n\nregister_meta_model(\"two_stage\"; run = function (inputs, call)\n    feats = call(\"two_stage_encode\", inputs)     # stage one: encode\n    rows  = rank_rows(feats)                     # ordinary Julia, data-dependent\n    out   = call(\"two_stage_score\", rows)        # stage two: score\n    return [ReactantServer.NamedTensor(\"SCORES\", out[1].data)]\nend)\n\nrun has the form run(inputs::Vector{NamedTensor}, call) -> Vector{NamedTensor}. The injected call(model_name, inputs) invokes another model and returns its outputs as NamedTensors. The callee must appear in meta.calls; calling an undeclared model is a loud error. The loader also rejects a meta bundle whose model.jl calls register_model instead of register_meta_model.","title":"Anatomy"},{"category":"section","location":"meta_models/#Execution:-local,-on-the-request-task,-one-meta-at-a-time","page":"Meta Models","text":"A meta's run(inputs, call) executes on the gRPC request task, off the GPU dispatch loop, exactly like a model's pre/post hooks. Each call(name, inputs) re-enters the local scheduler in-process: the sub-model is dispatched on the loop and its result handed back to the orchestration. There is no gateway round-trip and no serialization between stages; a tensor handed from one stage to the next is passed by reference. The same model.jl runs unchanged on a single worker or in a fleet; the author never writes routing and never needs to know where anything is placed, because a meta only ever runs where its sub-models already live (see Placement, below).\n\nTwo rules shape how a meta shares the GPU with everything else on the worker:\n\nA bounded number of GPU metas at a time. A per-worker gate admits REACTANT_META_CONCURRENCY meta orchestrations at once, default 2. A meta holds its permit across the whole run, including the CPU glue between stages, but it does not hold the GPU during that glue. While a meta computes between stages, the dispatch loop is free and serves other models, so the GPU is not idle during the glue. The default of 2 lets two heavy pipelines overlap (one's glue hides behind another's GPU stage); at 1 two heavy metas serialize, while one holds the gate the other waits and can miss its deadline. Raising it further favors meta completion via more concurrency, but more metas share the serial GPU and the scratch pool (size it for that many concurrent metas). A compute-only meta (empty meta.calls, below) issues no sub-calls and bypasses the gate entirely, so a heavy pure-Julia meta never holds a permit a GPU meta needs.\nIn-flight sub-calls jump the line. Once a meta holds the gate, each of its GPU stages is dispatched ahead of the queued regular work rather than waiting behind it. The number that can cut the line is tied to the gate: each in-flight meta contributes at most one pending sub-call, so the committed set is bounded by REACTANT_META_CONCURRENCY. At a setting of 1 exactly one meta cuts the line and the behavior is fully deterministic; raising the gate lets that many metas cut the line symmetrically (no contention for a single privileged slot), so the one knob sets both how many metas run and how many are prioritized: a single dial for how strongly to favor meta completion.\n\nClient --infer(two_stage)--> Gateway\n  Gateway --routes the whole group to the one worker that holds it--> Worker request task\n    Worker: run(inputs, call), holds a meta-gate permit\n            (up to REACTANT_META_CONCURRENCY, default 2)\n      call(\"two_stage_encode\", input)   committed, jumps the queue --> dispatch loop / GPU\n      feature maps <---------------------------------------------------------------|\n      Julia glue (the GPU is free; other models' requests are dispatched)\n      call(\"two_stage_score\", feats)    by reference, committed --------------> dispatch loop / GPU\n      scores <--------------------------------------------------------------------------|\n      assembled outputs\n  Worker --outputs--> Gateway --outputs--> Client","title":"Execution: local, on the request task, one meta at a time"},{"category":"section","location":"meta_models/#How-it-interacts-with-batch-scheduling","page":"Meta Models","text":"A meta no longer holds the GPU for its whole run. Each stage is an ordinary dispatch on the loop, so the GPU is occupied only while a stage actually computes and is free during the meta's CPU glue, when the loop serves other models. The trade is the gate: while a meta holds its permit, a second meta on the same worker waits (its stages cannot start), though the GPU is not wasted because regular models run in the meantime. A stage runs at batch 1 for the meta unless other clients' calls to the same sub-model happen to coalesce with it on the loop.\n\nA meta carries a deadline like any request. It is dropped before it starts if the deadline has already passed or if it cannot take a gate permit in time, so a meta backlog sheds at admission rather than piling up, and no GPU is spent on a meta that will not run. Once a meta is in flight, its continuation sub-calls are committed: they skip the predictive laxity drop, so a stage is never shed merely because it might run long after earlier stages already consumed GPU. They still honor the base check, so a meta whose deadline genuinely elapses mid-pipeline stops at the next stage boundary (raising DeadlineExceeded) instead of running the rest. The net effect is that a meta either runs to completion or is shed at a clean boundary, never silently discarding a stage it already paid for. Under the edf discipline the committed sub-call inherits the meta's deadline, so it is ordered ahead of fresher work (see the discipline notes in Node Configuration).","title":"How it interacts with batch scheduling"},{"category":"section","location":"meta_models/#Placement:-the-group-travels-together","page":"Meta Models","text":"A meta owns no weights or executable of its own, so it is not placed on a GPU by itself. Instead the meta and the sub-models it calls form a group that the gateway places as a single unit. The worker reports the meta to the gateway with a memory footprint equal to the sum of its sub-models' weights and hides the sub-models from discovery, so the gateway packs and routes the group by the meta's own traffic and never sees the individual stages. When the group is placed on a GPU, the sub-models' weights are kept resident there together, so the meta's in-process sub-calls always find their executables and weights local.\n\nThis is the inverse of routing each sub-call independently. The author still writes the orchestration with no knowledge of placement, but the cost of that abstraction is now paid once, at placement time (the group is co-located), rather than per sub-call at request time (a hop to wherever each stage happened to land). The sub-models are internal to the meta: they are never addressable by clients and never appear in the gateway's routing table.","title":"Placement: the group travels together"},{"category":"section","location":"meta_models/#Reuse-buffers-with-call.scratch","page":"Meta Models","text":"A meta's data-dependent glue often builds a large intermediate, for example a feature tensor of tens of megabytes handed from one stage to the next. Allocating that fresh on every request drives GC pressure. The injected call exposes a reuse-buffer allocator for this case:\n\nfeats = call.scratch((256, 512), Float32)      # from the worker's reuse pool (or a plain array)\nfill_features!(feats, inputs)                  # write directly into it\nout = call(\"two_stage_score\", [ReactantServer.NamedTensor(\"FEATS\", feats)])\n\ncall.scratch returns plain Arrays aliasing the pool's backing (or, with no pool configured, fresh heap arrays), so a buffer can be handed straight to the next stage as a NamedTensor by reference; either way the model.jl is identical. The pool is plain memory, local to the worker, and never shared across processes. It exists purely to keep large intermediates off the per-request allocation path; because the sub-call runs in-process, the buffer is handed to the next stage by reference, with no copy. A meta that never calls scratch just allocates normally. The pool defaults to 1 GiB across 16 slots, configured per worker, and the acquired slots are released when the orchestration returns. Concurrent metas share the pool, so size it for that many in-flight metas; the deadline-bounded acquire degrades gracefully if it is ever starved. The client driver exposes the same scratch interface for staging inference inputs (see Client Usage).\n\nRequest every buffer in one call.scratch call (pass a vector of dims => T pairs to get several at once); calling it more than once per request is rejected, and a scratch buffer must reach the sub-call as a contiguous array (a reshape or contiguous prefix is fine).","title":"Reuse buffers with call.scratch"},{"category":"section","location":"meta_models/#Constraints","page":"Meta Models","text":"A meta model may not call another meta model. This is validated at load: a group is exactly one level deep.\nCompute-only metas are allowed. A meta may declare an empty meta.calls and do all its work in Julia, calling no sub-models at all. This is useful for logic that is awkward to express as a traced graph but needs no separate executable. A compute-only meta issues no GPU sub-calls, so it bypasses the meta gate and runs on its request task without taking a permit.\nSub-models are internal. A model named in some meta's meta.calls is reachable only through that meta, never addressed by clients directly, and is hidden from the gateway's discovery and placement; the meta's group carries it.","title":"Constraints"},{"category":"section","location":"meta_models/#See-also","page":"Meta Models","text":"Object Detection for a worked end-to-end meta model: converting a torchvision Faster R-CNN into two StableHLO stages chained by data-dependent Julia glue\nBundles for the plain (non-meta) bundle path and pre/post hooks\nMulti-GPU Gateway for how the gateway routes and places models\nNode Configuration for the scheduling disciplines, including edf\nClient Usage for the client-side scratch interface\nregister_meta_model in the API reference","title":"See also"},{"category":"section","location":"gateway/#Multi-GPU-Gateway","page":"Multi-GPU Gateway","text":"The gateway is a gRPC reverse proxy that fronts several ReactantServer.jl workers behind one KServe V2 gRPC endpoint. It is pure Julia, lives in its own package ReactantServerGateway (serve_gateway), and reuses ReactantServerCore's node and config parsing plus the generated KServe protobuf. Because it builds only on ReactantServerCore and the gRPC layer, the gateway carries no Reactant dependency. The documented surface, serve_gateway and probe_worker_ready plus the ReactantServerGateway.RunningGateway handle serve_gateway(blocking=false) returns, is collected on the API page.\n\nYou do not start the gateway yourself. When a node has two or more workers, the supervisor (ReactantServerNode.supervise, the node's default entry point) runs the gateway as an embedded child and synthesizes its worker endpoint list from the node file; a single-worker node skips it entirely, because one worker already serves the full KServe V2 API. See Deployment for the node; this page describes what its embedded gateway does.\n\nClients connect to a single gRPC endpoint. The gateway extracts the model name from each ModelInferRequest and forwards the raw protobuf bytes over gRPC to the worker that hosts that model. The KServe V2 protobuf wire format is identical end to end, so the gateway is a gRPC-to-gRPC pass-through that never re-marshals the body.","title":"Multi-GPU Gateway"},{"category":"section","location":"gateway/#One-endpoint-whether-you-run-1-GPU-or-8","page":"Multi-GPU Gateway","text":"The key scaling fact: nothing about your model, client, or external interface changes as you add GPUs. You run the same launcher (or the same supervise call), the supervisor puts one worker on each GPU, and it fronts them with the embedded gateway, so clients still send one request to one endpoint and it is routed to a worker that serves the model. gpus: auto in the node file already means \"one worker per visible GPU\", so the single-GPU node file scales as-is; you only edit it to do something non-default (see Node Configuration).\n\nThe supervisor decides whether a gateway exists at all: a single-worker node serves the full KServe V2 API directly on the public ports with no gateway, and a node with two or more workers embeds the gateway in front of them, each worker binding base_port + i and the gateway binding the public 8001/8002. The supervisor synthesizes the gateway's worker list from the node file, so the gateway needs no configuration of its own; Deployment owns that decision. The external interface (8001 for gRPC, 8002 for metrics/health) is therefore identical whether you run 1 GPU or 8, which is why your client and the public ports never change as you scale; see Client Usage for the client side. The REACTANT_ROLE environment variable can split the default all role into workers only or gateway only for deployments that span machines; that topology is beyond the scope of these guides.\n\nOne scrape on 8002 still covers the whole node: the embedded gateway serves its own gateway_* series and merges in every worker's /metrics. Each worker tags its series with worker and gpu labels itself (the gpu value is the physical device behind its CUDA_VISIBLE_DEVICES), so per-GPU and per-worker breakdowns need no Prometheus relabeling, for example sum by (gpu) (rate(worker_dispatch_total[1m])). See Deployment for the scrape config.","title":"One endpoint whether you run 1 GPU or 8"},{"category":"section","location":"gateway/#What-the-gateway-does","page":"Multi-GPU Gateway","text":"Single endpoint: clients reach all workers through one gRPC listener.\nModel-name routing by autodiscovery: the gateway is given a flat list of worker endpoints: in its own gateway.yml and queries each worker's RepositoryIndex RPC (every 10s) to learn which models it currently serves. The discovered model-to-workers routing table is rebuilt and swapped in atomically on each probe, so a control-plane pin/unpin or a worker restart flips routing on the next probe.\nReplica scheduling: a model served by more than one worker is load-balanced across those workers, either uniformly (round_robin, the default) or by packing each model onto a fixed, operator-configured number of GPUs with coalescing-aware routing (lpt_packing); see Scheduling modes below. A third mode, least_outstanding, spreads by live occupancy. Either way, a request fails over to the remaining replicas when a worker returns NotFound or Unavailable.\nReadiness probe: a background loop calls each worker's KServe ServerReady RPC; /readyz is ready when at least one worker reports ready.\nRaw passthrough: the ModelInfer hot path never decodes or re-marshals the protobuf body. The request and response types are Vector{UInt8} end to end (gRPCServer.jl and gRPCClient.jl support raw byte messages natively). To route, the gateway decodes a partial schema declaring only model_name (field 1) and id (field 3). Under lpt_packing a second partial decode reads one named tensor's shape to size the request in items (see the fill quantum below). ProtoBuf seeks past everything else, including every tensor payload; shapes precede the data on the wire, so neither decode touches it.\nSHM broadcast: SystemSharedMemoryRegister / Unregister are fanned out to every worker. POSIX SHM regions are host-local; every worker attaches via shm_open independently. Register succeeds only if all workers succeed (it rolls back partial success); unregister succeeds if any worker does.\nSHM namespace probe: IsSameIPCNamespace is fanned out to every worker and the gateway returns true only if all of them can see the client's region (any worker may service a later ModelInfer). A worker that errors or does not implement the RPC counts as false.\nObservability: structured logs, Prometheus metrics, /healthz, and /readyz on a separate admin HTTP port.","title":"What the gateway does"},{"category":"section","location":"gateway/#Scheduling-modes","page":"Multi-GPU Gateway","text":"The gateway routes each model's requests across its replicas according to scheduling.mode in gateway.yml. round_robin is the predictable default and needs no measurements; least_outstanding spreads by live occupancy; lpt_packing concentrates each model's traffic on few GPUs for deep coalescing batches and is the mode whose placement the gateway manages. Every setting in the block below except mode can also be changed while the gateway runs, through its own gRPC control plane; see Runtime scheduling control.\n\nscheduling:\n  mode: lpt_packing             # round_robin (default) | least_outstanding | lpt_packing\n  work_basis: compute           # compute (default) | items | requests  (what \"least busy\" measures,\n                                #   for least_outstanding and for fill_least; see below)\n  rebalance_compute_seconds: 300 # fleet GPU-seconds consumed that triggers a repack\n  first_rebalance_compute_seconds: 60 # smaller budget for the first repack (0 = use rebalance_compute_seconds)\n  ema_halflife_compute_seconds: 0 # demand-EMA halflife in fleet compute-seconds (0 = track rebalance_compute_seconds)\n  hysteresis: 0.0               # extra improvement required before a model moves workers (0 = move on any gain)\n  default_replicas: 1           # GPUs per model unless overridden below (a number, or \"all\")\n  routing_fill_factor: 1.0      # fill quantum as a multiple of max batch size (lpt_packing only)\n  routing_policy: fill_least    # fill_least (default) | fill_rr  (lpt_packing only)\n  routing_fill_mode: run        # run (default) | spread | inflight  (what the quantum counts;\n                                #   `inflight` can park a model on one GPU, see the warning below)\n  forbid_memory_oversubscription: true # never strand a model on-demand when it could fit resident (default on)\n  compaction_mode: eager        # eager (default) | off | scheduled  (defragment workers after a repack)\n  compaction_interval: 1        # repacks between compactions; 0 disables  (see On-demand Weights)\n  models:\n    big-model:\n      replicas: 2               # this model is placed on 2 distinct GPUs (a number, or \"all\")\n      fill_mode: spread         # optional per-model override of routing_fill_mode\n      fill_factor: 0.5          # optional per-model override of routing_fill_factor","title":"Scheduling modes"},{"category":"section","location":"gateway/#round_robin","page":"Multi-GPU Gateway","text":"round_robin (the default) spreads each model's requests uniformly across its replicas. It is fully predictable from the config file and needs no measurements, at the cost of thin per-worker queues: when every model is on every worker, each worker sees a slice of every model's traffic, so coalesced batches rarely fill.","title":"round_robin"},{"category":"section","location":"gateway/#least*outstanding-and-work*basis","page":"Multi-GPU Gateway","text":"least_outstanding sends each request to the replica carrying the least in-flight work, spreading by live occupancy rather than blindly. Like round_robin it needs no preconditions and does not concentrate traffic, so it favors even spreading over batch coalescing; prefer it over round_robin when a model's replicas have uneven or unpredictable per-request latency, so a slow replica stops attracting new work instead of accumulating a backlog.\n\nscheduling.work_basis decides what \"work\" means. It is fleet-wide: the same setting denominates least_outstanding's per-replica counter and lpt_packing's fill_least per-worker load, so there is one answer to \"what does this fleet call busy\".\n\ncompute (the default) counts in-flight GPU-seconds: each request charges its item count times the model's worker-measured cost per item. This is the only basis that is comparable across models, where 32 items of a cheap model are not 32 items of an expensive one.\nitems counts in-flight items, so a client that pre-batches is weighed by how much it sent. Correct without any cost measurement when a fleet's models cost about the same per item. Weakest under fill_least, which compares one worker's load across all models, so dropping the cost weighting makes a cheap model's items look as expensive as a costly model's.\nrequests is the per-request denominator each mechanism used before this knob existed: a raw count for least_outstanding, and the model's measured cost per request for fill_least (which keeps its cross-model cost weighting). Blind to batch size either way, so a pre-batching client defeats the balancing. Set this to pin the pre-knob behavior exactly.\n\ncompute and items need to know where each model's batch axis is and what an item costs, which the gateway learns from a ModelControlStatus poll on each health-probe round. Under least_outstanding that is one extra RPC per worker per tick (requests polls nothing); lpt_packing already polls.\n\nUnder lpt_packing the basis is also runtime-mutable, like every other knob in the block: change it with tools/gateway_ctl.jl set-policy work_basis=items and read it back from GetSchedulingStatus, so a fleet can be switched and rolled back without a restart. Under least_outstanding it is fixed at startup, and the startup log line states which basis is in force.","title":"leastoutstanding and workbasis"},{"category":"section","location":"gateway/#Models-with-no-batch-axis,-and-metas","page":"Multi-GPU Gateway","text":"Both work bases degrade to one unit per request rather than misreporting, so no model ever routes on zeros:\n\nA model that declares no batch axis (wire_batch_spec finds none: an unbatched bundle, a model whose inputs are all fixed-shape) counts as one item per request. Its leading dimension names an axis, not a count, so there is nothing to read; under compute it still charges its own measured cost, which for such a model is a cost per request.\nA model the gateway has not polled yet counts as one item until the first successful poll, and under compute borrows the fleet-mean cost per item, so a cold model weighs like an average one instead of looking free and attracting every request.\nA meta (kind: meta) deliberately reports no batch axis and counts one row per request, even when its own client_inputs declare a batch axis. The two halves go together: rows equal requests, so a meta's cost per item equals its cost per request, and a work-routing gateway charges a meta its whole measured cost. Reporting an axis without also counting rows would charge N times the per-request cost; counting rows without reporting an axis would divide the cost by N and then charge one of them.\n\nA meta is also the clearest argument for compute over items. Its work is data-dependent (a detection meta's ROI count varies per image, and its Julia glue is not GPU time at all), so no item count predicts it, while under items the most expensive thing on the fleet would charge one unit and a cheap batched model would charge 32. Note that a meta's total_compute counts its sub-call GPU time only, and its sub-models are folded into it and hidden from the gateway, so that time is counted once rather than double counted.","title":"Models with no batch axis, and metas"},{"category":"section","location":"gateway/#lpt_packing","page":"Multi-GPU Gateway","text":"lpt_packing places each model on a fixed number of distinct GPUs and routes its requests to preserve batch fill. A model's replica count is operator-controlled: default_replicas (default 1, the single-GPU case that coalesces best), overridable per model under scheduling.models.<name>.replicas. Both accept a positive integer or all, which places the model on every ready worker (so default_replicas: all replicates the whole model set across all GPUs without listing each model, and tracks the fleet as workers come and go). The count never grows automatically under load, so a hot model relies on its worker's queue and coalescing rather than fanning out on its own; an operator can promote one at any time with SetModelPlacement, effective at the next repack.\n\nwarning: Replication is the operator's responsibility\nThe gateway does not check that a replica count is feasible for your hardware. Replicating a model charges its full weight footprint to every GPU it lands on, so replicas: 2 (or default_replicas: all) only makes sense when those weights actually fit on each card alongside everything else placed there. If the assigned footprint exceeds a worker's on-demand weight budget, the weights cannot all stay resident and the worker thrashes, loading and evicting weights on nearly every request, which destroys throughput. Size replica counts against your GPU memory. The gateway logs a weight footprint exceeds the worker's on-demand budget warning at each repack when a placement is oversubscribed, so watch for it.\n\nThe packer chooses which GPUs host each model's replicas by balancing two live measurements: compute demand (the gateway-measured arrival rate times the true per-request compute cost the workers report over the control plane) and resident weight footprint against each worker's weight-memory budget, placing models heaviest-first onto the least pressured workers, where pressure is whichever of compute or memory is closer to full. Packing by memory keeps each GPU's resident weight set bounded so evictions stay rare. With forbid_memory_oversubscription enabled (the default), this becomes a hard guarantee: a model is placed only on a worker where its weights still fit whenever some worker can hold it, so a feasible fully-resident placement is never passed over for one that strands a model on-demand (the worker LRU evicting a placed model). The packer falls back to the unconstrained choice, and logs the oversubscription warning above, only when the weights genuinely exceed every worker's budget.\n\nPlacement is kept stable mainly by smoothing the demand signal rather than by a large dead band: the arrival rate and per-request cost are each tracked with an exponential moving average whose halflife is measured in fleet compute-seconds (ema_halflife_compute_seconds, defaulting to one rebalance_compute_seconds interval, so the signal decays about half per repack and ages with how busy the fleet is rather than with wall-clock). hysteresis adds an optional dead band on top: a single-replica model moves only when the move improves its resulting pressure by more than hysteresis (default 0.0, i.e. move on any improvement), because batching depends on traffic staying where the queues are. (max_worker_share is accepted but advisory only; load no longer determines a model's GPU count.)\n\nRepacks are driven by accumulated compute, not wall-clock: the gateway polls the workers every probe round and recomputes the placement once the fleet has consumed rebalance_compute_seconds GPU-seconds since the last repack. This compute is cumulative across every GPU: the gateway sums each worker's consumed GPU-seconds, so a fleet of N busy GPUs accrues the budget about N times as fast as one (the accrual rate is the sum of the per-GPU utilizations). The budget is therefore in GPU-work, not real time, and the wall-clock gap between repacks shrinks as the fleet gets busier or larger and stretches out when it is idle (rebalance_compute_seconds divided by the sum of utilizations). The same cumulative clock drives ema_halflife_compute_seconds, so the demand signal also ages in GPU-work rather than wall-clock. The first traffic-driven repack can use a smaller first_rebalance_compute_seconds budget so an early rebalance corrects the cold placement quickly, then later repacks use the larger steady-state budget to limit memory churn (0 means the first repack uses rebalance_compute_seconds like the rest). An idle fleet does not repack until traffic resumes. Gateway-driven compaction of worker memory follows placement changes; see On-demand Weights for compaction_mode and compaction_interval.","title":"lpt_packing"},{"category":"section","location":"gateway/#The-fill-quantum","page":"Multi-GPU Gateway","text":"For a model with more than one replica, the gateway concentrates its requests on one replica at a time so the workers receive deep same-model groupings to coalesce (the coalescing itself stays at the worker; see Scheduling). The fill quantum Q is the model's worker-reported max batch size scaled by routing_fill_factor, and it is denominated in items, not requests. One request carrying a batch of 32 and 32 requests carrying one item each are the same amount of work, and both spend a quantum of 32.\n\nThe gateway does not guess where a request's items are counted. Each worker reports, per model, the name of the wire-facing input carrying the batch axis and that tensor's 1-based axis, taken from the bundle manifest's reserved n/b shape marker. The gateway matches that name against the request's own name-addressed inputs and reads that one dimension, without decoding the payload. This has to come from the manifest: the axis is per tensor and varies between bundles (an image model's whcn puts it last, a tokenized nc puts it first), the first input need not carry one at all (a cross-encoder's query is unbatched while its keys are batched), and it has moved between exports of the same model. A model that declares no batch axis counts one item per request.\n\nThat matters most for a client that batches on its own behalf. If your client already fills requests to the model's max batch size, a request-denominated quantum would hold one replica for 32 batches and your bursts would land on a single GPU; the item denomination makes one such request close the run. Models the worker does not batch (a meta, or a model compiled unbatched, both reporting a max batch of 0 or 1) charge one unit per request, since their leading dimension names an axis rather than a count.\n\nrouting_fill_mode decides what Q counts against:\n\nrun (default) counts items routed to the current replica. A run spends Q items on one replica, then the next run opens elsewhere, so a model's GPUs serve it in turn: batches stay deep and every replica gets an even share (to within one run) at any concurrency. A run also ends when the model has nothing in flight, so a low-rate model rotates per request instead of committing Q in a row, and when a replica falls a whole quantum behind the least-backed-up one, so a slow replica loses its turn rather than accumulating a queue.\nspread equalizes in-flight items across the replica set, so all k GPUs serve the model at once. This is the right mode when your client already batches to the max batch size, because then the worker has nothing left to coalesce and concentration only idles GPUs. It is also what promotion means for a latency-bound model whose client concurrency sits well below its max batch; the cost is coalescing depth, since each GPU now batches roughly 1/k as many requests. Prefer it per model rather than fleet-wide.\ninflight is the legacy basis: Q counts requests in flight, so a replica keeps receiving the model's traffic until it holds a full quantum at once.\n\nwarning: `inflight` can park a model on one GPU indefinitely\nBecause inflight compares the quantum against in-flight requests, a replica keeps winning until it holds Q of them simultaneously. A model whose in-flight concurrency stays between 2 and Q and never drains to zero is therefore served by one replica for the life of the process, and its other replicas receive no traffic at all. This is not a warm-up transient: with max_batch = 32 and a client holding 4 requests in flight, the second GPU receives zero requests, forever. Closed-loop clients (a fixed-size worker pool, a benchmark harness) hit this reliably, so promoting such a model to more GPUs under inflight buys nothing.Diagnose it with gateway_replica_routed_total: a flat series on one replica of a multi-replica model is the signature (gateway_replica_outstanding cannot show it, because a starved replica reads 0 either way). Use inflight only to reproduce the behavior that predated run.\n\nrouting_fill_factor is the direct trade between the two concerns: a model's share is even to within one quantum, so a smaller factor balances more finely at the cost of splitting more batches, and a larger one commits longer to each replica for deeper batches.\n\nrouting_policy (lpt_packing only) decides which replica each run opens on:\n\nfill_least (default) opens each run on the replica whose GPU currently carries the least in-flight work, measured across all models and denominated by work_basis (in-flight GPU-seconds by default). This is what you want whenever replicas share GPUs with other models: a model's runs open on whichever of its GPUs is least busy rather than always the same one.\nfill_rr rotates the opening replica across the model's set, so successive runs of the same model spread evenly over its GPUs. Deliberately load-blind, which is right only when every model on the fleet costs about the same and no GPU is shared with anything else.\n\nBoth policies are consulted at every run boundary, and exact ties rotate rather than falling back to the worker name, so an idle fleet warms every replica instead of pinning to the lowest-named one. Spreading every request without concentrating it at all is the separate least_outstanding scheduling mode above, not a fill mode. A single-replica model is the degenerate case: all its requests go to its one GPU (and still count toward that GPU's load for the fill_least decisions of models that share it).","title":"The fill quantum"},{"category":"section","location":"gateway/#Preconditions-and-startup","page":"Multi-GPU Gateway","text":"lpt_packing has two preconditions, verified at gateway startup: every worker must run the fifo or edf scheduler discipline, not fair (placement decisions move to the gateway, so workers should not re-order against it; see scheduler.discipline in Node Configuration), and every worker must serve the identical model set. Because a worker compiles and warms up every model before its control plane answers, the workers are usually not up when the gateway starts, so the gateway waits for all of them before serving rather than failing, logging which workers are still pending. Under the node supervisor (the embedded gateway) this wait is enabled automatically; for a standalone gateway set REACTANT_GATEWAY_STARTUP_WAIT_SECONDS (a number of seconds, or inf to wait indefinitely; the default 0 fails fast). Each poll is watchdog-bounded, and if the gRPC client stack wedges during the long warmup (a known libcurl failure mode) the gateway exits so the supervisor restarts it with a fresh stack; this self-heals and you may see one such restart before it serves. Once all workers are up, a wrong discipline or differing model set is a hard error.\n\nRuntime drift after startup degrades gracefully: a model temporarily missing from some workers is routed uniformly over its actual replicas with a warning until the fleet converges, and a worker that drops out is excluded from placement, its traffic failing over to the remaining replicas.","title":"Preconditions and startup"},{"category":"section","location":"gateway/#Observability","page":"Multi-GPU Gateway","text":"The placement is observable: gateway_model_replicas reports each model's replica count, gateway_placement_weight reports its per-worker weight, gateway_replica_outstanding reports the in-flight items per replica (summed batch sizes, matching the quantum's denomination; equal to a request count when every request carries one item), gateway_replica_routed_total counts the requests routed to each replica since start (the series that shows whether every replica is actually being used), gateway_model_fill_quantum reports each model's effective quantum, gateway_repacks_total counts repacks by what triggered them, and gateway_model_utilization reports each model's estimated demand in GPU-seconds per second.\n\ngateway_worker_inflight_work reports the per-worker load the router actually compares when it picks a replica, so a routing decision can be explained after the fact. It is exported by both work-routing mechanisms (fill_least under lpt_packing, and the least_outstanding mode) and carries a basis label, because the unit follows work_basis: GPU-seconds under compute, items under items, and a per-request cost under requests. Read it live at scrape time from the scheduler's counters, so it never lags a prober tick, and a worker that no longer hosts anything stops being emitted rather than freezing at its last value. round_robin exports nothing here, since it tracks no load.\n\nThe bundled Grafana dashboards use it: Scheduling & Placement plots the per-worker load, the spread across workers (max - min, which is what fill_least exists to shrink), and the basis in force as the running gateway reports it, and Fleet Overview carries the per-worker load alongside readiness and device memory. For whether the routing actually produced an even result, as opposed to what the router intended, the GPU Work Balance dashboard is the one to read. It is built on rate(worker_compute_seconds_total) per worker, the device-seconds each GPU really consumed, and leads with the spread of that rate across devices (0% is even). Its planned-vs-actual panel is the diagnostic that separates a skewed plan (a repack or a replica count is the lever) from routing not following an even plan. A persistently high spread usually means a model's replicas cannot absorb the imbalance (too few replicas, or a single-replica model pinning one GPU) rather than a misbehaving policy, so read it next to the placement table.","title":"Observability"},{"category":"section","location":"gateway/#Runtime-scheduling-control","page":"Multi-GPU Gateway","text":"The gateway answers its own gRPC service, reactant_control.GatewayControlService, on the same port it serves inference. It exposes the live scheduling state and lets an operator retune it without a restart:\n\nRPC Purpose\nGetSchedulingStatus The mode, every runtime knob, repack bookkeeping, and one row per model and per worker (placement, in-flight and routed counts, resolved fill mode and quantum, measured demand and cost, assigned weights against each worker's budget). Answers in every scheduling mode.\nSetSchedulingPolicy Change any knob in the scheduling: block except mode. update_mask names the fields to apply; one invalid value rejects the whole request and nothing changes.\nSetModelPlacement Set, change, or clear one model's replicas, fill_mode, and fill_factor. The three are independent, so one call can promote a model and change how it uses its GPUs. Effective at the next repack.\nRepack Repack now, bypassing the accumulated-compute budget, optionally waiting a bounded time for it to land.\n\ntools/gateway_ctl.jl is the operator front end, run against the gateway's gRPC port:\n\njulia --project=packages/ReactantServerGateway tools/gateway_ctl.jl --gateway HOST:8001 status\n... set-replicas big-model 2 --fill-mode spread\n... repack --wait 30\n... set-policy hysteresis=0.15 routing_policy=fill_least compaction_interval=4\n\ngrpcurl -plaintext -proto proto_src/reactant_control_v1.proto ... works too, with no Julia installed.\n\nwarning: Runtime changes are not persisted, and there is no authentication\nEvery override lives in memory only: a gateway restart reverts it to gateway.yml plus the environment, and restart is not hypothetical (the gateway deliberately exits on a wedged client stack, expecting its supervisor to bring it back). Treat these RPCs as a way to try a setting or respond to an incident, and write anything you want to keep into gateway.yml. SchedulingPolicy.generation reads 0 while every knob still matches the config file and is bumped by each accepted change, so you can tell a tuned gateway from a fresh one, and each accepted change is logged with its before and after values, which is the only durable record of it. Like the rest of the control plane these RPCs are unauthenticated (see Deployment), so the gateway's gRPC port must not be exposed outside the trusted network.","title":"Runtime scheduling control"},{"category":"section","location":"gateway/#What-the-gateway-does-not-do","page":"Multi-GPU Gateway","text":"Streaming RPCs.\nThe repository / model-config / statistics / trace / log RPCs in the Triton spec, plus ServerLive, ServerReady, ModelMetadata, and RepositoryIndex for clients. Of the KServe data plane only ModelInfer, the two SHM register/unregister RPCs, and IsSameIPCNamespace are proxied; everything else returns UNIMPLEMENTED. On the control plane the gateway answers ControlService/CompactMemory (fanned out to every worker) and all four GatewayControlService RPCs itself (see Runtime scheduling control); the other ControlService RPCs are worker-only.\nTLS: parsed but not yet enforced; the listener and the worker back-hop are cleartext h2c.\nCUDA shared memory.\nDynamic worker membership: the worker endpoint list is fixed at startup (from gateway.yml or REACTANT_GATEWAY_WORKERS). Which models each worker serves is rediscovered continuously, but adding or removing workers requires a gateway restart.","title":"What the gateway does not do"},{"category":"section","location":"gateway/#Configuration","page":"Multi-GPU Gateway","text":"The supervisor configures the embedded gateway for you: it synthesizes the worker endpoint list (and the worker metrics list) from the node file into REACTANT_GATEWAY_WORKERS / REACTANT_GATEWAY_WORKER_METRICS, and binds the gateway to the public ports (8001/8002). Nothing about model placement is configured on the gateway; it autodiscovers which models each worker serves via RepositoryIndex and refreshes its routing table periodically.\n\nTo tune the gateway, provide a gateway.yml and point REACTANT_GATEWAY_FILE at it (or leave it at the conventional /etc/reactantserver/gateway.yml, which the supervisor picks up automatically; or, for the embedded gateway, set the REACTANT_GATEWAY_* environment below); it carries the gateway's own settings (listen addresses, message limits, logging, and the scheduling: block above). Settings can also be overridden by environment with the prefix REACTANT_GATEWAY_ and the dotted path uppercased with underscores, for example REACTANT_GATEWAY_LOGGING_LEVEL=debug or REACTANT_GATEWAY_SCHEDULING_MODE=lpt_packing.\n\nnote: A mounted `gateway.yml` owns the endpoint list\nPer-model settings (scheduling.models.<name>) are the one part of the config with no environment equivalent, since the overrides only reach scalar keys, so promoting a single model requires a file. When the supervisor finds one it stops synthesizing the worker lists, because the file is now the authority: the file must therefore carry endpoints: itself, plus metrics_endpoints: and worker_names: if you want the aggregated /metrics and the worker0..N labels the Grafana dashboards join on. The startup wait is not suppressed (it describes the supervisor's own co-launched workers, not gateway configuration), so an lpt_packing node still waits for its workers to finish compiling rather than crash-looping.\n\nFor a standalone deployment, the package also ships probe_worker_ready, the worker container's healthcheck (a Julia replacement for the former Go reactant-healthprobe): it resolves a worker's port from the node file and calls its KServe ServerReady on localhost, returning whether it reported ready.","title":"Configuration"},{"category":"section","location":"gateway/#Operational-notes","page":"Multi-GPU Gateway","text":"The gateway is a single point of failure. Each Julia worker stays reachable on its own KServe V2 gRPC endpoint during a gateway outage, so a client can fall back to addressing a worker directly.\nThe routing table is rebuilt every 10s from each worker's RepositoryIndex and swapped in atomically. If a worker dies, its routes persist until the next successful probe (up to ~10s); in the gap, requests to its models fail over to the remaining replicas (on NotFound or Unavailable), and a model with no live replica returns NotFound. The worker-side readiness probe (ServerReady, same 10s loop) drives /readyz and the gateway_worker_ready metric.\nUnder lpt_packing, the gateway polls the workers on every 10s probe round to refresh routing metadata and accumulate consumed compute, but recomputes the placement only once the fleet has consumed scheduling.rebalance_compute_seconds GPU-seconds (the first repack uses scheduling.first_rebalance_compute_seconds when set). Each repack logs a lpt_packing: repack line with the number of models placed, how many moved workers, how many were held_by_hysteresis, the largest available max_improvement against the hysteresis threshold, and the compute_seconds/wall_seconds since the last repack, useful for watching placement churn and the trigger cadence.\nIf a probe to a worker hangs (times out, rather than failing fast), the gateway drops and recreates that worker's gRPC connection before the next attempt. This recovers from a half-open connection (for example one caught during a worker's brief silent-accept window at startup) that would otherwise be reused and stall every later request to that worker, the per-worker equivalent of a restart, without dropping HTTP/2 multiplexing for healthy workers.\nSuccessful ModelInfer requests are not logged (to keep the hot path quiet); worker errors and a model with no live replica are logged, and per-request latency and gRPC status are exported as Prometheus metrics. Logs contain no tensor data.","title":"Operational notes"},{"category":"section","location":"object_detection/#Object-Detection-(GeneralizedRCNN)","page":"Object Detection","text":"A two-stage object detector is the canonical case for a meta model. A torchvision Faster R-CNN (torchvision.models.detection.fasterrcnn_resnet50_fpn, an FPN backbone + RPN + RoIHeads GeneralizedRCNN) is mostly dense tensor math, but the middle of the pipeline is not: selecting RPN proposals, running NMS, pooling ROI features with roi_align, decoding boxes, and the final per-class NMS are all data-dependent and cannot be captured by torch.export as a single static graph. The shape of the work changes with the contents of the tensors, which is exactly what a static StableHLO program cannot express.\n\nThe package ships a converter that handles this split for you. It produces two dense StableHLO bundles for the parts that trace cleanly and a meta bundle whose model.jl runs the data-dependent glue in Julia between them. The reusable detection math (NMS, roi_align, box decode, anchor generation) lives in the ReactantServer.DetectionGlue module and is already part of the package, so the generated model.jl is small and the conversion is config-driven.\n\nThis page walks through converting a standard torchvision detector end to end. For the underlying execution model (the meta gate, committed sub-calls, placement) see Meta Models.","title":"Object Detection (GeneralizedRCNN)"},{"category":"section","location":"object_detection/#The-shape-of-the-conversion","page":"Object Detection","text":"One source model is emitted as three bundles under the output root:\n\nBundle Contents Kind\n<name>_stage1 Backbone + RPN head StableHLO\n<name>_stage2 Box head + box predictor over a fixed number of ROIs StableHLO\n<name> Chains stage1, then DetectionGlue, then stage2, then final NMS meta\n\nstage1 takes the preprocessed image and returns 14 dense tensors: the four ROI-pooling feature maps (feat_0-feat_3), the five per-level objectness maps (obj_0-obj_4), and the five per-level box deltas (delta_0-delta_4). stage2 takes ROI-pooled features for a fixed K proposals ([K, 256, 7, 7] in torch, (7, 7, 256, K) in the Julia column-major wire layout) and returns cls_logits ([K, num_classes]) and bbox_deltas ([K, num_classes*4]). The meta bundle owns no weights of its own; it is placed as a group with its two stages and routed by the gateway as a single unit (see the placement section of Meta Models).\n\nOnly <name> is addressable by clients. The two stages are internal to the meta and never appear in the gateway's routing table.\n\nCrucially, both stages are plain nn.Modules that torch.export traces directly: the converter builds the torchvision model, wraps backbone + rpn.head and box_head + box_predictor, and exports them. There is no torch.jit load and no reaching into a scripted graph's frozen internals.","title":"The shape of the conversion"},{"category":"section","location":"object_detection/#Running-the-converter","page":"Object Detection","text":"The converter is tools/convert_to_stablehlo.jl, driven by a YAML config. The torchvision detector is a handler (a special-case builder), shipped at tools/handlers/torchvision_frcnn_detector.jl. Reference it from the handlers: block of your config, keyed by a model name:\n\noutput_root: /docker/reactantserver/models\n\nhandlers:\n  - file: handlers/torchvision_frcnn_detector.jl\n    models: [my_detector]\n    options:\n      weights: DEFAULT      # DEFAULT = pretrained COCO; \"none\" = random; or a .pth path\n      num_classes: 91       # classes INCLUDING background; set when loading a custom head\n      image_size: 640       # canonical square input edge, divisible by 64\n      input_dtype: u8       # client image dtype (u8 | f32)\n      output_cols: 6        # 5 = [box4, score]; 6 = [box4, score, class]\n\nRelative paths (including file: and any option key ending in _dir/_path) resolve against the config file's directory. With weights: DEFAULT the converter builds the pretrained COCO model, so no source artifact is needed (the runnable demo path); to convert your own trained detector, point weights at a saved state_dict and set num_classes to match its head. A handler runs after the torch/torchax/triton imports, so it may freely call pyexec/pyimport.\n\nRun it from the repository root, instantiating against an environment that has torch, torchvision, torchax, and ReactantServerExport:\n\njulia tools/convert_to_stablehlo.jl <config>.yaml --only my_detector\n\nUse --dry-run to validate the config and handler load without paying torch startup, and --force to rebuild a bundle that already exists. The run emits my_detector_stage1, my_detector_stage2, and the my_detector meta bundle.","title":"Running the converter"},{"category":"section","location":"object_detection/#The-generated-meta-model.jl","page":"Object Detection","text":"The handler bakes the per-model config it reads from the live model (the per-level cell_anchors, the RPN/ROI box-coder weights, the RPN pre-NMS top-k and NMS threshold, and the final score/nms/detections_per_img) into the meta bundle's model.jl, then registers the orchestration with register_meta_model. The emitted function, lightly abridged, is:\n\nconst _G = ReactantServer.DetectionGlue\n\nfunction _run(inputs, call)\n    iw = size(inputs[1].data, 1); ih = size(inputs[1].data, 2)\n\n    # Stage 1: backbone + RPN head. 14 dense outputs.\n    s1 = call(\"my_detector_stage1\", inputs)\n    d  = Dict(t.name => t.data for t in s1)\n\n    # Per-level: generate anchors, decode RPN deltas to boxes, flatten objectness.\n    bl = Matrix{Float64}[]; sl = Vector{Float64}[]\n    for i in 1:5\n        O = d[_OBJ[i]]; D = d[_DEL[i]]\n        anc = _G.generate_anchors(size(O, 2), size(O, 1), _STR[i], _CELL[i])\n        push!(bl, _G.decode_boxes(_G.deltas_matrix(D), anc, _RPNW))\n        push!(sl, _G.objectness_flat(O))\n    end\n\n    # Select the top-K proposals (NMS across levels), then ROIAlign the feature maps.\n    pb = _G.select_rpn_proposals(bl, sl, ih, iw; pre=_PRE, post=_K, nms_thresh=_RPNNMS)\n    Kp = size(pb, 1)\n    feats = [_G.feature_chw(d[f]) for f in _FEAT]\n    roi = call.scratch((7, 7, 256, _K), Float32); fill!(roi, 0f0)\n    lv = [_G.assign_level(@view pb[k, :]) for k in 1:Kp]\n    for l in 0:3\n        sel = findall(==(l), lv); isempty(sel) && continue\n        _G.roi_align_wire!(view(roi, :, :, :, sel), feats[l+1], pb[sel, :], _SCALES[l+1];\n                           ratio=2, aligned=false)\n    end\n\n    # Stage 2: box head + predictor on the pooled ROIs, then the final per-class NMS.\n    s2 = call(\"my_detector_stage2\", [ReactantServer.NamedTensor(\"ROI_FEATS\", roi)])\n    d2 = Dict(t.name => t.data for t in s2)\n    cls = permutedims(d2[\"cls_logits\"], (2, 1))[1:Kp, :]\n    dl  = permutedims(d2[\"bbox_deltas\"], (2, 1))[1:Kp, :]\n    bx, sc, cl = _G.fast_rcnn_inference(cls, dl, pb, ih, iw;\n        score_thresh=_SCORE, nms_thresh=_NMS, topk=_TOPK, weights=_ROIW,\n        bg_first=true, min_size=1e-2)\n\n    out = isempty(sc) ? zeros(Float32, 6, 0) :\n        Array{Float32}(permutedims(hcat(bx, sc, Float64.(cl)), (2, 1)))\n    return [ReactantServer.NamedTensor(\"OUTPUT__0\", out)]\nend\n\nregister_meta_model(\"my_detector\"; run = _run)\n\nAbove _run, the handler emits the per-model constants the function references: the FPN strides and scales (_STR, _SCALES), the per-level cell anchors (_CELL), the RPN and ROI box-coder weights (_RPNW, _ROIW), the RPN pre-NMS top-k (_PRE) and NMS threshold (_RPNNMS), and the final score/nms/detections_per_img (_SCORE, _NMS, _TOPK). With output_cols: 5 the output assembly line instead emits [box4, score] rows.\n\nEvery data-dependent step is a plain function in ReactantServer.DetectionGlue: generate_anchors, decode_boxes, select_rpn_proposals, roi_align_wire!, assign_level, and fast_rcnn_inference. A few DetectionGlue knobs select the torchvision conventions: roi_align_wire!(...; aligned=false) uses torchvision's ROIAlign offset and malformed-ROI clamp, and fast_rcnn_inference(...; bg_first=true, min_size=1e-2) treats class 0 as background and drops sub-pixel final boxes the way torchvision's postprocess_detections does. The ROI feature tensor is the large intermediate handed between stages, so it is allocated from the worker's reuse pool with call.scratch; in a fleet that buffer is backed by a shared-memory slot so the sub-call sends it by reference instead of serializing it (see the call.scratch section of Meta Models).","title":"The generated meta model.jl"},{"category":"section","location":"object_detection/#Options-and-assumptions","page":"Object Detection","text":"Option Default Meaning\nweights DEFAULT DEFAULT = pretrained COCO; none = random; or a state_dict .pth.\nnum_classes 91 Classes including background; set this when loading a custom head.\nimage_size 640 Canonical square input edge, divisible by 64 (FPN p6/pool stride).\ninput_dtype u8 Client image dtype: u8 (/255 then normalized) or f32 (assumed in [0,1]).\noutput_cols 6 Per-detection width: 5 = [box4, score], 6 = [box4, score, class].\ninput_shapes none Optional [W, H] pairs for extra aspect ratios, one weight set.\n\nEach input_shapes edge must be divisible by 64. The meta routes each request to the matching variant by its input shape; stage2 is shared, because its ROI input is always the fixed 7x7 grid.\n\nTwo assumptions are worth calling out:\n\nInput is a batched RGB image. The client sends an NCHW image ([1, 3, H, W]) at the compiled size; stage1 bakes the ImageNet normalization (and the /255 for u8) so the client sends a raw image. The served wrapper letterboxes to one of the compiled image_size/input_shapes; the model is not resized at run time.\nClass ids follow torchvision. With output_cols: 6 the emitted class is the torchvision label (1..num_classes-1); background (class 0) is dropped.","title":"Options and assumptions"},{"category":"section","location":"object_detection/#Runnable-example","page":"Object Detection","text":"examples/object_detection/ in the repository is a complete, runnable version of this walkthrough: export a torchvision Faster R-CNN pretrained on COCO into StableHLO bundles, serve them on a single GPU, send an image, and draw the predicted boxes + COCO labels back onto it with CairoMakie (detections.jpg). The demo model is named object_detector, configured by examples/object_detection/detector.convert.yaml:\n\noutput_root: bundles\nreport_path: bundles/conversion_report.md\n\nhandlers:\n  - file: ../../tools/handlers/torchvision_frcnn_detector.jl\n    models: [object_detector]\n    options:\n      weights: DEFAULT     # pretrained COCO weights (downloaded by torchvision on first export)\n      num_classes: 91      # COCO classes including background\n      image_size: 640      # canonical square input edge (divisible by 64)\n      input_dtype: u8      # client sends a uint8 image; stage1 bakes /255 + ImageNet normalize\n      output_cols: 6       # per detection: [x1, y1, x2, y2, score, class]\n\nThe example is split into three single-purpose Julia environments so each loads only what it needs (and they stop invalidating each other's precompilation): export is the only one with PythonCall\n\ntorch, server is the only one with Reactant, and client has CairoMakie but no Reactant.\n\nEach environment resolves independently the first time you use it:\n\nfor env in export server client; do\n  julia --project=examples/object_detection/$env -e 'using Pkg; Pkg.instantiate()'\ndone\n\nThen run the three steps in order (the server stays running; drive it from a second terminal):\n\n# 1. Export the bundles (first time only; writes ./bundles/). Needs network for the COCO weights.\njulia --project=examples/object_detection/export \\\n    examples/object_detection/export/export.jl\n\n# 2. Serve on a single GPU (blocks; Ctrl-C to stop). Add --cpu for a GPU-free smoke test.\nCUDA_VISIBLE_DEVICES=0 julia --project=examples/object_detection/server \\\n    examples/object_detection/server/serve.jl\n\n# 3. In another terminal: send an image and draw the result -> ./detections.jpg\njulia --project=examples/object_detection/client \\\n    examples/object_detection/client/detect.jl\n\nPass your own image to step 3 as the first argument (a local path). The server port defaults to 8080; set OD_PORT (and OD_HOST for the client) to change it on both step 2 and step 3.","title":"Runnable example"},{"category":"section","location":"object_detection/#Export","page":"Object Detection","text":"export/export.jl drives the shared converter in-process, so torch imports before Reactant (the converter's required order), and writes examples/object_detection/bundles/ with the object_detector, object_detector_stage1, and object_detector_stage2 bundles. It skips conversion when the bundles already exist (delete the bundles/ dir to re-export). The pretrained COCO weights are downloaded by torchvision on the first run, so export needs network. On corporate networks export.jl points Python's TLS at the OS CA bundle (SSL_CERT_FILE, defaulting from REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE/JULIA_SSL_CA_ROOTS_PATH or /etc/ssl/certs/ca-certificates.crt) so the weight download trusts a MitM proxy's CA. The Python dependencies are export-only: torch/torchax/jax come from ReactantServerExport's CondaPkg and torchvision from export/CondaPkg.toml; CondaPkg resolves and installs them on the first export.","title":"Export"},{"category":"section","location":"object_detection/#Serve","page":"Object Detection","text":"server/serve.jl serves the object_detector meta bundle on 127.0.0.1:$OD_PORT (default 8080) and blocks until Ctrl-C. Pass --cpu to use ReactantServer.CPU_BACKEND for a GPU-free smoke test; otherwise it uses ReactantServer.CUDA_BACKEND.","title":"Serve"},{"category":"section","location":"object_detection/#Client","page":"Object Detection","text":"client/detect.jl connects to the running server with the ReactantServerClient library (no Reactant and no PythonCall, so it loads fast). It resizes the image to 640x640 (matching the export image_size), sends it as INPUT__0 through infer_sync, reads OUTPUT__0 as Float32, draws the boxes and labels with CairoMakie, and writes examples/object_detection/detections.jpg. With no image argument it downloads a default object-rich test photo (https://ultralytics.com/images/bus.jpg). Each output row is [x1, y1, x2, y2, score, class], boxes in the 640x640 input pixel space, class a COCO id mapped to a name through the COCO categories table in detect.jl. The model bakes a 0.05 score threshold; the client additionally only draws detections scoring at least 0.5.\n\nOn the first true end-to-end run, watch the client's Raw output size=... log line: it confirms the OUTPUT__0 orientation (parse_detections handles either), and sanity-check that the drawn boxes land on the right objects.","title":"Client"},{"category":"section","location":"object_detection/#See-also","page":"Object Detection","text":"Meta Models for the execution model, gating, deadlines, and placement\nBundles & model.jl for the plain bundle path and the manifest encoding\nClient Usage for the client library the demo uses\nregister_meta_model in the API reference","title":"See also"},{"category":"section","location":"api/#API-reference","page":"API","text":"Every exported name that carries a docstring across the five documented packages appears below, collected automatically. ReactantServer re-exports the whole ReactantServerCore substrate, so the shared API (dtypes, protobuf messages, boundary types, manifest, config, codec, shared memory, the staging BufferPool) is documented under its original bindings once. The guide pages give the worked context: Tutorial, Bundles, Node Configuration, Scheduling, On-demand Weights, Multi-GPU Gateway, Client Usage, Meta Models, Object Detection, Transformer Text Models, and Deployment.","title":"API reference"},{"category":"section","location":"api/#Operational-internals","page":"API","text":"A few names the guides and docstrings reach for are deliberately unexported; they are documented here so their docstrings render and the qualified references in the guides are backed by the manual. Reach them from the REPL fully qualified, for example ReactantServer.scheduler_metrics(). They are implementation details and may change without a breaking release.","title":"Operational internals"},{"category":"method","location":"api/#ReactantServer.register_heartbeat!-Tuple{Any}","page":"API","text":"register_heartbeat!(f) -> f\n\nRegister f(event::Symbol, detail) to receive this process's serving heartbeat:\n\n:up, detail::ServerConfig: a server finished bring-up and is accepting traffic\n:request, detail::String: a ModelInfer request for that model completed or failed\n:down, detail::ServerConfig: stop! tore the server down (a blocking serve fires it when run returns)\n:load / :unload, detail::String: the directory watcher loaded (or reloaded) or unloaded that bundle\n\n:request fires on failure too: a failing client is still a client using this server. The callback is fired synchronously on the request task and must not block.\n\n\n\n\n\n","title":"ReactantServer.register_heartbeat!"},{"category":"method","location":"api/#ReactantServer.register_meta_model-Tuple{AbstractString}","page":"API","text":"register_meta_model(name; run)\n\nCalled from a meta bundle's model.jl to register the orchestration function. run has the form run(inputs::Vector{NamedTensor}, call) -> Vector{NamedTensor}, where call(model_name, inputs) invokes another model. The meta runs as a scheduled unit holding the GPU exclusively, and call invokes the sub-model's compiled executable directly in-process (no queue re-entry, no gateway hop).\n\n\n\n\n\n","title":"ReactantServer.register_meta_model"},{"category":"method","location":"api/#ReactantServer.register_model-Tuple{AbstractString}","page":"API","text":"register_model(name; preprocess=identity, postprocess=identity)\n\nCalled from a bundle's model.jl to register custom pre/post-processing. Both hooks receive and return a Vector{NamedTensor}. Omitted hooks default to identity.\n\n\n\n\n\n","title":"ReactantServer.register_model"},{"category":"method","location":"api/#ReactantServer.register_serve_guard!-Tuple{Any}","page":"API","text":"register_serve_guard!(f) -> f\n\nRegister f(cfg::ServerConfig) to be called before every serve brings a server up. A guard refuses by throwing; the error propagates to the caller of serve unchanged and nothing has been allocated yet. Guards run in registration order.\n\n\n\n\n\n","title":"ReactantServer.register_serve_guard!"},{"category":"method","location":"api/#ReactantServer.serve-Tuple{AbstractString}","page":"API","text":"serve(node_path; worker=nothing, backend=ReactantBackend(), blocking=true) -> nothing | RunningServer\n\nLoad the node config at node_path, resolve this process's worker, bring up the runtime and its assigned models, and start the gRPC control plane. worker selects which worker entry to serve; it may be omitted when the node has exactly one worker. When blocking is false the server runs in the background and a RunningServer is returned (stop it with stop!).\n\n\n\n\n\n","title":"ReactantServer.serve"},{"category":"method","location":"api/#ReactantServer.serve_worker-Tuple{AbstractString, AbstractString}","page":"API","text":"serve_worker(node_path, worker; backend=ReactantBackend(), blocking=true)\n\nConvenience alias for serve that names the worker positionally.\n\n\n\n\n\n","title":"ReactantServer.serve_worker"},{"category":"method","location":"api/#ReactantServer.stop!-Tuple{ReactantServer.RunningServer}","page":"API","text":"stop!(s::RunningServer)\n\nShut down a server started with serve(...; blocking=false). Stops the model-directory watcher (if running), closes the metrics endpoint (if running) and the gRPC server, halts the scheduler's dispatch loop, and tears down any registered shared-memory regions. Returns nothing.\n\n\n\n\n\n","title":"ReactantServer.stop!"},{"category":"type","location":"api/#ReactantServerClient.AbstractInferenceIO","page":"API","text":"AbstractInferenceIO\n\nInterface for streaming a dataset through batched inference with infer_async / infer_sync. A concrete subtype implements length(io), item_input_bytes(io), infer_encode_chunk!(io, range, slot) (stage a chunk's inputs into the pool slot and return the input descriptors), and infer_decode_chunk!(io, range, response) (consume the response). The pool owns the staging bytes; the IO must not retain slot references past infer_encode_chunk!.\n\nA subtype may optionally implement output_specs to read its outputs back through shared memory. The default is empty, which keeps every output inline (the response carries raw_output_contents as before). Declaring outputs is transparent to infer_decode_chunk!: the driver reads the shared-memory results back into the response before handing it over, so InferOutput works the same on either transport.\n\n\n\n\n\n","title":"ReactantServerClient.AbstractInferenceIO"},{"category":"type","location":"api/#ReactantServerClient.KServeModel","page":"API","text":"KServeModel(host, port, model_name; secure=false, max_batch_size=1, deadline=10.0, ...)\nKServeModel(url, model_name; max_batch_size=1, deadline=10.0, ...)\n\nA handle to one model served by a KServe V2 gRPC endpoint (a ReactantServer worker or the gateway). The second form parses a url of the form grpc://host:port (or host:port); grpcs/https selects a secure channel. max_batch_size caps how many items the batched infer_async / infer_sync drivers coalesce per request; deadline is the per-request timeout in seconds. max_send_message_length / max_receive_message_length bound a single gRPC message; each defaults to 512 MiB (matching the worker and gateway), or the REACTANT_CLIENT_GRPC_MAX_SEND_MSG_BYTES / REACTANT_CLIENT_GRPC_MAX_RECV_MSG_BYTES environment variable when set and the kwarg is omitted.\n\nshared_memory controls system shared-memory transport for staged inputs/outputs:\n\n:auto (default): probe the server with IsSameIPCNamespace; use shared memory only if the server confirms it shares this client's IPC namespace. If the server returns false, or does not implement the RPC, fall back to inline transport. There is no silent runtime fallback.\n:on: force shared memory. The server confirming a different namespace is a hard error (fail loudly, no fallback). If the server does not implement IsSameIPCNamespace (e.g. stock Triton), shared memory is still attempted via SystemSharedMemoryRegister; making that work is then the caller's responsibility.\n:off: never use shared memory; always send inline. The probe is not sent.\n\n\n\n\n\n","title":"ReactantServerClient.KServeModel"},{"category":"type","location":"api/#ReactantServerClient.OutputSpec","page":"API","text":"OutputSpec(name, dtype, per_item_dims)\n\nDeclares an expected network output so the driver can read it back through shared memory. name is the output tensor name, dtype its element type, and per_item_dims its Julia column-major shape for a single item, excluding the leading batch dimension. For each chunk the driver reserves sizeof(dtype) * prod(per_item_dims) * items_in_chunk bytes in the staging slot and asks the server to write that output there. See output_specs.\n\n\n\n\n\n","title":"ReactantServerClient.OutputSpec"},{"category":"method","location":"api/#ReactantServerClient.InferInput-Union{Tuple{T}, Tuple{String, AbstractArray{T}}} where T<:Union{Bool, Float16, Float32, Float64, Int16, Int32, Int64, Int8, UInt16, UInt32, UInt64, UInt8}","page":"API","text":"InferInput(name, array) -> ModelInferRequest.InferInputTensor\n\nBuild a wire input tensor named name from a Julia array, shipping the bytes inline. You pass the array in its natural Julia column-major shape (W, H, …, N); the client reverses it to the network's row-major (N, …, H, W) internally (the bytes are unchanged). Pass a vector of these to the one-shot infer_sync(model, inputs). Variants taking an explicit Julia column-major shape and a typed contents vector are also provided.\n\n\n\n\n\n","title":"ReactantServerClient.InferInput"},{"category":"method","location":"api/#ReactantServerClient.InferOutput-Union{Tuple{T}, Tuple{String, ReactantServerClient.inference.ModelInferResponse, Type{T}}} where T<:Union{Bool, Float16, Float32, Float64, Int16, Int32, Int64, Int8, UInt16, UInt32, UInt64, UInt8}","page":"API","text":"InferOutput(name, response, dtype) -> Array\nInferOutput(name, response) -> Array\n\nExtract the output tensor named name from a ModelInferResponse as a Julia array. The wire row-major shape (N, …, H, W) is reshaped (no copy) to Julia column-major (W, …, N). Pass the element type as dtype for a type-stable result; the two-argument form reads the dtype from the response metadata.\n\n\n\n\n\n","title":"ReactantServerClient.InferOutput"},{"category":"method","location":"api/#ReactantServerClient.infer_async-Tuple{AbstractInferenceModel, AbstractInferenceIO}","page":"API","text":"infer_async(model, io::AbstractInferenceIO)\n\nRun inference over every item in io, staging inputs through the shared-memory [BufferPool] and dispatching chunks concurrently (bounded by the pool's slot count). Each chunk acquires a disjoint slot, so this is safe to call from multiple threads against one model. Results are delivered through io's infer_decode_chunk!. Use infer_sync for serial dispatch.\n\n\n\n\n\n","title":"ReactantServerClient.infer_async"},{"category":"method","location":"api/#ReactantServerClient.infer_sync-Tuple{AbstractInferenceModel, AbstractInferenceIO}","page":"API","text":"infer_sync(model, io::AbstractInferenceIO)\ninfer_sync(model, network_inputs) -> ModelInferResponse\n\nSynchronous inference. The AbstractInferenceIO form drives io one chunk at a time (no concurrency). The second form is a one-shot call: network_inputs is a vector of wire tensors built with InferInput, sent inline in a single ModelInferRequest, and the decoded ModelInferResponse is returned for reading with InferOutput.\n\n\n\n\n\n","title":"ReactantServerClient.infer_sync"},{"category":"method","location":"api/#ReactantServerClient.kserve_init-Tuple{}","page":"API","text":"kserve_init(; pool_bytes=DEFAULT_POOL_BYTES, n_slots=DEFAULT_POOL_SLOTS, shm_reprobe_interval=60.0)\n\nInitialize the gRPC subsystem and (re)set the staging-pool parameters. n_slots is the number of fixed-size slots each pool is divided into, which bounds how many chunks can be in flight concurrently against a single pool. shm_reprobe_interval (seconds) is how often the background task re-probes endpoints that fell back to inline transport and restores shared memory if the server has recovered; set it to 0 (or negative) to disable that background recovery.\n\n\n\n\n\n","title":"ReactantServerClient.kserve_init"},{"category":"method","location":"api/#ReactantServerClient.kserve_shutdown-Tuple{}","page":"API","text":"kserve_shutdown()\n\nTear down the client: unregister and unlink the shared-memory pool from every server it was registered with, drop the cached pools and per-URL routes, and shut the gRPC subsystem down. Pair with kserve_init.\n\n\n\n\n\n","title":"ReactantServerClient.kserve_shutdown"},{"category":"method","location":"api/#ReactantServerClient.manifest_io_spec-Tuple{AbstractString}","page":"API","text":"manifest_io_spec(path) -> ModelIOSpec\n\nLoad a model's input/output spec from a manifest YAML at path, with no running server. Reuses the server's own wire encoding (encode_model_metadata), so the result matches model_io_spec for the same model. Suitable for an offline precompile or build-time check. The reported shapes are Julia column-major (batch axis last), so they read in the same axis order as the manifest's einsum letters.\n\n\n\n\n\n","title":"ReactantServerClient.manifest_io_spec"},{"category":"method","location":"api/#ReactantServerClient.model_io_spec-Tuple{KServeModel}","page":"API","text":"model_io_spec(model::KServeModel) -> ModelIOSpec\n\nFetch a model's input/output spec from a running server over the ModelMetadata RPC. Throws if the server is unreachable or does not implement ModelMetadata; the call is explicit, so failing loudly is intended. Pair with validate_io or use it to introspect a model's I/O directly. The reported shapes are Julia column-major (batch axis last), matching the order you build arrays in; the row-major KServe wire shape is reversed away by the client.\n\n\n\n\n\n","title":"ReactantServerClient.model_io_spec"},{"category":"method","location":"api/#ReactantServerClient.output_specs-Tuple{AbstractInferenceIO}","page":"API","text":"output_specs(io) -> Vector{OutputSpec}\n\nOutputs an AbstractInferenceIO declares for shared-memory read-back. Defaults to empty, which keeps every output inline (raw_output_contents) exactly as before: no output declaration means no shared memory, the safe fallback. Returning a non-empty vector opts the IO into shared-memory outputs and explicit-output mode: the request asks the server for exactly these outputs in this order, so every output the IO consumes must be declared. Outputs with data-dependent (dynamic) shapes cannot be sized ahead of time and must stay inline (leave them out, which means returning empty here).\n\n\n\n\n\n","title":"ReactantServerClient.output_specs"},{"category":"method","location":"api/#ReactantServerClient.validate_io-Tuple{ModelIOSpec, AbstractInferenceIO}","page":"API","text":"validate_io(spec::ModelIOSpec, io::AbstractInferenceIO; items=1)\nvalidate_io(model::KServeModel, io::AbstractInferenceIO; items=1)\n\nDry-run io against a model's true I/O spec without sending an inference request. Runs the user's infer_encode_chunk! and infer_decode_chunk! (see AbstractInferenceIO) once for the first items items against a synthetic, spec-shaped request and response, checking input/output names, dtypes, and shapes and surfacing indexing or shape errors in the user's own code as exceptions.\n\nspec comes from manifest_io_spec (offline) or model_io_spec (online); the KServeModel form fetches it online. The harness runs the user's real methods, so it has side effects (it may write into the io's buffers at positions 1:items); call it on a representative or dummy io. Zeroed synthetic data does not exercise data-dependent branches.\n\nThe dry run runs the user methods inside with_bounds_checks, so indexing written with @infer_inbounds is bounds-checked here even though it elides in normal use. Bare @inbounds is not affected by that context; to catch out-of-bounds in bare-@inbounds code, start Julia with --check-bounds=yes (which Pkg.test does).\n\n\n\n\n\n","title":"ReactantServerClient.validate_io"},{"category":"method","location":"api/#ReactantServerClient.with_bounds_checks-Tuple{Any}","page":"API","text":"with_bounds_checks(f)\n\nRun f with forced bounds checking enabled for any @infer_inbounds blocks reached during the call (task-local, restored on exit). validate_io uses this so the dry run honors bounds checks even where the IO would normally elide them.\n\n\n\n\n\n","title":"ReactantServerClient.with_bounds_checks"},{"category":"method","location":"api/#ReactantServerCore.scratch-Union{Tuple{T}, Tuple{PoolSlot, AbstractString, Any, Type{T}}} where T","page":"API","text":"scratch(slot, name, dims, T) -> PoolInferInput\nscratch(slot, [\"name1\" => (dims1, T1), \"name2\" => (dims2, T2), ...]) -> Vector{PoolInferInput}\n\nCarve one input buffer per named spec from the chunk's slot, advancing its cursor so the buffers occupy disjoint, contiguous byte ranges, and return the wire descriptors ready to hand back from infer_encode_chunk!. dims is the Julia column-major shape (per-item dims then the batch axis), or a bare integer for a vector. Get the writable views with pool_view: one descriptor returns one view, and splatting the vector returns all of them to destructure at once.\n\nThe scalar form returns one PoolInferInput; the vector form returns a Vector{PoolInferInput}. infer_encode_chunk! accepts either as its return value, so a single-input IO can return the scalar form directly without wrapping it in a vector.\n\nfunction infer_encode_chunk!(io, r, slot)\n    n = length(r)\n    inputs = scratch(slot, [\"INPUT__0\" => ((4, n), Float32), \"MASK\" => ((n,), Int32)])\n    feats, mask = pool_view(inputs...)\n    for (j, i) in enumerate(r); feats[:, j] .= io.feats[i]; mask[j] = io.labels[i]; end\n    return inputs\nend\n\nThe returned vector is homogeneous (Vector{PoolInferInput}), so it never triggers element promotion the way a literal of differently-typed arrays would. item_input_bytes(io) must equal the per-item sum over these buffers, since the driver sizes the slot from it before infer_encode_chunk! runs. The lower-level path (carve a subslot, pool_view it, build InferInput by hand) is still supported and may be mixed into the returned vector.\n\n\n\n\n\n","title":"ReactantServerCore.scratch"},{"category":"macro","location":"api/#ReactantServerClient.@infer_inbounds-Tuple{Any}","page":"API","text":"@infer_inbounds expr\n\nLike @inbounds, but the elision is conditional: outside a with_bounds_checks context the wrapped expr runs with bounds checks elided (as @inbounds); inside one it runs with bounds checks. Use it instead of @inbounds in infer_encode_chunk! / infer_decode_chunk! so a validate_io dry run still catches out-of-bounds indexing. Wrap a whole loop or block so the runtime branch is taken once, not per element.\n\nfunction ReactantServerClient.infer_decode_chunk!(io::MyIO, r, response)\n    out = InferOutput(\"OUTPUT__0\", response, Float32)\n    @infer_inbounds for (j, i) in enumerate(r)\n        io.results[i] = collect(out[:, j])\n    end\n    return nothing\nend\n\n\n\n\n\n","title":"ReactantServerClient.@infer_inbounds"},{"category":"module","location":"api/#ReactantServerExport.ReactantServerExport","page":"API","text":"ReactantServerExport\n\nOffline tooling that assembles ReactantServer model bundles (manifest.yaml, model[.b{N}].mlir, weights.safetensors) from StableHLO modules and weights, and the Reactant tracing frontend that turns a model + parameters into a bundle. This package is the bundle-format authority for producers; it is not depended on by the server runtime. The format is kept in sync with the server by the round-trip tests.\n\nwrite_bundle/IOSpec are the low-level writer. export_bundle traces a Reactant model (the former LuxExport; it needs only Reactant, not Lux). PyTorch support (export_bundle(:pytorch, ...) and export_bundle(:torchscript, ...)) lives in a package extension that loads when PythonCall is present — using ReactantServerExport, PythonCall enables it.\n\n\n\n\n\n","title":"ReactantServerExport.ReactantServerExport"},{"category":"type","location":"api/#ReactantServerExport.IOSpec","page":"API","text":"IOSpec(name, dtype, shape; batch_axis=nothing, letters=nothing)\n\nA tensor's client-facing spec. shape is the network (row-major) shape with a concrete value at the batch axis; batch_axis is the 0-based network axis that carries the batch dimension, or nothing. The manifest serialization encodes the shape as an einsum-style letter string (\"chwn\") plus a dims map of letter→size; the batch axis is emitted as n. Non-batch letters are auto-allocated from _AXIS_LETTERS and carry no semantic meaning across tensors.\n\nletters optionally overrides that auto-allocation with explicit non-batch axis letters, one per non-batch axis in shape order (the batch axis is still emitted as n). Pass it to give axes meaningful names, e.g. ['w','h'] for an image input so its manifest reads whn. The reserved markers n/b are rejected.\n\n\n\n\n\n","title":"ReactantServerExport.IOSpec"},{"category":"method","location":"api/#ReactantServerExport.assert_bundle_arity-Tuple{AbstractString}","page":"API","text":"assert_bundle_arity(dir) -> NamedTuple\n\nbundle_arity_report as a refusal: raise unless every compiled module in dir takes exactly declared inputs + serialized weights arguments. Returns the report when it passes.\n\n\n\n\n\n","title":"ReactantServerExport.assert_bundle_arity"},{"category":"method","location":"api/#ReactantServerExport.bundle_arity_report-Tuple{AbstractString}","page":"API","text":"bundle_arity_report(dir) -> NamedTuple\n\nRead a written bundle's three numbers and say whether they agree: the inputs its manifest.yaml declares, the tensors its weights.safetensors holds, and the arity of each compiled module.\n\nReturns (; name, n_inputs, n_weights, expected, modules, servable), where modules is one (; module_file, entry_args, servable) per *.mlir. Nothing is thrown for a mismatch, so this can be run across a directory of bundles to triage them; assert_bundle_arity is the same check as a refusal.\n\n\n\n\n\n","title":"ReactantServerExport.bundle_arity_report"},{"category":"method","location":"api/#ReactantServerExport.bundle_entry_arity-Tuple{AbstractString}","page":"API","text":"bundle_entry_arity(mlir_path) -> Int\n\nThe number of arguments the compiled program in mlir_path takes, read out of the StableHLO portable artifact.\n\nmodel*.mlir is a serialized vhlo artifact, so parse(MLIR.IR.Module, ...) fails with \"dialect 'vhlo' does not implement the bytecode interface\": it has to be deserialized against a Reactant context first. That is the whole reason this is a function rather than a regex at a call site.\n\n\n\n\n\n","title":"ReactantServerExport.bundle_entry_arity"},{"category":"function","location":"api/#ReactantServerExport.collect_provenance","page":"API","text":"collect_provenance(repo_dir=pwd(); extra=Dict()) -> Dict{String,Any}\n\nBest-effort reproducibility provenance for a bundle. Captures the state of the model repo at repo_dir (repo_remote, git_commit, git_tree_sha1, git_branch, git_dirty) plus the export environment (julia_version, reactantserverexport_version) and an exported_at UTC timestamp. Merge the result into export_bundle's provenance; the frontends separately stamp their framework versions (reactant_version, or torch_version/torchax_version).\n\nWhen the work tree is dirty, the uncommitted changes are captured as git_diff (git diff --binary HEAD), so git_commit plus the patch reconstructs the exact exported code (untracked files are not captured). write_bundle extracts git_diff into a working_tree.patch file in the bundle dir rather than embedding it in the manifest.\n\nEvery git field is best-effort: when git is missing, repo_dir is not inside a work tree, or a repo has no origin remote, the affected fields are omitted (with a warning for the non-repo case) and the function never throws. extra entries are merged last and override collected fields.\n\n\n\n\n\n","title":"ReactantServerExport.collect_provenance"},{"category":"method","location":"api/#ReactantServerExport.export_bundle-Tuple{Symbol, Vararg{Any}}","page":"API","text":"export_bundle(frontend::Symbol, args...; kwargs...) -> dir\n\nExport a model to a bundle, naming the frontend explicitly so the call site is unambiguous:\n\nexport_bundle(:lux, model, ps, st, example_input; ...) — a Lux-style model with separate parameters/state (any Reactant-traceable model(x, ps, st); Lux itself is not required).\nexport_bundle(:reactant, f, inputs::Tuple, weights; ...) — any Reactant-traceable function f(inputs..., weights...) with explicit name => array weight pairs.\nexport_bundle(:pytorch, model, example_inputs::Tuple; ...) — a torch.nn.Module; provided by the package extension, so it requires using PythonCall.\n\n\n\n\n\n","title":"ReactantServerExport.export_bundle"},{"category":"method","location":"api/#ReactantServerExport.export_bundle-Tuple{Val{:lux}, Any, Any, Any, AbstractArray}","page":"API","text":"export_bundle(:lux, model, ps, st, example_input; dir, name, input_name=\"input\",\n              output_name=\"output\", batch_sizes=[1], provenance=Dict()) -> dir\n\nTrace model(x, ps, st) (taking the first return as the output) at each batch size and write a bundle. The batch dimension is the last Julia axis (Lux convention) and the leading network axis. Works for any Reactant-traceable model; Lux itself is not required.\n\n\n\n\n\n","title":"ReactantServerExport.export_bundle"},{"category":"method","location":"api/#ReactantServerExport.export_bundle-Tuple{Val{:lux}, Any, Any, Any, Tuple}","page":"API","text":"export_bundle(:lux, model, ps, st, example_inputs::Tuple; dir, name,\n              input_names=nothing, output_names=nothing,\n              output_select = y -> (y isa Tuple ? y : (y,)),\n              input_batch_axes=nothing, output_batch_axes=nothing,\n              client_inputs=nothing, client_outputs=nothing,\n              batch_sizes=[1], provenance=Dict()) -> dir\n\nMulti-input / multi-output Lux export. client_inputs/client_outputs (each nothing or a Vector{IOSpec}) declare the wire-facing spec when a shipped model.jl postprocess transforms the executable tensors into different client tensors; they are passed through to write_bundle. Traces model(x_tuple, ps, st) where x_tuple is the tuple of array inputs the model's forward expects as its single positional argument. output_select(first(model(...))) maps the raw model output to the ordered tuple of arrays to export; non-array returns (e.g. an Int step count) are dropped by the selector and must not appear in its result. Weights are extracted from ps automatically (same as the single-array method). Per-tensor batch axes default to each array's last Julia axis (natural Julia batch-last, the Lux convention) so every exported input and output is batch-last without the caller hand-specifying it. Override with input_batch_axes/output_batch_axes (1-based Julia axes) when the batch axis is not last (e.g. a (D, N, K+1) output whose batch axis N is the middle one). An entry of nothing in either vector opts that one tensor out of batching (a genuinely unbatched tensor), so single-dispatch or partially-unbatched models can still be exported; note the server requires all executable inputs to be batched or none, so a per-tensor opt-out that leaves a mix of batched and unbatched inputs is rejected at load time.\n\n\n\n\n\n","title":"ReactantServerExport.export_bundle"},{"category":"method","location":"api/#ReactantServerExport.export_bundle-Tuple{Val{:reactant}, Any, Tuple, AbstractVector{<:Pair}}","page":"API","text":"export_bundle(:reactant, f, inputs::Tuple, weights; dir, name, input_names=nothing,\n              output_name=\"output\", provenance=Dict()) -> dir\n\nGeneric single-size export for any Reactant-traceable f(inputs..., weights...). weights is an ordered collection of name => array pairs. Produces one unbatched model.mlir.\n\n\n\n\n\n","title":"ReactantServerExport.export_bundle"},{"category":"method","location":"api/#ReactantServerExport.write_bundle-Tuple{AbstractString}","page":"API","text":"write_bundle(dir; name, executable_inputs, executable_outputs, modules, weights,\n             client_inputs=nothing, client_outputs=nothing, input_shapes=nothing,\n             provenance=Dict()) -> dir\n\nWrite a bundle. With input_shapes === nothing, modules is a Dict keyed by batch size of StableHLO modules (or text or bytes); a single entry under key 0 writes model.mlir, otherwise each writes model.b{N}.mlir. weights is an ordered collection of name => array pairs whose order becomes the safetensors argument_order. The per-input batch axis is recorded in each input's IOSpec.batch_axis; the manifest derives batching.batch_dim from there.\n\ninput_shapes (a Vector{Vector{Int}} of compiled input-shape variants) turns on the multi-shape layout: the variable executable-input axes are marked -1 in executable_inputs, and each variant gives the concrete sizes of those axes in (input, axis) order. modules is then keyed by variant (each key a Vector{Int} equal to one input_shapes entry) and maps to that variant's batch-size module dict; the files are written as model.v{i}.*.mlir (i indexing input_shapes), all sharing the single weights.safetensors. The variants must share one set of batch sizes.\n\nclient_inputs/client_outputs (each nothing or a Vector{IOSpec}) declare the wire-facing spec when it differs from the executable spec, for bundles that ship a model.jl whose preprocess/postprocess transform between the two. They are emitted only when given. A variable (non-batch) axis is encoded by passing -1 for that axis size (e.g. the variable detection count of a postprocessed detector). The server requires these only when a model.jl is present, so the caller is responsible for also shipping model.jl into the bundle dir (see the converter handlers).\n\n\n\n\n\n","title":"ReactantServerExport.write_bundle"},{"category":"function","location":"api/#ReactantServerGateway.probe_worker_ready","page":"API","text":"probe_worker_ready(node_path, worker=nothing) -> Bool\n\nResolve a worker's port from the node file and call its KServe ServerReady on localhost, returning whether it reported ready. Used as the worker container's healthcheck (a Julia replacement for the former Go reactant-healthprobe). worker may be omitted when the node has a single worker.\n\n\n\n\n\n","title":"ReactantServerGateway.probe_worker_ready"},{"category":"function","location":"api/#ReactantServerGateway.serve_gateway","page":"API","text":"serve_gateway(gateway_path=nothing; blocking=true) -> nothing | RunningGateway\n\nLoad gateway.yml (listen addresses and the worker endpoint list), build the worker client pool, start the admin HTTP server and the readiness/discovery prober (which probes each endpoint's ServerReady and RepositoryIndex and swaps in the discovered routing table), and serve the KServe gRPC proxy. When blocking is false the server runs in the background and a RunningGateway is returned (stop it with stop!).\n\ngateway_path may be omitted (or nothing) to configure the gateway from defaults and REACTANT_GATEWAY_* environment variables alone; the endpoint list then comes from REACTANT_GATEWAY_WORKERS. The node supervisor uses this to run an embedded gateway without a gateway.yml.\n\n\n\n\n\n","title":"ReactantServerGateway.serve_gateway"},{"category":"method","location":"api/#ReactantServerNode.supervise-Tuple{AbstractString}","page":"API","text":"supervise(node_path; role=nothing, gateway_path=nothing, sink=stdout, env=ENV,\n          install_signal_handlers=true, kwargs...) -> Int\n\nRun the node: spawn one worker subprocess per visible GPU (and the embedded gateway in the default all-in-one role), multiplex their output onto sink with [name] line prefixes, restart children that die, and block until SIGTERM/SIGINT (or a crash-loop budget breach). Returns the process exit code.\n\n\n\n\n\n","title":"ReactantServerNode.supervise"},{"category":"constant","location":"api/#ReactantServerCore.TIMEOUT_NS_PARAM","page":"API","text":"TIMEOUT_NS_PARAM\n\nKey of the request-level KV parameter carrying the caller's REMAINING budget in nanoseconds (relative, not an absolute timestamp). Like the shared-memory region parameters, this is an extension to KServe V2 passed through ModelInferRequest.parameters. It is relative so each hop converts it to its own local absolute deadline (time_ns() + budget), which makes it robust to cross-process monotonic-clock differences and lets it ride unchanged through the gateway's raw-byte request forwarding. See deadline_params.\n\n\n\n\n\n","title":"ReactantServerCore.TIMEOUT_NS_PARAM"},{"category":"type","location":"api/#ReactantServerCore.BatchingSpec","page":"API","text":"BatchingSpec\n\nThe set of batch sizes a bundle was compiled for (compiled_batch_sizes). At inference the request's size along the batch axis must equal one of these; the scheduler coalesces requests up to a compiled size and selects the matching executable.\n\n\n\n\n\n","title":"ReactantServerCore.BatchingSpec"},{"category":"method","location":"api/#ReactantServerCore.BufferPool-Tuple{Integer}","page":"API","text":"BufferPool(n_bytes; n_slots=8, use_shm=true, name=\"reactant_server_pool\")\n\nAllocate a staging pool of n_bytes divided into n_slots equal slots. slot_bytes is fixed at construction (n_bytes ÷ n_slots), not recomputed per request, so the allocator can hand disjoint slots to concurrent callers. A SHM-backed pool can be registered with a server; an inline pool (use_shm=false) is the fallback transport.\n\n\n\n\n\n","title":"ReactantServerCore.BufferPool"},{"category":"type","location":"api/#ReactantServerCore.DType","page":"API","text":"DType\n\nCanonical element-type enumeration shared across the server, the single source of truth for dtype translation. The companion maps convert between three representations: the manifest token form (e.g. \"f32\", \"bf16\"), the Julia element type (e.g. Float32, BFloat16), and the KServe V2 wire datatype string (e.g. \"FP32\", \"BF16\").\n\nThe DType to XLA primitive-type mapping deliberately lives in the Reactant backend, not here, so this layer carries no Reactant dependency.\n\nFP8 (F8E5M2, F8E4M3) has no standard KServe wire datatype, so those two variants are intentionally absent from the wire mapping and may appear only on executable-internal tensors, never on client-facing inputs or outputs. Conversions are performed by dtype_from_token, dtype_token, julia_type, dtype_of, dtype_size, kserve_string, and dtype_from_kserve.\n\n\n\n\n\n","title":"ReactantServerCore.DType"},{"category":"type","location":"api/#ReactantServerCore.DeadlineExceeded","page":"API","text":"DeadlineExceeded(model_name)\n\nRaised when a request's deadline has already passed at dispatch admission: the scheduler refuses to begin GPU work that is already expired, and a meta orchestration refuses to issue a further sub-call once its budget is gone. It never interrupts a running PJRT/GPU call; it only declines to start new work. The gRPC layer maps it to DEADLINE_EXCEEDED.\n\n\n\n\n\n","title":"ReactantServerCore.DeadlineExceeded"},{"category":"type","location":"api/#ReactantServerCore.Dim","page":"API","text":"Dim\n\nA single axis of a tensor shape. kind is one of FIXED, BATCH, or VARIABLE; size is meaningful only when kind == FIXED (it is 0 otherwise). A FIXED dim has a concrete size, a BATCH dim is the batch axis (from the reserved n/b shape letters), and a VARIABLE dim (a -1 in the manifest dims map) is a dynamic non-batch axis.\n\n\n\n\n\n","title":"ReactantServerCore.Dim"},{"category":"type","location":"api/#ReactantServerCore.EndpointsConfig","page":"API","text":"EndpointsConfig\n\nThe listen addresses (the endpoints: config block): host, the gRPC port, the optional metrics_port for the Prometheus exposition endpoint (0 = disabled), and max_concurrent_requests, the cap on simultaneously-handled RPCs (0 = uncapped). For a worker fronted by the gateway, bind host to all interfaces (0.0.0.0) so the gateway and Prometheus can reach it; the gRPC port is usually derived from the node file's base_port and the metrics port from metrics_base_port.\n\nmax_concurrent_requests is a worker-level overload backstop: past the cap, new requests are shed immediately with RESOURCE_EXHAUSTED rather than queued. Keep it strictly above the gateway's per-worker outbound stream limit so it never sheds traffic the gateway has already admitted (and so it never rejects a meta-model's loopback sub-call); in single-worker mode (no gateway, clients hit the worker directly) it is the only inbound admission control.\n\n\n\n\n\n","title":"ReactantServerCore.EndpointsConfig"},{"category":"type","location":"api/#ReactantServerCore.GrpcConfig","page":"API","text":"GrpcConfig\n\ngRPC transport limits for a worker's server. max_recv_msg_bytes / max_send_msg_bytes bound a single gRPC message in each direction (decode/encode caps, not allocations). Configured under the grpc: block of a worker (or node global:) config, with INFERENCE_SERVER_GRPC_MAX_RECV_MSG_BYTES / INFERENCE_SERVER_GRPC_MAX_SEND_MSG_BYTES environment overrides. Default DEFAULT_GRPC_MSG_BYTES (512 MiB).\n\n\n\n\n\n","title":"ReactantServerCore.GrpcConfig"},{"category":"type","location":"api/#ReactantServerCore.InferRequest","page":"API","text":"InferRequest\n\nA decoded inference request, the scheduler's unit of work. It names the target model (model_name), the requested_outputs the caller wants returned, and the input tensors (inputs, a Vector{NamedTensor}). deadline_ns is an absolute local time_ns() deadline (0 means none): a remaining-budget timeout carried over the wire is converted to this local absolute form at decode, so cross-process monotonic-clock differences never matter. The codec produces it from a wire ModelInferRequest; the scheduler and runtime consume only this transport-agnostic form.\n\n\n\n\n\n","title":"ReactantServerCore.InferRequest"},{"category":"type","location":"api/#ReactantServerCore.Manifest","page":"API","text":"Manifest\n\nThe parsed and validated manifest.yaml of a model bundle. It records the format_version, the bundle name and description, the executable input/output specs (executable_inputs/executable_outputs), the optional client-facing specs (client_inputs/client_outputs, present only when a model.jl transforms the I/O), the BatchingSpec, provenance metadata, and the derived 0-based input_batch_dim. Tensor specs are TensorSpec values; see TensorSpec and Dim for the einsum-style shape encoding.\n\n\n\n\n\n","title":"ReactantServerCore.Manifest"},{"category":"type","location":"api/#ReactantServerCore.ModelControlMode","page":"API","text":"ModelControlMode\n\nHow a worker manages the set of loaded models over its lifetime (mirrors NVIDIA Triton's model-control-mode). STATIC loads and compiles every bundle once at startup and never changes the set. DYNAMIC (the default) additionally runs a filesystem watcher that polls the model repository every model_poll_seconds and hot-swaps bundles as they are added, changed, or removed on disk. EXPLICIT cedes authority to an upstream control plane (the externally-managed residency behavior): no autonomous watcher, and non-resident models are not served until the control plane pins them.\n\n\n\n\n\n","title":"ReactantServerCore.ModelControlMode"},{"category":"type","location":"api/#ReactantServerCore.ModelSchedConfig","page":"API","text":"ModelSchedConfig\n\nPer-model scheduler overrides (an entry under scheduler.models). weight is the model's relative compute share (default 1.0, so all-default weights yield uniform shares; consulted only by the fair discipline). residency is the model's initial residency floor (see ResidencyState); nothing means unspecified, which the server resolves at startup to PINNED_SYSTEM when the on-demand weight cache is enabled (so every model's weights are materialized into host RAM and an on-demand GPU load is a pure host-to-device transfer) and UNPINNED otherwise. max_batch_size caps how many rows the scheduler coalesces into one dispatch of this model; nothing means uncapped. The cap does not change compiled shapes: a partial fill still pads up to the smallest compiled batch size, and a single request larger than the cap is still served (requests are never split).\n\n\n\n\n\n","title":"ReactantServerCore.ModelSchedConfig"},{"category":"type","location":"api/#ReactantServerCore.NamedTensor","page":"API","text":"NamedTensor(name, dtype, shape, data)\nNamedTensor(name, data)\n\nA named host tensor carried across the transport boundary as both an input and an output. It pairs a tensor name with its DType, its shape (Julia column-major dimensions), and the backing data array. The two-argument form derives dtype and shape from a typed host Array.\n\n\n\n\n\n","title":"ReactantServerCore.NamedTensor"},{"category":"type","location":"api/#ReactantServerCore.NumericsMode","page":"API","text":"NumericsMode\n\nHow f32 matmul/convolution precision is resolved at model compile time (the runtime.numerics knob). NUMERICS_AUTO (the default) is hardware-adaptive: TF32 is used where the GPU supports it (compute capability >= 8.0) and explicit TF32 algorithms are stripped where it does not, so the same bundle compiles everywhere but its numerics follow the hardware. NUMERICS_F32 pins full f32 everywhere: TF32 DotAlgorithms are rewritten to f32 and every algorithm-free f32 dot_general/convolution gets precision_config = HIGHEST, so numerics are identical across GPU generations (the mode for validated deployments; costs tensor-core throughput on TF32-capable GPUs). NUMERICS_TF32 compiles exactly like auto (TF32 permitted; kernel choice stays with XLA/cuBLAS, and StableHLO cannot force TF32 for convolutions at all) but makes the hardware requirement a guarantee: the worker fails startup on hardware that cannot run TF32, rather than silently degrading per worker in a mixed fleet.\n\n\n\n\n\n","title":"ReactantServerCore.NumericsMode"},{"category":"type","location":"api/#ReactantServerCore.PoolAcquireTimeout","page":"API","text":"PoolAcquireTimeout(span, waited_ns)\n\nRaised by acquire_slot! when a deadline_ns was supplied and passed before span contiguous slots became free. The waiter is dequeued before this is thrown, so it never stalls the line. Callers that carry a request deadline (e.g. a meta model's fan-out) translate this into their own deadline-exceeded error.\n\n\n\n\n\n","title":"ReactantServerCore.PoolAcquireTimeout"},{"category":"type","location":"api/#ReactantServerCore.PrivateWeightStore","page":"API","text":"PrivateWeightStore()\n\nThe default store: each worker materializes its own host copy of a model's weights.\n\n\n\n\n\n","title":"ReactantServerCore.PrivateWeightStore"},{"category":"type","location":"api/#ReactantServerCore.ResidencyMode","page":"API","text":"ResidencyMode\n\nWho owns device residency on a worker, fixed at startup. SELF_MANAGED lets the worker autonomously transfer and evict weights above each model's floor within the device budget; EXTERNALLY_MANAGED makes a control plane authoritative (no autonomous eviction, non-resident models are not served until pinned). This is no longer configured directly: it is derived from ModelControlMode (explicit ⇒ EXTERNALLY_MANAGED, otherwise SELF_MANAGED).\n\n\n\n\n\n","title":"ReactantServerCore.ResidencyMode"},{"category":"type","location":"api/#ReactantServerCore.ResidencyState","page":"API","text":"ResidencyState\n\nThe residency floor an operator (self-managed) or control plane (externally-managed) sets for a model's weights. UNPINNED keeps no guaranteed residency (loaded from the mmap on demand); PINNED_SYSTEM guarantees the weights stay resident in host RAM (and must be transferred to the device before execution); PINNED_DEVICE guarantees them resident on the GPU for the server's lifetime (exempt from eviction).\n\n\n\n\n\n","title":"ReactantServerCore.ResidencyState"},{"category":"type","location":"api/#ReactantServerCore.RuntimeConfig","page":"API","text":"RuntimeConfig\n\nRuntime and device settings (the runtime: config block). backend selects CPU or CUDA execution; device_ordinal picks the GPU among several visible ones; mem_fraction is the fraction of device memory claimed for the pool; preallocate claims that pool up front; allow_cpu_fallback permits falling back to CPU when the device is unavailable; weight_cache_fraction is the single knob sizing on-demand (unpinned) weight residency, the fraction of the BFC arena (mem_fraction * device memory) devoted to all weights, pinned plus on-demand (1.0, the default, uses the whole arena minus measured scratch and wiggle; 0 disables the cache so every model's weights stay resident; resolved to a byte budget at startup, GPU only); weight_cache_wiggle_fraction is the fraction of the arena kept free as anti-fragmentation slack and feeds the startup auto-sizing (the worker probes each model's peak device usage and sizes the cache so pinned weights + on-demand + scratch + this slack fit); residency_mode selects self-managed or externally-managed residency; shared_host_weights opts the node into the shared-memory host-weight store so same-node workers share one host copy of each system-pinned model; and shared_host_weights_mode sets the permission bits (an octal string, default \"666\") for those shared regions and their lock files. The \"666\" default keeps cross-UID container setups working but is world-writable; \"660\" is recommended for production and multi-user systems. autotune (default true) enables XLA's GPU compile autotuner; set it false to compile with xla_gpu_autotune_level=0 (default gemm/conv algorithm selection, no timing trials), which removes autotuning's run-to-run non-determinism and its device scratch that otherwise inflates the startup memory probe (_probe_max_scratch!) on the first, un-cached start. autotune_cache (default nothing, meaning inherit Reactant's LocalPreferences.toml) toggles the persistent per-fusion autotune cache, and autotune_cache_dir (default \"\", inherit) sets its directory; both are applied to Reactant's compile cache at worker startup, so a container can drive them by env. numerics (default auto) sets the f32 matmul/convolution precision policy; see NumericsMode.\n\n\n\n\n\n","title":"ReactantServerCore.RuntimeConfig"},{"category":"type","location":"api/#ReactantServerCore.SchedulerConfig","page":"API","text":"SchedulerConfig\n\nGlobal scheduler settings (the scheduler: config block). ema_halflife_seconds is the half-life of the recent-compute moving average that drives fairness; recency_penalty_cap bounds the recency adjustment; coalescing_discount is the cost discount applied to coalesced batches; cost_ema_alpha is the smoothing factor for the learned per-batch-size cost; max_queue_depth caps each model's queue independently (a full model rejects new requests without affecting admission for the others); dispatch_timeout_seconds is the per-request execution timeout; discipline selects the inter-model ordering (see SchedulingDiscipline); compaction_interval runs worker-local memory compaction every N on-demand weight-cache loads (0 disables), the standalone (no-gateway) trigger, off by default so a gateway-fronted worker never self-compacts; and models holds the per-model ModelSchedConfig overrides.\n\n\n\n\n\n","title":"ReactantServerCore.SchedulerConfig"},{"category":"type","location":"api/#ReactantServerCore.SchedulingDiscipline","page":"API","text":"SchedulingDiscipline\n\nThe inter-model dispatch ordering. FAIR is the deficit-weighted, cost-aware policy with per-model weights and the coalescing discount; FIFO serves in global arrival order; EDF (earliest-deadline-first) serves the model whose most-urgent queued request has the soonest deadline. Coalescing runs underneath all three.\n\nGuidance: FAIR is for deployments where models share this worker with no upstream placement authority, a single-GPU worker or a multi-GPU fleet behind the round-robin gateway, where the worker itself must stop one model from crowding out the rest. Under the gateway's lpt_packing scheduling the gateway is the fairness authority (placement concentration plus the per-worker share cap), and workers must run FIFO or EDF so the two do not fight; lpt_packing supersedes the role FAIR played on manually-assigned multi-GPU fleets.\n\nEDF is for deadline-sensitive deployments where requests carry a remaining-budget timeout (see the request-level timeout parameter). It degrades to FIFO whenever queued requests share the same deadline, so its only divergence from FIFO is to promote requests with less budget left, in practice the in-flight meta-model sub-calls that have already spent part of their budget on an earlier stage. It also sheds work that cannot finish within its learned compute cost (laxity), so it trades some throughput (batch fragmentation, and no per-model fairness) for hitting more deadlines under load. NOTE: EDF derives urgency purely from the deadline, so issuing different per-client deadlines for the same model reorders that model's service and therefore affects fairness across clients; uniform deadlines keep it behaving like FIFO.\n\n\n\n\n\n","title":"ReactantServerCore.SchedulingDiscipline"},{"category":"type","location":"api/#ReactantServerCore.ServerConfig","page":"API","text":"ServerConfig\n\nThe fully resolved configuration for a single worker process, frozen for the process lifetime. It is produced from a node file (see node.jl) with environment-variable overrides applied, then checked by validate_config. Fields: model_dirs (bundle search paths), cache_dir, the RuntimeConfig, SchedulerConfig, and EndpointsConfig sub-configs, models_include (an allowlist of model names to load; empty means load all), model_poll_seconds (the dynamic-mode interval at which the worker re-scans its model_dirs for added, changed, or removed bundles and hot-swaps them), model_control_mode (see ModelControlMode: static, dynamic, or explicit), and the GrpcConfig grpc sub-config (gRPC message-size limits). ReactantServer.serve also accepts a ServerConfig directly.\n\n\n\n\n\n","title":"ReactantServerCore.ServerConfig"},{"category":"type","location":"api/#ReactantServerCore.SharedWeightStore","page":"API","text":"SharedWeightStore(; mode=0o666)\n\nOpt-in store backing each model's host weights with a node-level POSIX shared-memory region so same-node workers share one copy. See the file header for the flock-coordinated protocol. mode sets the permission bits for the regions and their lock files. The default 0o666 lets workers running as unrelated UIDs (for example different containers) share the regions, but is world-writable; 0o660 is recommended for production and multi-user systems.\n\n\n\n\n\n","title":"ReactantServerCore.SharedWeightStore"},{"category":"type","location":"api/#ReactantServerCore.TensorSpec","page":"API","text":"TensorSpec\n\nOne tensor in a Manifest: its name, DType, shape (a Vector{Dim}), and batch_axis (the 1-based index of the batch dim, or nothing when the tensor is not batched).\n\n\n\n\n\n","title":"ReactantServerCore.TensorSpec"},{"category":"type","location":"api/#ReactantServerCore.WeightStore","page":"API","text":"WeightStore\n\nHost-RAM residency backend for a model's materialized weights. PrivateWeightStore (the default) gives each worker its own copy; SharedWeightStore shares one copy per model across same-node workers through a POSIX shared-memory region. See the file header for the coordination protocol.\n\n\n\n\n\n","title":"ReactantServerCore.WeightStore"},{"category":"function","location":"api/#ReactantServerCore.acquire_slot!","page":"API","text":"acquire_slot!(pool, span=1; deadline_ns=0) -> PoolSlot\n\nBlock until span physically contiguous slots are free, then return them as one slot whose [offset, offset + span*slot_bytes) range no other in-flight slot overlaps. Throws an ArgumentError immediately if span exceeds the pool's total slot count, since such a request could never be satisfied and would otherwise deadlock. Waiters are served in FIFO order. Pair with release_slot!, which frees the whole run.\n\ndeadline_ns is an absolute time_ns() deadline (0 = wait indefinitely, the default and prior behavior). When set, a waiter that has not acquired by the deadline throws [PoolAcquireTimeout] instead of parking past it. A one-shot timer wakes the waiter at the deadline even if no slot is released in the meantime, so a starved waiter fails fast rather than burning a request's whole budget in the park.\n\n\n\n\n\n","title":"ReactantServerCore.acquire_slot!"},{"category":"method","location":"api/#ReactantServerCore.deadline_params-Tuple{Integer}","page":"API","text":"deadline_params(budget_ns) -> Dict{String,InferParameter}\ndeadline_params(PB::Module, budget_ns) -> Dict{String,InferParameter}\n\nBuild the request-level parameters map carrying a remaining-budget timeout of budget_ns nanoseconds (see TIMEOUT_NS_PARAM). A non-positive budget_ns yields an empty map (no deadline). Merge the result into a ModelInferRequest's parameters. The PB-first form builds the map from pb module PB (consumer packages pass their own generated module; the single-argument form uses this package's inference module).\n\n\n\n\n\n","title":"ReactantServerCore.deadline_params"},{"category":"function","location":"api/#ReactantServerCore.decode_infer_request","page":"API","text":"decode_infer_request(msg, registry=nothing) -> DecodedRequest\ndecode_infer_request(PB::Module, msg, registry=nothing) -> DecodedRequest\n\nTranslate a decoded ModelInferRequest message into the boundary InferRequest. The transport (gRPC) hands us the already-decoded protobuf message, so the codec never touches wire bytes. Input tensor data is read from a registered shared-memory region (preferred when the tensor declares one), otherwise from rawinputcontents, otherwise from the typed contents field. The PB-first form accepts a message from pb module PB (consumer packages pass their own generated module; the shorter form uses this package's inference module).\n\n\n\n\n\n","title":"ReactantServerCore.decode_infer_request"},{"category":"method","location":"api/#ReactantServerCore.decode_infer_response-Tuple{Any}","page":"API","text":"decode_infer_response(msg) -> Vector{NamedTensor}\ndecode_infer_response(PB::Module, msg) -> Vector{NamedTensor}\n\nTranslate a ModelInferResponse into boundary NamedTensor outputs. Data is read from rawoutputcontents when present, otherwise from the typed contents field. Shared-memory-backed outputs are not supported on this path (the caller never requests them). The PB-first form accepts a message from pb module PB (consumer packages pass their own generated module).\n\n\n\n\n\n","title":"ReactantServerCore.decode_infer_response"},{"category":"method","location":"api/#ReactantServerCore.encode_infer_request-Tuple{AbstractString, Vector{NamedTensor}}","page":"API","text":"encode_infer_request(model_name, inputs; requested_outputs=String[], id=\"\") -> ModelInferRequest\nencode_infer_request(PB::Module, model_name, inputs; requested_outputs=String[], id=\"\") -> ModelInferRequest\n\nBuild a ModelInferRequest from boundary NamedTensor inputs, with tensor data inline in rawinputcontents. requested_outputs, when non-empty, names the outputs to return. The PB-first form builds the message from pb module PB (consumer packages pass their own generated module).\n\n\n\n\n\n","title":"ReactantServerCore.encode_infer_request"},{"category":"method","location":"api/#ReactantServerCore.encode_infer_request_shm-Tuple{AbstractString, Vector{NamedTensor}, AbstractString, Vector{<:Integer}}","page":"API","text":"encode_infer_request_shm(model_name, inputs, region, offsets; requested_outputs, id)\nencode_infer_request_shm(PB::Module, model_name, inputs, region, offsets; requested_outputs, id)\n\nEncode a request whose inputs are ALL staged in shared-memory region at the given byte offsets (parallel to inputs); no raw_input_contents (the receiver reads each tensor via shm_read). The receiver must have region registered. This is all-or-nothing per request: the decode path treats raw_input_contents as parallel-to-inputs, so a request never mixes raw and SHM inputs. The PB-first form builds the message from pb module PB.\n\n\n\n\n\n","title":"ReactantServerCore.encode_infer_request_shm"},{"category":"method","location":"api/#ReactantServerCore.encode_infer_response-Tuple{AbstractString, AbstractString, Vector{NamedTensor}}","page":"API","text":"encode_infer_response(model_name, id, outputs) -> ModelInferResponse\nencode_infer_response(PB::Module, model_name, id, outputs) -> ModelInferResponse\n\nBuild the response message with outputs entirely inline (rawoutputcontents). The transport serializes the returned message. The PB-first form builds the message from pb module PB (consumer packages pass their own generated module; the shorter form uses this package's inference module).\n\n\n\n\n\n","title":"ReactantServerCore.encode_infer_response"},{"category":"method","location":"api/#ReactantServerCore.encode_infer_response-Tuple{AbstractString, DecodedRequest, Vector{NamedTensor}, Union{Nothing, SharedMemoryRegistry}}","page":"API","text":"encode_infer_response(model_name, decoded, outputs, registry) -> ModelInferResponse\nencode_infer_response(PB::Module, model_name, decoded, outputs, registry) -> ModelInferResponse\n\nBuild the response message, writing any output whose requested entry named a shared-memory region into that region (and referencing it in the response) instead of inline. rawoutputcontents holds the inline outputs in order. The PB-first form builds the message from pb module PB (consumer packages pass their own generated module).\n\n\n\n\n\n","title":"ReactantServerCore.encode_infer_response"},{"category":"method","location":"api/#ReactantServerCore.encode_model_metadata-Tuple{AbstractString, Manifest, AbstractString}","page":"API","text":"encode_model_metadata(name, manifest, platform) -> ModelMetadataResponse\nencode_model_metadata(PB::Module, name, manifest, platform) -> ModelMetadataResponse\n\nBuild a ModelMetadataResponse message from the manifest's client-facing I/O spec. The PB-first form builds the message from pb module PB (consumer packages pass their own generated module).\n\n\n\n\n\n","title":"ReactantServerCore.encode_model_metadata"},{"category":"method","location":"api/#ReactantServerCore.encode_repository_index-Tuple{AbstractVector{<:AbstractString}}","page":"API","text":"encode_repository_index(names) -> RepositoryIndexResponse\nencode_repository_index(entries::AbstractVector{<:Pair}) -> RepositoryIndexResponse\nencode_repository_index(PB::Module, names) -> RepositoryIndexResponse\nencode_repository_index(PB::Module, entries::AbstractVector{<:Pair}) -> RepositoryIndexResponse\n\nBuild a RepositoryIndexResponse. The first form lists every model as READY (direct-client introspection). The second takes name => ready::Bool pairs and reports READY or UNAVAILABLE per model, so the gateway can discover which replicas actually serve a model (readiness reflects residency on the worker). The PB-first forms build the message from pb module PB.\n\n\n\n\n\n","title":"ReactantServerCore.encode_repository_index"},{"category":"method","location":"api/#ReactantServerCore.is_meta-Tuple{Manifest}","page":"API","text":"is_meta(m::Manifest) -> Bool\n\nTrue when the bundle is a meta model (a Julia orchestration over other models) rather than a regular StableHLO executable.\n\n\n\n\n\n","title":"ReactantServerCore.is_meta"},{"category":"method","location":"api/#ReactantServerCore.load_manifest-Tuple{AbstractString}","page":"API","text":"load_manifest(path) -> Manifest\n\nParse a manifest YAML file at path into a Manifest. This runs the structural parsing and validation of parse_manifest but not the bundle-directory checks in validate_manifest, so it is usable wherever only the manifest's contents are needed, for example a client deriving a model's I/O spec offline.\n\n\n\n\n\n","title":"ReactantServerCore.load_manifest"},{"category":"method","location":"api/#ReactantServerCore.load_node-Tuple{AbstractString}","page":"API","text":"load_node(path) -> Dict{String,Any}\n\nRead and structurally validate a node config file.\n\n\n\n\n\n","title":"ReactantServerCore.load_node"},{"category":"method","location":"api/#ReactantServerCore.load_node_raw-Tuple{AbstractString}","page":"API","text":"load_node_raw(path) -> Dict{String,Any}\n\nRead a node config file without structural validation. Used by the supervisor, which may need to synthesize the workers: list (see materialize_node!) before validating.\n\n\n\n\n\n","title":"ReactantServerCore.load_node_raw"},{"category":"method","location":"api/#ReactantServerCore.materialize_host_weights!-Tuple{PrivateWeightStore, Vararg{Any, 4}}","page":"API","text":"materialize_host_weights!(store, key, digest, specs, fill!) -> Vector{Any}\n\nReturn a model's host weight Arrays (in weight order), populating them via fill!(arrays) when this worker is the one that must materialize them. specs is a vector of (eltype, dims) per tensor. For PrivateWeightStore the arrays are freshly allocated; for SharedWeightStore they alias a node-shared region.\n\n\n\n\n\n","title":"ReactantServerCore.materialize_host_weights!"},{"category":"method","location":"api/#ReactantServerCore.materialize_node!-Tuple{AbstractDict, AbstractVector{<:AbstractString}}","page":"API","text":"materialize_node!(node, devices; cpu_workers=1) -> Vector{Union{String,Nothing}}\n\nPrepare a raw node config for supervised single-container deployment: assign one visible device per worker and return the per-worker device selectors (in worker declaration order; nothing means the worker gets no CUDA_VISIBLE_DEVICES of its own, the CPU case).\n\nWith no workers: list, one is synthesized: worker0..workerN-1, one per device (or cpu_workers workers when devices is empty). An explicit workers: list wins: each worker is assigned devices[i] positionally, or devices[gpu+1] when the entry carries a gpu: key. The gpu: key is consumed here (deleted after assignment) so each child process, seeing a single device through CUDA_VISIBLE_DEVICES, resolves device ordinal 0. Assigning one device to two workers, or having more workers than devices, is a ConfigError. Call validate_node on the result before use.\n\n\n\n\n\n","title":"ReactantServerCore.materialize_node!"},{"category":"method","location":"api/#ReactantServerCore.node_gpus-Tuple{AbstractDict}","page":"API","text":"node_gpus(node) -> :auto | Int | Vector{String}\n\nParse the optional top-level gpus: key: auto (the default when absent) asks the supervisor to enumerate visible devices; an integer is a device count (expanded to ordinals 0..n-1); a list gives explicit device selectors (ordinals or GPU UUIDs, passed to CUDA_VISIBLE_DEVICES verbatim).\n\n\n\n\n\n","title":"ReactantServerCore.node_gpus"},{"category":"method","location":"api/#ReactantServerCore.node_server_config-Tuple{AbstractDict, Union{Nothing, AbstractString}}","page":"API","text":"node_server_config(node, worker) -> (ServerConfig, applied_overrides, worker_name)\n\nResolve the ServerConfig for one worker of a node. worker may be nothing when the node has exactly one worker (it defaults to that sole entry); otherwise it must name a defined worker. Environment overrides (INFERENCE_SERVER_*) are applied on top, as for any server config. Does not validate; call validate_config on the result.\n\n\n\n\n\n","title":"ReactantServerCore.node_server_config"},{"category":"method","location":"api/#ReactantServerCore.release_host_weights!-Tuple{PrivateWeightStore, Any}","page":"API","text":"release_host_weights!(store, key) -> nothing\n\nRelease a model's host weights. For SharedWeightStore this detaches the region and, if this was the last holder on the node (a non-blocking upgrade to an exclusive flock succeeds), unlinks the region and its lock file. A no-op for the private store. The caller must drop all references to the arrays first.\n\n\n\n\n\n","title":"ReactantServerCore.release_host_weights!"},{"category":"method","location":"api/#ReactantServerCore.release_slot!-Tuple{PoolSlot}","page":"API","text":"release_slot!(slot)\n\nReturn a slot acquired with acquire_slot! to the pool's free set, freeing every physical slot in its span. Releasing a derived subslot (index == 0) is a no-op. Throws if any slot in the span is already free (double release).\n\n\n\n\n\n","title":"ReactantServerCore.release_slot!"},{"category":"method","location":"api/#ReactantServerCore.rename_host_weights!-Tuple{PrivateWeightStore, Any, Any}","page":"API","text":"rename_host_weights!(store, old, new) -> nothing\n\nRekey a model's attached host-weight region from old to new (a model rename; the weights are unchanged). The region itself keeps its original content-addressed SHM name; only this worker's bookkeeping key moves, so a later release_host_weights!(store, new) detaches the same region. A no-op for the private store and when nothing is attached under old.\n\n\n\n\n\n","title":"ReactantServerCore.rename_host_weights!"},{"category":"method","location":"api/#ReactantServerCore.same_ipc_namespace-Tuple{AbstractString}","page":"API","text":"same_ipc_namespace(name) -> Bool\n\nReturn whether the POSIX shared-memory object name is visible in this process's IPC namespace. The client passes the name of an object it created; we answer true only if we can open that same object ourselves, which is what determines whether system shared-memory transport can work between the two processes. The open is read-only and detached immediately; nothing is registered or kept mapped. Any failure (object absent because we are in a different namespace, permission error, malformed name) is reported as false.\n\n\n\n\n\n","title":"ReactantServerCore.same_ipc_namespace"},{"category":"method","location":"api/#ReactantServerCore.scratch-Union{Tuple{T}, Tuple{PoolSlot, Any, Type{T}}} where T","page":"API","text":"scratch(slot, dims, T) -> Array{T}\nscratch(slot, [dims1 => T1, dims2 => T2, ...]) -> Vector{Array}\n\nCarve one typed buffer per dims => T spec from slot in a single call, advancing the slot's cursor so the buffers occupy disjoint, contiguous byte ranges. Each buffer is an Array{T} aliasing the pool's backing (via pool_view; zero-copy, and uniform across SHM- and Memory-backed pools since it is just pool_base + offset); write into the returned arrays directly. dims is a shape tuple (or a bare integer for a vector).\n\nThis is the buffer-request interface shared by the meta-model call.scratch and the client driver: ask for ALL buffers up front in one call. The carved buffers' lifetime is bounded by slot (and by the pool, which the caller keeps alive); they become invalid once it is released.\n\n\n\n\n\n","title":"ReactantServerCore.scratch"},{"category":"method","location":"api/#ReactantServerCore.shm_read-Tuple{SharedMemoryRegistry, AbstractString, Integer, Integer}","page":"API","text":"shm_read(registry, name, offset, byte_size) -> Vector{UInt8}\n\nCopy [offset, offset+byte_size) of the named region into a fresh byte vector. The copy is done while the region's lock is held, so a concurrent shm_unregister! of the same region cannot detach the mapping underneath the read.\n\n\n\n\n\n","title":"ReactantServerCore.shm_read"},{"category":"method","location":"api/#ReactantServerCore.shm_register!-Tuple{SharedMemoryRegistry, AbstractString, AbstractString, Integer, Integer}","page":"API","text":"shm_register!(registry, name, key, offset, byte_size)\n\nAttach the existing POSIX shared-memory object key read-write and register it under name. Re-registering a name replaces (and detaches) the previous mapping.\n\n\n\n\n\n","title":"ReactantServerCore.shm_register!"},{"category":"method","location":"api/#ReactantServerCore.shm_unregister!-Tuple{SharedMemoryRegistry, AbstractString}","page":"API","text":"shm_unregister!(registry, name)\n\nUnregister and detach a region, or all regions when name is empty. Idempotent: unregistering a name that is not registered is a successful no-op (it matches KServe semantics and lets the gateway fan-out and the client's pre-emptive cleanup unregister succeed without a spurious error). Registration, by contrast, fails loudly (see shm_register!) so a bad region surfaces at register time rather than during inference.\n\n\n\n\n\n","title":"ReactantServerCore.shm_unregister!"},{"category":"method","location":"api/#ReactantServerCore.shm_write!-Tuple{SharedMemoryRegistry, AbstractString, Integer, Vector{UInt8}}","page":"API","text":"shm_write!(registry, name, offset, bytes)\n\nCopy bytes into [offset, offset+length(bytes)) of the named region. The copy is done while the region's lock is held, so a concurrent shm_unregister! of the same region cannot detach the mapping underneath the write.\n\n\n\n\n\n","title":"ReactantServerCore.shm_write!"},{"category":"method","location":"api/#ReactantServerCore.validate_node-Tuple{AbstractDict}","page":"API","text":"validate_node(node)\n\nStructural validation of a parsed node config. Raises ConfigError on a malformed file: missing model_repo, duplicate worker names, colliding ports, or a models: entry that targets an undefined worker. The models: map is optional for any node: when omitted, every worker loads every bundle in the repo; when present, it is a per-model override that pins the named models to device memory on the listed workers (see worker_raw_config).\n\n\n\n\n\n","title":"ReactantServerCore.validate_node"},{"category":"method","location":"api/#ReactantServerCore.weights_digest-Tuple{AbstractString, Any}","page":"API","text":"weights_digest(key, specs; content=0) -> UInt64\n\nDigest over a model's identity, weight layout (name, per-tensor dtype/size/shape, a format version), and a content token identifying the weight file's on-disk version (see weights_file_token). Two workers computing this for the same model and the same file agree on the same region key. Without the content token a weights-only update (same name, same tensor layout) would collide with the previous version's region and silently serve stale weights, including across server restarts (regions in /dev/shm outlive the process by design).\n\n\n\n\n\n","title":"ReactantServerCore.weights_digest"},{"category":"method","location":"api/#ReactantServerCore.weights_file_token-Tuple{AbstractString}","page":"API","text":"weights_file_token(path) -> UInt64\n\nIdentity token for a weights file's current on-disk version, folded into weights_digest via its content keyword. Derived from the file's size and mtime: cheap (no content read, so the shared store's zero-copy attach path stays zero-read) and identical across same-node workers stat'ing the same file. Returns 0 for an empty or missing path (hand-built test entries), which reproduces the layout-only digest.\n\n\n\n\n\n","title":"ReactantServerCore.weights_file_token"},{"category":"method","location":"api/#ReactantServerCore.wire_batch_spec-Tuple{Manifest}","page":"API","text":"wire_batch_spec(m::Manifest) -> Union{Nothing,Tuple{String,Int}}\n\nThe name and 1-based batch axis of the first WIRE-FACING input that declares one, or nothing when the model declares no batch axis at all (every request is then one item).\n\n\"Wire-facing\" is client_inputs when the bundle has them and executable_inputs otherwise: client_inputs is present exactly when a model.jl intercepts the request (see validate_manifest), and without one the client sends the executable inputs directly.\n\nThis exists so a gateway can count the items in a request it never fully decodes. It cannot infer the axis: the position is per tensor and genuinely varies across bundles (whcn puts it last, nc first), the first input need not carry one at all (a cross-encoder's query is unbatched while its keys are batched), and it has moved between exports of the same model. Picking the first input that declares an axis mirrors _derive_input_batch_dim, and the name lets the reader match it against the request's own name-addressed tensors rather than trusting their order.\n\n\n\n\n\n","title":"ReactantServerCore.wire_batch_spec"},{"category":"method","location":"api/#ReactantServerCore.worker_names-Tuple{AbstractDict}","page":"API","text":"worker_names(node) -> Vector{String}\n\nThe worker names in declaration order.\n\n\n\n\n\n","title":"ReactantServerCore.worker_names"},{"category":"method","location":"api/#ReactantServerCore.worker_raw_config-Tuple{AbstractDict, AbstractString}","page":"API","text":"worker_raw_config(node, name) -> Dict{String,Any}\n\nResolve a single worker's raw config dict from a (validated) node config: deep-merge global with the named worker's override blocks, then set model_dirs to the shared repo, the endpoint port from base_port, the runtime device ordinal from the worker's GPU, and the node-level shared_host_weights flag. Every worker loads every bundle in the repo; the top-level models: map is an optional per-model override that pins the named models to device memory on the listed workers (translated here into scheduler.models.<name>.residency: device). The result has the shape build_config consumes.\n\n\n\n\n\n","title":"ReactantServerCore.worker_raw_config"},{"category":"type","location":"api/#ReactantServer.Scheduler","page":"API","text":"Scheduler\n\nThe deficit-weighted, cost-aware, coalescing dispatch engine. Concurrent requests land on per-model queues; a single dispatch loop runs one GPU execution at a time, coalescing same-model requests into one batched execution at a compiled size and sharing GPU time by relative model weight and a learned per-batch-size cost estimate. It holds the model registry, the backend, the device memory pool, the SchedulerConfig, per-model dispatch state, and an optional on-demand WeightCache. Submit work with infer and read observability counters with scheduler_metrics.\n\n\n\n\n\n","title":"ReactantServer.Scheduler"},{"category":"function","location":"api/#ReactantServer.infer","page":"API","text":"infer(scheduler, request) -> Vector{NamedTensor}\n\nSubmit a request and block until the scheduler returns the result. Re-raises any error captured during dispatch.\n\nRuns the model's preprocess/postprocess hooks here, on the caller's task, rather than on the dispatch loop: preprocess before the request is queued, postprocess on the raw device outputs the loop hands back. Because each gRPC request runs on its own task, many requests' hook work proceeds in parallel and overlaps the single, serialized GPU execution. The dispatch loop coalesces and runs qr.prepared and never executes a request whose preprocess has not finished, since a request is only enqueued (made visible to the loop) after preprocess returns.\n\n\n\n\n\n","title":"ReactantServer.infer"},{"category":"function","location":"api/#ReactantServer.scheduler_metrics","page":"API","text":"scheduler_metrics(scheduler) -> Dict{String,NamedTuple}\n\nSnapshot per-model observability: dispatch count, total compute consumed, current recent-compute EMA, queue-wait P50/P99, the histogram of dispatch batch sizes, and residency.\n\n\n\n\n\n","title":"ReactantServer.scheduler_metrics"},{"category":"function","location":"api/#ReactantServer.set_policy!","page":"API","text":"set_policy!(scheduler, name; weight=nothing) -> nothing\n\nUpdate a model's live scheduler policy. weight is consulted only by the fair discipline. Available in both residency modes. Raises on an unknown model.\n\n\n\n\n\n","title":"ReactantServer.set_policy!"},{"category":"function","location":"api/#ReactantServer.set_residency!","page":"API","text":"set_residency!(scheduler, name, target::ResidencyState) -> ResidencyState\n\nMove a model to the target residency floor. Only meaningful in externally-managed mode; the worker rejects it otherwise. Runs on the dispatch thread (the sole residency mutator) and blocks until applied. Raises on an unknown model or in self-managed mode.\n\n\n\n\n\n","title":"ReactantServer.set_residency!"},{"category":"function","location":"api/#ReactantServer.control_status","page":"API","text":"control_status(scheduler) -> NamedTuple\n\nA control-plane snapshot of the worker: its residency mode and scheduling discipline, plus a per-model view. A meta is reported as a single model whose footprint is the sum of its sub-models' weights; the internal sub-models are folded into it and not reported on their own, so the gateway packs and routes the group as one unit and never sees the stages.\n\n\n\n\n\n","title":"ReactantServer.control_status"},{"category":"function","location":"api/#ReactantServer.compact!","page":"API","text":"compact!(cache, registry; reload) -> Int\n\nDefragment the device arena. Frees every resident non-pinned device weight buffer at once so the BFC allocator coalesces its free list, then reloads each non-pinned model named in reload. Models not in reload are left freed; in self-managed mode they reload lazily through acquire! on their next dispatch. Host floors (host_weights) are never touched, so the reload is a pure host->device copy where a floor exists.\n\nDevice-pinned models are deliberately left in place: they are loaded once at startup, before any on-demand traffic, so they sit at the base of the arena, and compaction neither frees them nor pays to re-read them from disk. Only the on-demand churn region above them is freed and coalesced.\n\nRuns on the dispatch-loop thread (the sole residency mutator), so no execution reads a buffer while it is being freed. In externally-managed mode acquire! will not autonomously load, so the reload list is ignored there (the control plane re-pins what it needs). Returns the number of models reloaded.\n\n\n\n\n\ncompact!(scheduler; reload_models=String[]) -> Int\n\nFree the resident non-pinned device weights so the allocator coalesces its free list, then reload each model named in reload_models. An empty list frees only; non-pinned models not listed reload lazily on their next dispatch (self-managed mode). Device-pinned models are left in place (loaded once at startup, never reloaded from disk). A no-op when the on-demand weight cache is disabled. Runs on the dispatch thread (the sole residency mutator) and blocks until applied. Returns the number of models reloaded.\n\n\n\n\n\n","title":"ReactantServer.compact!"},{"category":"function","location":"api/#ReactantServer.acquire!","page":"API","text":"acquire!(cache, entry) -> nothing\n\nGuarantee entry.executable.weights is resident before the model runs. Device-pinned and already-resident models return immediately (the latter is bumped to most-recently-used). In self-managed mode an evicted model is loaded autonomously, evicting LRU victims until it fits the budget (a model larger than the whole budget is loaded anyway after evicting everything, with a warning, since it cannot run otherwise). In externally-managed mode the worker does not autonomously load: an evicted model raises NotResidentError.\n\n\n\n\n\n","title":"ReactantServer.acquire!"},{"category":"type","location":"api/#ReactantServer.NotResidentError","page":"API","text":"NotResidentError\n\nRaised by acquire! in externally-managed mode when a request targets a model whose weights are not currently resident. A control plane is authoritative for residency in that mode, so the worker does not autonomously load; the model must be pinned first.\n\n\n\n\n\n","title":"ReactantServer.NotResidentError"},{"category":"type","location":"api/#ReactantServer.RunningServer","page":"API","text":"RunningServer\n\nHandle to a server started with serve(...; blocking=false). It holds the resolved ServerConfig, the model registry, the running Scheduler, the device memory pool, the shared-memory registry, the underlying gRPC server, and the listen port. Pass it to stop! to shut the server down.\n\n\n\n\n\n","title":"ReactantServer.RunningServer"},{"category":"function","location":"api/#ReactantServer.weight_budget","page":"API","text":"weight_budget(; arena, fraction, wiggle, max_scratch, pinned_bytes) -> (; on_demand_budget, weight_pool, scratch_ceiling, pinned_over_commit)\n\nSolve for the on-demand weight-cache byte budget from the unified memory model. The arena holds, at the worst instant, pinned weights + on-demand weights + one model's execution max_scratch + a wiggle fraction of free slack:\n\nscratch_ceiling  = (1 - wiggle) * arena\nweight_pool      = min(fraction * arena, scratch_ceiling - max_scratch)   # for pinned + on-demand\non_demand_budget = clamp(weight_pool - pinned_bytes, 0, weight_pool)\n\nfraction caps the share of the arena devoted to weights (1.0 = all of it, minus scratch + wiggle). Pinned models reserve their footprint off the top, so the operator never subtracts it by hand. pinned_over_commit (pinned exceed the pool) flags a genuine over-commit no sizing can fix. Pure arithmetic, no device access, so it is unit testable.\n\n\n\n\n\n","title":"ReactantServer.weight_budget"},{"category":"function","location":"api/#ReactantServer.weight_cache_stats","page":"API","text":"weight_cache_stats(cache) -> NamedTuple\n\nSnapshot the cache counters under its lock for observability.\n\n\n\n\n\n","title":"ReactantServer.weight_cache_stats"},{"category":"function","location":"api/#ReactantServer.free_weights!","page":"API","text":"free_weights!(backend, bufs) -> nothing\n\nRelease every device buffer in bufs. Used to evict an unpinned model's resident weights.\n\n\n\n\n\n","title":"ReactantServer.free_weights!"},{"category":"function","location":"api/#ReactantServer.transfer_to_device","page":"API","text":"transfer_to_device(backend, pool, hosts) -> Vector{Any}\n\nTransfer already-materialized host weight Arrays to device buffers, in order. This is the only cost paid on an on-demand load when weights are pinned in host RAM.\n\n\n\n\n\n","title":"ReactantServer.transfer_to_device"},{"category":"type","location":"api/#ReactantServerClient.RetryPolicy","page":"API","text":"RetryPolicy(; enabled=true, initial_backoff=1.0, factor=2.0, max_backoff=Inf,\n              min_budget=0.05, jitter=true, retry_unavailable=true)\n\nClient-side retry policy for requests the server sheds under overload. When a ModelInfer call is rejected with RESOURCE_EXHAUSTED (a worker or gateway at its concurrency cap, see endpoints.max_concurrent_requests), the client waits initial_backoff seconds and retries, growing the interval by factor each attempt (1s, 2s, 4s, ...) up to max_backoff, until the model's deadline budget is spent. The per-attempt deadline sent to the server shrinks to the remaining budget, so the retries never push total wall time past the original deadline.\n\njitter picks each wait uniformly in [0, backoff] (full jitter) so that concurrent chunk requests do not synchronize into a retry storm against the same worker. min_budget (seconds) is the smallest remaining budget worth another attempt; below it the client stops and surfaces the shed error. retry_unavailable also retries UNAVAILABLE (no replica reachable). Deadline, NOTFOUND, and INVALIDARGUMENT errors always fail fast. Set enabled=false to restore the old fail-fast behavior.\n\n\n\n\n\n","title":"ReactantServerClient.RetryPolicy"},{"category":"type","location":"api/#ReactantServerGateway.RunningGateway","page":"API","text":"RunningGateway\n\nHandle to a gateway started with serve_gateway(...; blocking=false). Pass it to stop! to shut the gRPC server, the readiness prober, and the admin HTTP server down.\n\n\n\n\n\n","title":"ReactantServerGateway.RunningGateway"},{"category":"section","location":"bundles/#Bundles-and-model.jl","page":"Bundles","text":"A model is delivered to the server as a self-contained bundle: a directory holding a compiled model (an MLIR module Reactant compiles, a StableHLO program today), its weights, and a manifest. Bundles are produced offline by the conversion tooling and loaded at server startup.","title":"Bundles & model.jl"},{"category":"section","location":"bundles/#Bundle-layout","page":"Bundles","text":"A bundle is a directory containing:\n\nmanifest.yaml - the metadata parsed into a Manifest: I/O specs, dtypes, shapes, and the compiled batch sizes.\nmodel.mlir - the MLIR module Reactant compiles, currently a serialized StableHLO portable artifact (single batch size), or one module per size as model.b{N}.mlir sharing a single weights.safetensors.\nweights.safetensors - the model weights, memory-mapped at load time.\nmodel.jl - optional; registers custom pre/post-processing (see below).\n\nThe directory name is the model name: renaming the directory renames the model, with no edits to the manifest or model.jl (a name declared in either is informational and ignored). Each immediate subdirectory of model_repo that contains a manifest.yaml is a bundle. In dynamic mode a renamed bundle directory (same contents) is detected by the watcher and renamed in place, keeping the compiled executables and resident weights; only new or changed bundles are compiled.","title":"Bundle layout"},{"category":"section","location":"bundles/#Manifest-shape-encoding","page":"Bundles","text":"Each tensor's shape is an einsum-style string of single ASCII letters, one per axis, with a companion dims: map giving the size of every non-batch letter:\n\nexecutable_inputs:\n  - name: input\n    dtype: f32\n    shape: \"chwn\"     # channel, height, width, batch\n    dims:\n      c: 3\n      h: 224\n      w: 224\n\nThe letters n and b are reserved batch markers (at most one occurrence per tensor). Other letters are tensor-scoped (no implicit cross-tensor equality) and must be unique within a single shape. A size of -1 in dims marks a variable axis, a Dim with kind VARIABLE: a dynamic non-batch axis. On client_outputs that pass through model.jl that is how a result with a data-dependent extent is declared, for example the detection count of a postprocessed detector. On executable inputs a variable axis is servable only when the compiled variants are enumerated in a top-level input_shapes block (see write_bundle below); the Object Detection example compiles one weight set for several image shapes this way.\n\nThe per-input batch axis is derived from the position of n/b; at inference the request's size along that axis must equal one of batching.compiled_batch_sizes. Each tensor parses into a TensorSpec with a Dim per axis, and the compiled sizes form the BatchingSpec. The writer stamps format_version: \"2.0\"; the loader accepts 2.0 or 2.\n\nShapes use the Julia column-major convention (the batch dimension is the last axis), which is the reverse of the row-major form a Python/XLA exporter would write. The wire codec handles the conversion, so KServe clients see canonical row-major shapes.\n\nDatatypes are written as manifest tokens (f32, bf16, i64, bool, and so on); see DType for the full mapping between tokens, Julia types, and KServe wire strings. Client-facing tensors must use a dtype that has a KServe wire mapping (FP8 is executable-only).","title":"Manifest shape encoding"},{"category":"section","location":"bundles/#Custom-pre/post-processing-with-model.jl","page":"Bundles","text":"A bundle may include a model.jl that calls register_model to attach preprocess and postprocess hooks. Both hooks receive and return a Vector{NamedTensor} (see NamedTensor); omitted hooks default to identity.\n\n# model.jl, inside the bundle directory\nusing ReactantServer\n\nfunction normalize(inputs)\n    # inputs :: Vector{NamedTensor}; transform and return a Vector{NamedTensor}\n    return inputs\nend\n\nfunction to_classes(outputs)\n    # e.g. map logits to class ids\n    return outputs\nend\n\nregister_model(\"resnet50\"; preprocess=normalize, postprocess=to_classes)\n\nThe worker runs the hooks on each request's own task (preprocess before the request is queued, postprocess on the result), crossing the world-age boundary with invokelatest. This means the hooks for different requests run concurrently, on multiple threads, overlapping the GPU execution: keep them free of shared mutable state (or guard it yourself). When model.jl transforms the I/O, declare the client-facing tensors via client_inputs / client_outputs in the manifest; without a model.jl those keys are not permitted and the executable specs are the client-facing specs. See register_model in the API reference for the exact hook signatures.\n\nFor a bundle whose model.jl chains several models with data-dependent logic rather than wrapping one executable, see Meta Models.","title":"Custom pre/post-processing with model.jl"},{"category":"section","location":"bundles/#Producing-bundles","page":"Bundles","text":"ReactantServerExport produces bundles offline and is kept out of the server's dependency graph. It is not part of the server runtime.\n\nA project that owns a Lux model (or any Reactant-traceable function) uses ReactantServerExport; Lux itself is not a dependency of the package:\n\nusing ReactantServerExport\nexport_bundle(:lux, model, ps, st, example_input;\n    dir=\"bundles/mlp\", name=\"mlp\", batch_sizes=[1, 8])\n\nThe :lux frontend traces model(x, ps, st) at each requested batch size, taking the first return as the output; the batch dimension is the last Julia axis (the Lux convention) and the leading network axis. A multi-input / multi-output variant takes example_inputs::Tuple and output_select mapping the raw model output to the ordered tuple of arrays to export; per-tensor batch axes default to each array's last Julia axis, overridable with input_batch_axes / output_batch_axes (1-based Julia axes, or nothing to opt one tensor out of batching).\n\nThe :reactant frontend is the generic form: any Reactant-traceable f(inputs..., weights...) with explicit name => array weight pairs, producing a single unbatched model.mlir:\n\nexport_bundle(:reactant, f, (x,), [(\"w1\", w1), (\"b1\", b1)];\n    dir=\"bundles/mlp\", name=\"mlp\")\n\nA PyTorch project also loads PythonCall, which triggers the package extension driving torch.export.export and torchax:\n\nusing ReactantServerExport, PythonCall\nexport_bundle(:pytorch, model, (example_input,);\n    dir=\"bundles/mlp\", name=\"mlp\", batch_sizes=[1, 8])\n\nThe :lux and :pytorch frontends trace once per requested batch size and write a server-loadable bundle. The batch dimension is the last Julia axis (the leading PyTorch axis after the row-major / column-major reversal). export_bundle(:torchscript, ...) exports a TorchScript artifact (a .pt file or a loaded ScriptModule) through the same extension; without PythonCall the :pytorch and :torchscript calls fail with a message directing you to load it.","title":"Producing bundles"},{"category":"section","location":"bundles/#write_bundle-and-IOSpec","page":"Bundles","text":"write_bundle is the low-level writer: it takes the executable IOSpecs, the StableHLO modules (one per batch size, or text or serialized bytes), and an ordered weights collection whose order becomes the safetensors argument_order, and emits manifest.yaml, the model[.b{N}].mlir files, and weights.safetensors. An IOSpec names a tensor, its network (row-major) shape, its dtype, the 0-based batch_axis, and optional explicit letters for the non-batch axes (the reserved markers n/b are rejected); a -1 axis size encodes a variable axis. The bundle directory's basename must equal name (the identity rule above).\n\nWith input_shapes given, the writer marks the variable executable-input axes -1 in the manifest and emits one module set per compiled variant as model.v{i}[.b{N}].mlir, all sharing the single weights.safetensors; each variant lists the variable-axis sizes in (input, axis) order, and the variants must share one set of batch sizes. client_inputs / client_outputs (each nothing or a Vector{IOSpec}) declare the wire-facing spec when it differs from the executable spec, for bundles whose model.jl transforms between the two; they are emitted only when given.\n\ncollect_provenance captures best-effort reproducibility metadata for a bundle (repo remote, commit, tree, branch, dirty flag, Julia and exporter versions, and an exported_at timestamp). export_bundle merges it into the manifest's provenance block; on a dirty work tree the uncommitted diff is written to the bundle as working_tree.patch rather than embedded in the manifest, and git_commit plus git apply --binary working_tree.patch reconstructs the exported code exactly.\n\nThe test suite also builds small bundles directly; see test/stablehlo_fixtures.jl.","title":"write_bundle and IOSpec"},{"category":"section","location":"bundles/#Checking-that-a-bundle-is-servable","page":"Bundles","text":"A bundle is served as executable(inputs..., weights...), so the compiled program must take exactly as many arguments as the manifest declares inputs plus the weights file holds tensors. Both of those halves are readable, and the number the executable actually wants is in neither: it lives inside model*.mlir, which is a serialized vhlo artifact rather than text. A bundle can therefore be internally inconsistent while every readable part of it looks correct, and the symptom is that the model registers and serves and then fails every inference with\n\nINVALID_ARGUMENT: Execution supplied 216 arguments but compiled program expected 217\n\nexport_bundle now checks this itself and refuses to write a bundle whose graph disagrees with it, naming both numbers and the usual cause. The usual cause is a device-resident value reachable from the traced closure: Reactant lifts every one of those into an argument whether the program reads it or not, so an RNG in layer state (which appears as a leading tensor<2xui64>) or a device-resident configuration value captured by the model becomes an argument no client can supply. Reading such values back to the host before tracing bakes them into the graph as constants instead.\n\nFor bundles that were written before that check existed, assert_bundle_arity reads the same three numbers back out of the artifact:\n\nusing ReactantServerExport\n\nassert_bundle_arity(\"export_out/my_model_v1\")          # raises if the bundle is unservable\nr = bundle_arity_report(\"export_out/my_model_v1\")      # the numbers, without raising\nr.servable, r.n_inputs, r.n_weights, r.expected\n\nbundle_arity_report never raises, so it can be run across a directory of bundles to triage them, and bundle_entry_arity reads one module's arity on its own. All three read the artifact rather than the process that produced it, so they hold for a bundle from any writer.","title":"Checking that a bundle is servable"},{"category":"section","location":"bundles/#Related-pages","page":"Bundles","text":"The manifest and boundary types are documented on the API page: Manifest, TensorSpec, Dim, BatchingSpec, load_manifest, DType, and NamedTensor. export_bundle, write_bundle, IOSpec, collect_provenance, assert_bundle_arity, bundle_arity_report, and bundle_entry_arity are documented in the ReactantServerExport docstrings. The Tutorial walks the full export-to-serve path, and Node Configuration covers how the server loads and watches a repository of bundles.","title":"Related pages"},{"category":"section","location":"on_demand_weights/#On-demand-Weights","page":"On-demand Weights","text":"Because only one model executes at a time, the GPU does not need every model's weights resident simultaneously. A GPU sized for a handful of models can therefore serve a much larger catalog, paying a small transfer cost only when a cold model is first called. This page is the operational guide to the weight memory system: the two memory tiers, the self-sizing probe that budgets them, residency control, node-shared host weights, BFC fragmentation and compaction, and the observability that confirms it all. Dispatch and batching are the Scheduler's job; the reverse proxy and its placement policy live on Multi-GPU Gateway.","title":"On-demand Weights"},{"category":"section","location":"on_demand_weights/#Two-tiers-of-weight-memory","page":"On-demand Weights","text":"Host RAM (resident by default). With the on-demand cache enabled, every model's weights default to system-pinned (residency: system): materialized once from disk into host RAM at startup and kept there. Host memory is plentiful and cheap relative to GPU memory, which removes disk from the hot path. A model can opt out with residency: unpinned, paying an mmap re-materialization on each on-demand load instead. Materializing the host floor is the expensive step, a layout copy at host-memory speed (materialize_host_weights!), and it happens exactly once at startup.\nGPU (managed working set). Pinned models keep their weights on the GPU for the server's lifetime. Every other model is loaded onto the GPU on demand when a request arrives, kept resident afterward so repeat requests are free, and evicted under a configured GPU byte budget using a least-recently-used policy. Eviction frees the device memory immediately through an explicit PJRT buffer release (ReactantServer.free_weights!).\n\nBecause the weights are already in RAM, an on-demand GPU load is a single host-to-device transfer (ReactantServer.transfer_to_device) rather than a reload from disk: tens of milliseconds even for the largest models, the same order of magnitude as a single inference.","title":"Two tiers of weight memory"},{"category":"section","location":"on_demand_weights/#Enabling-it","page":"On-demand Weights","text":"There is one knob: runtime.weight_cache_fraction, the fraction of the BFC arena (mem_fraction * device memory) devoted to all weights, pinned plus on-demand. It defaults to 1.0 (use the whole arena, GPU only), and 0 disables the cache so every model's weights stay resident. You normally do not set it at all.\n\nglobal:\n  runtime:\n    backend: cuda\n    weight_cache_fraction: 1.0         # default: use the whole arena, self-sized (see below)\n    weight_cache_wiggle_fraction: 0.1  # default: keep 10% of the arena free as headroom\n\nThese are the weight_cache_fraction / weight_cache_wiggle_fraction fields of RuntimeConfig, also settable via INFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_FRACTION and INFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_WIGGLE_FRACTION environment variables. The cache is GPU-only; on CPU there is no arena, so weights stay resident regardless.","title":"Enabling it"},{"category":"section","location":"on_demand_weights/#Self-sizing:-pinned-scratch-wiggle","page":"On-demand Weights","text":"The fraction is a ceiling on weight memory, not a promise it all fits as cache: each execution also needs transient device memory (activations, conv/GEMM workspace, IO) on top of resident weights, and pinned models occupy the arena too. The worker resolves the on-demand budget for you at startup with ReactantServer.weight_budget. It probes each model once in isolation (loads and runs it), reads the allocator's peak device usage to measure the worst-case scratch, and sets\n\non_demand_budget = min(fraction*arena, (1-wiggle)*arena - max_scratch) - pinned_weights\n\nso pinned weights reserve their share, the measured scratch and the wiggle slack are held back, and the rest is the on-demand cache. When everything fits, the cache simply holds all weights resident (no eviction); when it does not, it is memory-safe. The probe is bounded one-time startup work and doubles as an execution smoke test; the resolved budget is logged. Pinned weights or scratch large enough to leave no room are logged as a warning, since no sizing can fix that.","title":"Self-sizing: pinned + scratch + wiggle"},{"category":"section","location":"on_demand_weights/#Pinning-hot-models","page":"On-demand Weights","text":"A model that must never pay the on-demand transfer cost can be pinned to stay GPU-resident for the server's lifetime. Pinned models are exempt from eviction; the budget bounds only the unpinned working set.\n\nscheduler:\n  models:\n    resnet50:\n      residency: device      # pin_to_gpu: true is a back-compat alias\n\nThis is the residency field of ModelSchedConfig (unpinned, system, or device; unspecified models default to system when the cache is enabled, see ResidencyState). Pin the latency-sensitive or highest-traffic models, and let the long tail load on demand.","title":"Pinning hot models"},{"category":"section","location":"on_demand_weights/#Sharing-host-weights-across-same-node-workers","page":"On-demand Weights","text":"With several workers on one node, runtime.shared_host_weights: true backs each system-pinned model's host copy with a node-shared POSIX shared-memory region so the workers share one copy. The default PrivateWeightStore gives each worker its own copy; SharedWeightStore places one content-addressed region per model in /dev/shm, coordinated peer-to-peer through an advisory flock, so same-node workers attach one copy and each transfers it to its own device. host_materialize routes through the configured store, which is what makes this a configuration decision rather than a code change.\n\nThe regions and their lock files are created with mode 666 by default so containers running as unrelated UIDs can share them; that is world-writable, so set runtime.shared_host_weights_mode: \"660\" on production and multi-user systems (the server warns at startup when the shared store runs with the 666 default). Both fields belong to RuntimeConfig, with INFERENCE_SERVER_RUNTIME_SHARED_HOST_WEIGHTS and INFERENCE_SERVER_RUNTIME_SHARED_HOST_WEIGHTS_MODE environment overrides.","title":"Sharing host weights across same-node workers"},{"category":"section","location":"on_demand_weights/#Memory-fragmentation-and-compaction","page":"On-demand Weights","text":"When you size a GPU to squeeze the largest possible working set onto it, fragmentation becomes a real constraint. The device pool is one BFC (best-fit-with-coalescing) arena, claimed up front when runtime.preallocate: true. As the on-demand cache loads and evicts models of different sizes over time, the freed regions return to the arena's free list but do not always sit next to each other, so the arena can hold plenty of free bytes in total yet have no single gap large enough for the next model's weights. The load then fails or forces extra eviction even though the memory is technically there. The tighter you pack the GPU, the sooner this bites.\n\nPinned models are not the problem here: they are loaded once at startup, before any on-demand traffic, so they sit at the base of the arena and never move. Fragmentation accumulates only in the on-demand working set above them. Compaction targets exactly that region. It frees every resident on-demand weight buffer at once, so the allocator coalesces the now-contiguous free space back into one large region above the pinned base. Pinned models are left in place: they are never freed and never re-read from disk. Host floors (the system-pinned RAM copies) are also left untouched, so when an on-demand model reloads it is a fast host-to-device transfer rather than a disk re-materialization (ReactantServer.compact! implements this on the worker).\n\nWhatever drives compaction frees the on-demand region the same way; what differs is when it fires and what gets reloaded eagerly. Underneath, compaction is a worker control RPC, CompactMemory on the ControlService, that frees the on-demand region and reloads a list of models (empty means free only); both the standalone trigger and the gateway use it. There are two ways to drive it, matched to the two deployment shapes.","title":"Memory fragmentation and compaction"},{"category":"section","location":"on_demand_weights/#Standalone-worker-(no-gateway)","page":"On-demand Weights","text":"A worker runs compaction itself on a cadence counted in weight-cache loads, set under scheduler::\n\nscheduler:\n  compaction_interval: 200   # compact every 200 on-demand loads (0 disables, the default)\n\nA load is what places a variable-size weight block on the device, so loads are what fragment the arena; a dispatch to an already-resident model does not, which is why the cadence counts loads rather than requests or time. The worker trigger is always eager: it frees the on-demand region and lets it refill lazily as requests arrive. It is off by default, and a gateway-fronted worker should leave it off, so the gateway is the sole authority on compaction in a gateway deployment (next section). It is the compaction_interval field of SchedulerConfig, also exposed as INFERENCE_SERVER_SCHEDULER_COMPACTION_INTERVAL.","title":"Standalone worker (no gateway)"},{"category":"section","location":"on_demand_weights/#Behind-a-gateway","page":"On-demand Weights","text":"In a gateway deployment the gateway owns compaction; workers leave scheduler.compaction_interval at 0. The gateway ties compaction to placement changes, the fleet-level event that churns worker memory: when the lpt_packing scheduler repacks and moves a model from one worker to another, the new worker loads it and the old worker's copy goes cold. Two settings under the gateway's scheduling: block control it:\n\nscheduling:\n  mode: lpt_packing\n  compaction_mode: eager       # eager (default) | off | scheduled\n  compaction_interval: 1       # every placement-changing repack (see below)\n\ncompaction_mode selects what each affected worker reloads eagerly after the free:\n\neager (the default) frees the on-demand region on each worker whose placement changed and lets it refill with live traffic. Models the worker no longer serves are dropped immediately; the ones it still serves reload on their next request.\noff disables gateway-driven compaction.\nscheduled also reloads the set of models the repack just assigned to that worker, so the worker's new placement is warm right away instead of cold-loading on first request. The gateway computes that per-worker list from the placement, which is why this is a gateway concept.\n\ncompaction_interval is the cadence in repacks (default 1, so every placement-changing repack compacts). Because the demand signal is smoothed, most repacks recompute the same placement and do nothing; the counter advances on every repack but the fan-out only fires on the first placement-changing repack at or after the interval, so it can land a little later than exactly N. Both settings are also exposed as REACTANT_GATEWAY_SCHEDULING_COMPACTION_MODE and REACTANT_GATEWAY_SCHEDULING_COMPACTION_INTERVAL. A single client CompactMemory call to the gateway also fans out to every worker on demand, independent of the repack cadence, for a one-off fleet defragment.","title":"Behind a gateway"},{"category":"section","location":"on_demand_weights/#Cost-and-when-to-enable","page":"On-demand Weights","text":"With the on-demand cache disabled (weight_cache_fraction: 0) every model is permanently resident from startup and nothing churns, so compaction has no working set to defragment and is a no-op. When it does run it has a cost: freeing the on-demand region drops models that were warm, so they pay a host-to-device reload (lazily on next request for eager, or up front for scheduled).\n\nThe default is eager on every placement-changing repack (compaction_interval: 1), which keeps fragmentation from ever accumulating: whenever placement shifts, the affected workers defragment and refill from live traffic. The trade is a one-time reload on the first request per moved model, which the relatively infrequent repack cadence (rebalance_compute_seconds, 300 GPU-seconds by default) keeps modest. To cut reloads further, raise compaction_interval so it fires less often, switch to scheduled to warm the new placement up front instead of lazily, or set compaction_mode: off to disable it and watch for allocation failures under memory pressure (the signal that fragmentation has become the bottleneck).","title":"Cost and when to enable"},{"category":"section","location":"on_demand_weights/#Observability","page":"On-demand Weights","text":"The ReactantServer.Scheduler exposes weight-cache residency and load/evict counters alongside its dispatch metrics; read them with ReactantServer.scheduler_metrics (ReactantServer.weight_cache_stats is the underlying snapshot). Use them to confirm that hot models stay resident and that the eviction rate is acceptable for your budget. Coalescing (packing many requests into one execution) amortizes a one-time on-demand transfer across every item in the batch, so on-demand loading and batching reinforce each other.\n\nEach compaction also logs a memory compacted line with the device free space and on-demand budget before and after, and the weight cache tracks a compactions counter. Compare the before/after device free figures to confirm that compaction actually recovered contiguous space on your hardware: it relies on the allocator returning freed buffers to the arena and coalescing them, which the log lets you verify rather than assume.\n\nThe worker also exports the BFC allocator's live numbers as Prometheus gauges (aggregated through the gateway's /metrics): worker_device_memory_in_use_bytes, worker_device_memory_free_bytes, worker_device_memory_peak_in_use_bytes (the session high-water, your empirical scratch + resident ceiling), and worker_device_memory_pool_bytes. The peak gauge is what the startup auto-sizing measured; a rising peak over a long uptime is the signal to revisit the budget. Note the GPU BFC allocator does not report a largest-contiguous-free-block figure, so fragmentation is not directly observable here; in practice it surfaces as an allocation failing despite ample reported free bytes, which is what compaction (above) addresses.","title":"Observability"},{"category":"section","location":"client/#Client-Usage","page":"Client Usage","text":"ReactantServerClient is the inference client for a ReactantServer worker or the Gateway. It builds KServe V2 requests against ReactantServerCore's protobuf messages and forwards them over gRPC. It depends only on ReactantServerCore and the gRPC layer, so it carries no Reactant/XLA dependency and installs quickly on a plain client machine. (The same KServe V2 wire protocol means any Triton or KServe gRPC client also works; this package is the convenient Julia option.) The server-side handling of the wire and shared-memory data planes is covered on the Bundles page.","title":"Client Usage"},{"category":"section","location":"client/#Installation","page":"Client Usage","text":"The client is a workspace member. From this repository, activate its project:\n\njulia --project=packages/ReactantServerClient -e 'using Pkg; Pkg.instantiate()'\n\nYou can confirm it pulls no Reactant:\n\nusing ReactantServerClient\n@assert Base.identify_package(ReactantServerClient, \"Reactant\") === nothing","title":"Installation"},{"category":"section","location":"client/#One-shot-inference","page":"Client Usage","text":"Point a KServeModel at a running server (a worker, or the gateway for a multi-GPU node), build inputs with InferInput, call the one-shot infer_sync, and read outputs with InferOutput:\n\nusing ReactantServerClient\n\nkserve_init()\ntry\n    model = KServeModel(\"grpc://127.0.0.1:8080\", \"scale4\"; max_batch_size = 1)\n    x = Float32[1, 2, 3, 4]\n    response = infer_sync(model, [InferInput(\"INPUT__0\", x)])\n    y = InferOutput(\"OUTPUT__0\", response, Float32)\n    @show vec(collect(y))\nfinally\n    kserve_shutdown()\nend\n\nKServeModel takes either (host, port, model_name) or a url of the form grpc://host:port (or bare host:port); grpcs/https selects a secure channel. max_batch_size caps how many items the batched drivers coalesce per request, and deadline is the per-request timeout in seconds. Run the snippet under the client project against a server you have started, for example a worker on the e2e scale4 bundle; the Tutorial walks through the full setup end to end.","title":"One-shot inference"},{"category":"section","location":"client/#Batched-inference-over-a-dataset","page":"Client Usage","text":"For many items, implement AbstractInferenceIO and use infer_async (concurrent) or infer_sync(model, io) (serial). The driver stages each chunk through a shared staging pool, an InferenceBufferPool wrapping ReactantServerCore.BufferPool. The first call to each server probes it with the IsSameIPCNamespace RPC: if the server confirms it shares the client's IPC namespace, inputs travel over the system shared-memory data plane with zero extra copies on the wire; otherwise, or if the server does not implement the probe, the driver uses inline transport.\n\nThe shared_memory keyword on KServeModel controls this choice:\n\n:auto (default) probes and uses shared memory when available.\n:on forces it, and errors loudly if the server is in a different namespace.\n:off always sends inline; the probe is not sent.\n\nThere is no silent runtime fallback once shared memory is chosen. A stale registration (the server restarted and no longer knows the region) is re-registered and retried once in-band; if that fails, the client latches that endpoint to inline transport and a background task re-probes it every shm_reprobe_interval seconds, restoring shared memory when the server recovers.\n\nYour IO implements length, item_input_bytes, infer_encode_chunk! (request the chunk's input buffers with scratch, write into them, and return them tagged with their tensor names), and infer_decode_chunk! (consume the response). The pool's fixed-slot allocator hands every chunk a disjoint slot, so concurrent infer_async calls are safe. kserve_init(; pool_bytes, n_slots, shm_reprobe_interval) sizes the staging pool (256 MiB by default) and its number of slots (8 by default), which bounds how many chunks can be in flight against one pool; kserve_shutdown() tears the pool and the gRPC subsystem down.\n\nA concrete IO for a model with input INPUT__0 and output OUTPUT__0, each a length-4 Float32 vector per item:\n\nusing ReactantServerClient\n\nstruct VectorIO <: AbstractInferenceIO\n    inputs::Vector{Vector{Float32}}    # each item: 4 Float32\n    outputs::Vector{Vector{Float32}}   # filled in by infer_decode_chunk!\nend\n\nBase.length(io::VectorIO) = length(io.inputs)\n\n# Bytes one item contributes to the request, summed over all inputs.\nReactantServerClient.item_input_bytes(::VectorIO) = 4 * sizeof(Float32)\n\nfunction ReactantServerClient.infer_encode_chunk!(io::VectorIO, r::UnitRange, slot::PoolSlot)\n    n = length(r)\n    # Request all input buffers up front by name (Julia column-major: per-item dims, batch\n    # axis last). `scratch` carves them from the chunk's slot and returns wire descriptors;\n    # write into each with pool_view. The SHM-vs-inline transport is handled by the driver,\n    # so this is the same code either way.\n    input = scratch(slot, \"INPUT__0\", (4, n), Float32)  # scalar form: one descriptor\n    buf = pool_view(input)                    # multiple inputs: feats, mask = pool_view(inputs...)\n    @infer_inbounds for (k, i) in enumerate(r)  # elided normally, checked under validate_io\n        buf[:, k] .= io.inputs[i]\n    end\n    return input                              # a lone PoolInferInput is accepted directly\nend\n\nfunction ReactantServerClient.infer_decode_chunk!(io::VectorIO, r::UnitRange, response)\n    out = InferOutput(\"OUTPUT__0\", response, Float32) # Julia column-major (4, n)\n    @infer_inbounds for (j, i) in enumerate(r)\n        io.outputs[i] = collect(out[:, j])\n    end\n    return nothing\nend\n\nio = VectorIO([Float32[i, i, i, i] for i in 1:100], [Float32[] for _ in 1:100])\ninfer_async(model, io)                        # results land in io.outputs\n\nscratch is the same buffer-request interface meta models use through call.scratch; see the Meta Models page. The lower-level path is still supported: carve a subslot, pool_view it, and return an InferInput(name, sub, shape, T) descriptor; both forms may be mixed in the returned vector.\n\nOutputs can also be read back through shared memory: return a non-empty output_specs vector of OutputSpec(name, dtype, per_item_dims) entries and the driver asks the server to write each declared output into the staging slot, then copies the bytes into the response before infer_decode_chunk! sees it, so InferOutput works the same on either transport. The default (empty) keeps every output inline. A non-empty vector also opts the IO into explicit-output mode: the request asks for exactly those outputs, so declare every output the IO consumes. Outputs with data-dependent shapes cannot be sized ahead of time and must stay inline.","title":"Batched inference over a dataset"},{"category":"section","location":"client/#Validating-an-IO-against-a-model","page":"Client Usage","text":"Because an IO hard-codes a model's tensor names, dtypes, and shapes, it can silently drift from the served model. validate_io dry-runs an IO against the model's true I/O spec without sending an inference request: it runs infer_encode_chunk! and infer_decode_chunk! once over synthetic, spec-shaped data and surfaces name, dtype, shape, and indexing mismatches as errors. Get the spec from model_io_spec(model) against a running server (the ModelMetadata RPC; it throws if the server is unreachable or does not implement metadata), or from manifest_io_spec(path) offline against a model's manifest.yaml, suitable for a build-time check. Reported shapes are Julia column-major (batch axis last), matching the axis order you build arrays in.\n\nvalidate_io(model, io)                              # online: fetches the spec from the server\nvalidate_io(manifest_io_spec(\"manifest.yaml\"), io)  # offline: no server needed\n\nThe harness runs your real methods, so call it on a representative or dummy IO (it may write into the IO's buffers at positions 1:items). For example, validating the VectorIO above against a model whose OUTPUT__0 is actually a length-3 vector reports the shape mismatch, and an infer_decode_chunk! that indexed out[:, j] past the real batch size surfaces as an indexing error, both without sending a request.","title":"Validating an IO against a model"},{"category":"section","location":"client/#Bounds-checks-in-hot-paths","page":"Client Usage","text":"validate_io can only catch an out-of-range index if the access is bounds-checked. Julia's built-in @inbounds elides the check unconditionally, so an @inbounds access stays unchecked even inside the dry run. If you want @inbounds performance in production but a robust check during validation, write the access with @infer_inbounds instead, as the VectorIO example does. It elides the check in normal use, but because validate_io runs your methods inside with_bounds_checks, an @infer_inbounds access is bounds-checked during the dry run and an out-of-range index is caught rather than silently read. This is opt-in: using @infer_inbounds is the user's responsibility, and bare @inbounds remains unchecked unless Julia is started with --check-bounds=yes (which Pkg.test does).\n\n# Bounds-checked under validate_io, elided in production:\n@infer_inbounds buf[(4k - 3):(4k)] .= io.inputs[i]\n\n# Force the checked behavior anywhere, not just in validate_io:\nwith_bounds_checks() do\n    run_my_io_step()\nend","title":"Bounds checks in hot paths"},{"category":"section","location":"client/#Retries","page":"Client Usage","text":"A worker or gateway at its concurrency cap (endpoints.max_concurrent_requests, see Node Configuration) sheds requests with RESOURCE_EXHAUSTED. By default the client retries: ReactantServerClient.RetryPolicy waits initial_backoff seconds and grows the interval by factor each attempt (1s, 2s, 4s, ...) up to max_backoff, until the model's deadline budget is spent. The per-attempt deadline sent to the server shrinks to the remaining budget, so retries never push total wall time past the original deadline. jitter (full jitter: wait uniform in [0, backoff]) keeps concurrent chunk requests from synchronizing into a retry storm, and min_budget (seconds) is the smallest remaining budget worth another attempt. retry_unavailable also retries UNAVAILABLE (no replica reachable). Deadline, NOT_FOUND, and INVALID_ARGUMENT errors always fail fast. Pass a RetryPolicy(...) as the retry keyword to KServeModel; ReactantServerClient.RetryPolicy(enabled = false) restores the old fail-fast behavior.\n\nEvery documented name in ReactantServerClient, including the signatures covered here, is collected on the API page.","title":"Retries"},{"category":"section","location":"transformers/#Transformer-Text-Models-(BERT)","page":"Transformer Text Models","text":"BERT-family text models are the canonical case for the plain bundle path with a model.jl that owns tokenization. The transformer itself is dense tensor math that torch.export traces cleanly to a single static StableHLO program, but two things around it are not tensor math: turning a string into token ids, and turning raw logits into the shape a caller wants. ReactantServer keeps both in the bundle's model.jl, so the traced graph is exactly the encoder (plus any in-graph numeric head), the client sends raw UTF-8 text, and no Python tokenizer or activation library is needed at serve time.\n\nThis page walks through exporting four public HuggingFace checkpoints that cover the common text-serving shapes. For the bundle/manifest/model.jl contract itself see Bundles & model.jl; for the PyTorch export mechanics see the export_bundle(:pytorch, ...) section there.\n\ntip: Runnable example\nexamples/transformers/ in the repository is a complete, runnable version of this walkthrough. It is split into three copy-pasteable commands, each with its own environment: export the four bundles, serve them on a single GPU, and a client that queries all four over KServe V2. See examples/transformers/README.md.","title":"Transformer Text Models (BERT)"},{"category":"section","location":"transformers/#The-four-models","page":"Transformer Text Models","text":"Bundle HuggingFace checkpoint torch class In-graph head model.jl\nsplade naver/splade-cocondenser-ensembledistil AutoModelForMaskedLM log1p/relu/mask/max term scores tokenize; sparsify to CSR\nembedding sentence-transformers/all-MiniLM-L6-v2 AutoModel masked mean-pool + L2 normalize tokenize; passthrough\ncross_encoder cross-encoder/ms-marco-MiniLM-L-6-v2 AutoModelForSequenceClassification none (raw logit) tokenize pair; add sigmoid\nsentiment distilbert-base-uncased-finetuned-sst-2-english AutoModelForSequenceClassification none (raw logits) tokenize; add softmax + argmax\n\nThe four cover the common text-serving shapes: learned sparse term expansion (SPLADE), a dense 384-dim unit-norm sentence embedding, query/document relevance scoring, and binary sentiment classification. All four share the bert-base-uncased WordPiece vocab (30522 tokens), so one tokenizer serves every bundle: the code in ReactantServer.BertText, the vocab as a per-bundle vocab.txt.","title":"The four models"},{"category":"section","location":"transformers/#Tokenization-lives-in-the-bundle","page":"Transformer Text Models","text":"Clients send raw UTF-8 bytes; the bundle's model.jl decodes them and tokenizes in Julia with ReactantServer.BertText, a self-contained BERT WordPiece (uncased) reimplementation that depends on nothing but Base. It matches the HuggingFace Rust tokenizer, including the awkward corners: unassigned codepoints stay in the word, ASCII symbols like  +  =  ^    count as punctuation, LongestFirst pair truncation, and special-token literals in raw text.\n\nBertText is the request-side counterpart to ReactantServer.DetectionGlue (the reusable detection math behind the Object Detection converter): shared code lives in the package, per-model configuration is baked into model.jl, and the only tokenizer asset the export driver copies into a bundle is the checkpoint's vocab.txt (loaded at serve time relative to model.jl's directory). A bundle exported this way needs a server that has BertText, the same version coupling the detector bundles already accept for DetectionGlue.\n\npreprocess turns the wire tensors into the executable's token-id inputs. The single-sequence models (splade, embedding, sentiment) call encode_text_batch and emit input_ids + attention_mask; the cross encoder is a text pair (one query scored against N keys) and calls encode_pair_batch, adding token_type_ids. Both decode the padded UInt8 rows, encode with the BERT template, and pad the batch up to the smallest compiled sequence bucket that fits:\n\nconst BT = ReactantServer.BertText\nconst TOKENIZER = BT.load_tokenizer(joinpath(@__DIR__, \"vocab.txt\"))\n\nfunction preprocess(inputs::Vector{NamedTensor})\n    byname = Dict(t.name => t for t in inputs)\n    input_ids, attention_mask = BT.encode_text_batch(TOKENIZER,\n        byname[\"texts\"].data::Matrix{UInt8},             # (max_bytes, batch) col-major\n        vec(byname[\"text_lens\"].data);\n        max_len=MAX_LEN, buckets=SEQ_BUCKETS, lens_name=\"text_lens\")\n    return NamedTensor[NamedTensor(\"input_ids\", input_ids),\n                       NamedTensor(\"attention_mask\", attention_mask)]\nend\n\nThe BertText surface is a small set of composable helpers:\n\nHelper What it does\nload_tokenizer(vocab_path) Load a HuggingFace vocab.txt (one token per line, line number minus 1 is the id); returns the BertTokenizer the other helpers take.\nwire_text(bytes) / wire_texts(texts, lens) Decode one UTF-8 tensor (the cross encoder's shared query) or a (max_bytes, batch) matrix of zero-padded rows using the per-row byte lengths.\nencode_single(t, text) / encode_pair(t, a, b) Encode one text as [CLS] text [SEP], or a pair as [CLS] a [SEP] b [SEP] with HF LongestFirst truncation; the pair form also returns token_type_ids.\nencode_text_batch / encode_pair_batch The preprocess entry points: decode the wire rows, encode each, and pad to the smallest bucket that fits.\npad_batch(encoded, buckets) / seq_bucket(n, buckets) Pack per-row token ids into (seq, batch) matrices at the smallest compiled sequence length that holds them; pad_batch errors clearly if an input exceeds the largest bucket.\n\nA bundle that needs a shape these two do not cover can drop a level and compose the same pieces: wire_texts decodes the padded rows to Strings, encode_single / encode_pair encode one text, and pad_batch packs the result into (seq, batch) matrices at the right bucket.","title":"Tokenization lives in the bundle"},{"category":"section","location":"transformers/#The-raw-logits-/-classifier-rule","page":"Transformer Text Models","text":"The two classifiers (cross_encoder, sentiment) emit raw logits from the traced graph; the activation is applied in postprocess, which returns both the raw logits and the probabilities. Keeping the sigmoid/softmax out of the graph follows the same rule as every other classification bundle in the package, and lets a caller read either value:\n\nfunction postprocess(out::Vector{NamedTensor})           # sentiment\n    logits = out[1].data::Matrix{Float32}                # (2, batch): rows are classes\n    probs = ReactantServer.NNlib.softmax(logits; dims=1)\n    label_id = Int32[argmax(view(logits, :, b)) - 1 for b in 1:size(logits, 2)]\n    return NamedTensor[NamedTensor(\"logits\", logits),\n                       NamedTensor(\"probs\", probs),\n                       NamedTensor(\"label_id\", label_id)]\nend\n\nThe cross encoder's postprocess is the same shape with a sigmoid instead: prob = sigmoid.(logits) over the per-pair score vector. By contrast, SPLADE's log1p/relu/mask/max and the embedding model's masked mean-pool + L2 normalization stay in the traced graph: they define the model's numeric output (and, for SPLADE, do the (batch, seq, vocab) -> (batch, vocab) reduction), rather than being a presentation-layer activation. SPLADE's postprocess then only sparsifies the dense term-score vector to a CSR triple, applying the Python server's keep rule (round(x, decimals=2) != 0, half-to-even ties, which torch.round semantics match Julia's default rounding) and emitting 0-based vocab indices, unrounded weights, and row offsets; the embedding postprocess is a passthrough.","title":"The raw-logits / classifier rule"},{"category":"section","location":"transformers/#The-wire-contract","page":"Transformer Text Models","text":"Because model.jl reshapes the wire I/O, the manifest declares client_inputs/client_outputs (what the caller sends and receives) distinct from executable_inputs/executable_outputs (the token tensors and raw logits the traced program sees). The export driver states this with IOSpecs passed to export_bundle(:pytorch, ...); for example the sentiment bundle:\n\nexport_bundle(:pytorch, model, (input_ids, attention_mask);\n    dir, name,\n    input_names  = [\"input_ids\", \"attention_mask\"],\n    output_names = [\"logits\"],\n    axis_letters = Dict(\"input_ids\" => ['s'], \"attention_mask\" => ['s'], \"logits\" => ['c']),\n    batch_sizes = [1, 8],\n    matmul_precision = \"highest\",\n    client_inputs = [\n        IOSpec(\"texts\", UInt8, [1, -1]; batch_axis=0, letters=['c']),\n        IOSpec(\"text_lens\", Int32, [1]; batch_axis=0),\n    ],\n    client_outputs = [\n        IOSpec(\"logits\", Float32, [1, 2]; batch_axis=0, letters=['c']),\n        IOSpec(\"probs\", Float32, [1, 2]; batch_axis=0, letters=['c']),\n        IOSpec(\"label_id\", Int32, [1]; batch_axis=0),\n    ],\n    provenance = _prov(...))\n\nmatmul_precision = \"highest\" is required on the PyTorch path: JAX freezes float32 matmul precision at trace time based on the export host, so a CPU trace without it would bake a lower precision. See Bundles & model.jl for the full IOSpec and manifest encoding.\n\nThe client-facing shapes below are in row-major (KServe/client) order; the wire codec converts to the Julia column-major convention at the boundary.\n\nBundle Client inputs Client outputs\nsplade texts UINT8 [batch, max_bytes] (zero-padded rows), text_lens INT32 [batch] indices INT32 [K], values FP32 [K], row_offsets INT64 [batch+1] (CSR, 0-based vocab ids)\nembedding texts, text_lens (as above) embedding FP32 [batch, 384] (unit-norm)\ncross_encoder query UINT8 [q_bytes] (shared across all keys), keys UINT8 [batch, max_bytes], key_lens INT32 [batch] logits FP32 [batch] (raw), prob FP32 [batch] (sigmoid)\nsentiment texts, text_lens logits FP32 [batch, 2] (raw), probs FP32 [batch, 2] (softmax), label_id INT32 [batch] (0 NEGATIVE, 1 POSITIVE)\n\nAll inputs are raw UTF-8 bytes; the bundle tokenizes them. Note the two text encodings in one table: the single-sequence models read one (max_bytes, batch) matrix of zero-padded rows plus one length per row, while the cross encoder's query is a single unpadded byte vector shared by every (query, key) pair.","title":"The wire contract"},{"category":"section","location":"transformers/#Sequence-length-and-batching","page":"Transformer Text Models","text":"Each bundle compiles a single sequence length of 512 and batch sizes [1, 8], i.e. two programs per bundle (model.b1.mlir, model.b8.mlir), all batch variants sharing one weights.safetensors. Every request pads to 512, which is bit-identical to a tighter bucket because every op is attention-mask aware; the tradeoff is wasted compute on short inputs in exchange for far fewer programs and lower compile time and command-buffer count. The constant lives in two places that must agree: SEQ_LEN in the export driver and SEQ_BUCKETS = (512,) in each model.jl. To trade compute back for more programs, add sequence buckets or batch sizes in the driver (export_bundle supports multiple input shapes over one weight set) and bump SEQ_BUCKETS/max_batch to match. An over-long text fails in preprocess with a clear error naming the encoded length and the largest compiled bucket, instead of a BoundsError while filling the padded matrices.","title":"Sequence length and batching"},{"category":"section","location":"transformers/#Running-the-example","page":"Transformer Text Models","text":"examples/transformers/ is split into three single-purpose Julia environments so each loads only what it needs (and they stop invalidating each other's precompilation): export is the only one with PythonCall + torch, server is the only one with Reactant, and client has neither. Each environment resolves independently the first time you use it:\n\nfor env in export server client; do\n  julia --project=examples/transformers/$env -e 'using Pkg; Pkg.instantiate()'\ndone\n\nThen run the three steps in order (the server stays running; drive it from a second terminal):\n\n# 1. Export the bundles (first time only; writes ./bundles/). Needs network for the checkpoints.\njulia --project=examples/transformers/export examples/transformers/export/export_stablehlo.jl\n\n# 2. Serve on a single GPU (blocks; Ctrl-C to stop). Add --cpu for a GPU-free smoke test.\nCUDA_VISIBLE_DEVICES=0 julia --project=examples/transformers/server examples/transformers/server/serve.jl\n\n# 3. In another terminal: query all four models.\njulia --project=examples/transformers/client examples/transformers/client/query.jl\n\nThe server port defaults to 8080; set TX_PORT (and TX_HOST for the client) to change it on both step 2 and step 3.","title":"Running the example"},{"category":"section","location":"transformers/#Export-(export_stablehlo.jl)","page":"Transformer Text Models","text":"The driver imports torch/torchax through PythonCall before ReactantServerExport pulls in Reactant; that init order is critical, because Triton's static LLVM/MLIR registration SIGSEGVs if Reactant loads first. Each of the four builders wraps its checkpoint (torch class per the table above, plus the in-graph head for splade/embedding), traces it with export_bundle(:pytorch, ...), and stages the per-bundle model.jl plus the shared vocab.txt into ./bundles/<name>/ (the tokenizer code itself lives in ReactantServer.BertText, so it is not staged). Bundles that already exist are skipped; delete ./bundles/ to re-export.\n\nPython dependencies come from ReactantServerExport's CondaPkg (torch, torchax, jax) plus transformers from export/CondaPkg.toml; CondaPkg resolves and installs them on the first export, and the first run downloads the four checkpoints from HuggingFace, so both need network. For corporate proxies, the driver points Python's TLS at the OS CA bundle (SSL_CERT_FILE, defaulting from REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE/JULIA_SSL_CA_ROOTS_PATH or /etc/ssl/certs/ca-certificates.crt) so the checkpoint downloads trust a MitM proxy's CA.","title":"Export (export_stablehlo.jl)"},{"category":"section","location":"transformers/#Serve-(serve.jl)","page":"Transformer Text Models","text":"The server validates that all four bundles exist under examples/transformers/bundles, picks ReactantServer.CPU_BACKEND or CUDA_BACKEND from --cpu, and builds a ReactantServer.ServerConfig (runtime, scheduler, and endpoints on 127.0.0.1:$TX_PORT) before calling ReactantServer.serve(...; backend = ReactantServer.ReactantBackend()), which blocks until Ctrl-C. Running the export or serve.jl --cpu outside Docker can hit an EACCES on Reactant's default compile cache; set a writable REACTANT_CACHE_DIR if so.","title":"Serve (serve.jl)"},{"category":"section","location":"transformers/#Client-(query.jl)","page":"Transformer Text Models","text":"The client depends only on ReactantServerClient (no Reactant, no PythonCall), so it loads fast. It packs strings into the wire tensors itself (pack_texts: a (max_bytes, batch) UInt8 matrix plus an Int32 byte-length vector) and sends raw UTF-8 to all four models, then decodes each response: cosine similarity between unit-norm embeddings, SPLADE's top expansion terms turned back into tokens via the shared vocab, cross-encoder reranking of candidate answers, and sentiment labels (0 NEGATIVE, 1 POSITIVE) with their probabilities.","title":"Client (query.jl)"},{"category":"section","location":"transformers/#See-also","page":"Transformer Text Models","text":"examples/transformers/ for the runnable end-to-end example (export, serve, client)\nBundles & model.jl for the bundle contract, IOSpec, and the manifest encoding\nObject Detection for the data-dependent (meta) export path\nClient Usage for building requests with InferInput/InferOutput","title":"See also"},{"category":"section","location":"node_config/#Node-Configuration","page":"Node Configuration","text":"A deployment is described by a single node file. It is the only supported config format for workers. The file describes one or more single-GPU workers on one machine and, optionally, the gateway that fronts them. Each worker reads this same file, resolves its own entry by name, and loads (and can serve) every bundle in the shared model repository.\n\nA single GPU is just a one-worker node: keep one entry under workers: (or omit workers: entirely under the supervisor, below). Growing to more GPUs means adding workers, not changing the config format.\n\nUnder the node supervisor (the supervisor default, see Deployment) the workers: list is optional: omit it and add gpus: auto (or an integer count, or an explicit device list) and the supervisor synthesizes one worker per detected GPU, assigning each its device. An explicit workers: list still wins when present. The keys below describe that explicit form, which the supervisor also honors.","title":"Node Configuration"},{"category":"section","location":"node_config/#Top-level-keys","page":"Node Configuration","text":"# One shared bundle repository. Each immediate subdirectory containing a manifest.yaml is a\n# bundle; its directory name is the model name.\nmodel_repo: /var/lib/reactantserver/models\n\n# Worker at index i binds base_port + i unless it sets an explicit `port:`.\nbase_port: 8080\n\n# Optional Prometheus metrics port per worker (metrics_base_port + i): worker0 -> 9100,\n# worker1 -> 9101, and so on. Each worker serves /metrics, /healthz, /readyz on it.\n# Absent disables per-worker metrics.\nmetrics_base_port: 9100\n\n# Supervisor-only: `auto` (the default), an integer device count, or an explicit device list\n# (ordinals or GPU UUIDs). The supervisor synthesizes one worker per selected device.\n# gpus: auto\n\nglobal:    # defaults merged into every worker (any block may be overridden per worker)\nworkers:   # optional under the supervisor; one entry per GPU\nmodels:    # optional; pins the named models to device memory on the listed workers\ngateway:   # optional; read only by the gateway, never by a worker\n\nmodel_repo (required) is the shared bundle repository. Each immediate subdirectory that contains a manifest.yaml is a bundle, and its directory name is the model name. It resolves into every worker's model_dirs (a single entry).\nbase_port is the first worker's gRPC port. Worker at index i binds base_port + i (worker0 -> base_port) unless that worker sets an explicit port:.\nmetrics_base_port (optional) derives each worker's Prometheus metrics port the same way: metrics_base_port + i. Absent, per-worker metrics are disabled.\ngpus (optional, supervisor-only) selects how many workers the supervisor synthesizes when no workers: list is present: auto (the default) enumerates the visible devices, an integer is a device count, and a list gives explicit device selectors (ordinals or GPU UUIDs). The REACTANT_GPUS environment variable overrides this key.\nglobal holds the defaults merged into every worker; any block may be overridden per worker.\nworkers lists the worker entries (optional under the supervisor).\nmodels optionally pins the named models to device memory on the listed workers.\ngateway is read only by the gateway, never by a worker.","title":"Top-level keys"},{"category":"section","location":"node_config/#Global-settings","page":"Node Configuration","text":"The global: block holds defaults merged into every worker; a worker entry may override any of these blocks. The sub-blocks map onto the resolved ServerConfig:\n\nglobal:\n  cache_dir: /var/cache/reactantserver\n  model_control_mode: dynamic  # dynamic (watch the repo) | static | explicit\n  model_poll_seconds: 15.0     # repository poll interval in dynamic mode\n  runtime:                     # -> RuntimeConfig\n    backend: cuda              # cpu or cuda\n    mem_fraction: 0.9          # fraction of device memory claimed for the pool (GPU only)\n    preallocate: true          # claim the pool up front (GPU only)\n    allow_cpu_fallback: false\n    numerics: auto             # f32 | auto | tf32; see below\n    weight_cache_fraction: 1.0 # arena fraction for all weights (pinned + on-demand); 0 disables\n    weight_cache_wiggle_fraction: 0.1  # arena fraction kept free; drives startup auto-sizing\n    autotune: true             # XLA GPU compile autotuner; false = default kernels, no trials\n    # autotune_cache: false    # persistent per-fusion autotune cache; omit to inherit Reactant's\n    # autotune_cache_dir: /cache/autotune  # cache directory; omit to inherit Reactant's defaults\n    shared_host_weights: false # one shm-backed host copy of each model on this node's workers\n    shared_host_weights_mode: \"666\"  # octal permissions for the shared regions; \"660\" preferred\n  scheduler:                   # -> SchedulerConfig\n    discipline: fair           # fair | fifo | edf; fifo or edf behind a gateway in lpt_packing\n    ema_halflife_seconds: 30.0\n    max_queue_depth: 1024      # per-model queue cap; a full model rejects new requests\n    dispatch_timeout_seconds: 30.0\n    compaction_interval: 0     # defragment device memory every N on-demand weight loads; 0 disables\n    models: {}                 # per-model overrides -> ModelSchedConfig (see below)\n  endpoints:                   # -> EndpointsConfig\n    host: 0.0.0.0              # bind all interfaces so the gateway and clients can reach the worker\n    max_concurrent_requests: 64  # in-flight RPC cap; 0 = uncapped; shed with RESOURCE_EXHAUSTED\n  grpc:                        # -> GrpcConfig\n    max_recv_msg_bytes: 536870912  # 512 MiB; max inbound gRPC message\n    max_send_msg_bytes: 536870912  # 512 MiB; max outbound gRPC message\n\nEach sub-block corresponds to a typed config struct (RuntimeConfig, SchedulerConfig, ModelSchedConfig, EndpointsConfig, GrpcConfig); the whole resolved global: is the ServerConfig a worker serves from. The API page lists every field and its default.\n\nruntime.numerics sets the f32 matmul/convolution precision policy. auto (the default) is hardware-adaptive: TF32 is used on GPUs that support it (NVIDIA compute capability 8.0 and up) and stripped where it would not compile, so one bundle runs everywhere but its numerics follow the hardware. f32 pins full f32 on every target: TF32 DotAlgorithms are rewritten to f32, every algorithm-free f32 dot_general/convolution gets precision_config = HIGHEST, and NVIDIA_TF32_OVERRIDE=0 is set as defense in depth. This makes numerics identical across GPU generations, which is the mode for validated deployments where a hardware refresh must not change model outputs; the cost is tensor-core throughput for f32 matmuls on TF32-capable GPUs. tf32 compiles exactly like auto (TF32 is permitted and XLA/cuBLAS pick the kernels; StableHLO has no way to force TF32 for convolutions) but turns the hardware requirement into a guarantee: startup fails on hardware that cannot run TF32, so a mixed fleet cannot silently serve divergent numerics. On CUDA workers a startup probe logs whether TF32 arithmetic is actually in use and, under f32, proves the pin bit-exactly; the per-model outcome (ops pinned, algorithms rewritten or stripped) is recorded in each \"model loaded\" log line.\n\nmodel_control_mode sets how the loaded model set evolves: dynamic (the default) watches the repository and loads, unloads, reloads, and renames bundles online as files change (a renamed directory with unchanged contents renames the model in place, no recompile); static fixes the startup set; explicit cedes the lifecycle to an external control plane via the worker control RPCs. model_poll_seconds is the repository poll interval in dynamic mode.\n\nruntime.device_ordinal is not something you normally set here: the node resolution derives it from the worker's gpu: key (defaulting to 0), and under the supervisor each worker sees a single device through its own CUDA_VISIBLE_DEVICES.\n\nThe global.grpc block is the single node-level place for gRPC message-size limits: every worker reads it directly, and the supervisor also mirrors it into the embedded gateway (as REACTANT_GATEWAY_GRPC_MAX_RECV_MSG_BYTES / REACTANT_GATEWAY_GRPC_MAX_SEND_MSG_BYTES), so one block sizes the whole node. The defaults are 512 MiB each, and these are decode/encode caps, not allocations: raising them costs nothing until a message that large actually arrives. Per-component environment overrides still apply, with the gateway's own REACTANT_GATEWAY_GRPC_MAX_RECV_MSG_BYTES winning over the mirrored value: INFERENCE_SERVER_GRPC_MAX_RECV_MSG_BYTES for a worker, REACTANT_GATEWAY_GRPC_MAX_RECV_MSG_BYTES for the gateway.\n\nscheduler.discipline selects the dispatch policy: fair shares GPU time across models by weighted deficit and learned cost, fifo serves the oldest queued request first, and edf (earliest-deadline-first) serves the model whose most-urgent queued request has the soonest deadline, where the deadline comes from the request's remaining-budget timeout. Workers fronted by a gateway in lpt_packing mode must run fifo or edf (not fair), so the gateway stays the placement and fairness authority (see Multi-GPU Gateway).\n\nUnder edf, a meta model is not scheduled itself (it runs on the request task), but each of its in-flight sub-calls inherits the meta's deadline, so its continuation is ordered ahead of fresher regular work. edf also sheds work it cannot finish within its learned compute cost (laxity), trading some throughput (batch fragmentation, no per-model weighting) for meeting more deadlines under load. Note that edf derives urgency solely from the deadline: issuing different per-client deadlines for the same model reorders that model's service and therefore affects fairness across clients, so keep deadlines uniform to retain fifo-like fairness. See Scheduling for how the disciplines and coalescing work internally.","title":"Global settings"},{"category":"section","location":"node_config/#Workers","page":"Node Configuration","text":"workers:\n  - { name: worker0 }\n  - { name: worker1 }\n\nname is the routing identity (and, under Docker, the compose service name). The listen port is base_port + index unless the worker sets an explicit port:. A worker entry may also carry override blocks (for example a runtime: block) that deep-merge over global:.\n\nUnder the supervisor you do not assign GPUs yourself: it detects the visible devices, gives each worker one of them, and sets that worker's own CUDA_VISIBLE_DEVICES, so every worker sees a single GPU at ordinal 0. Influence the assignment with the top-level gpus: key (auto, a count, or an explicit device list), the REACTANT_GPUS environment variable, or by adding gpu: N to a worker entry to pin it to a specific visible device. A host-level CUDA_VISIBLE_DEVICES acts as a coarse filter on which physical GPUs the supervisor sees (see Deployment). Running a single worker by hand without the supervisor (a bare serve), the worker uses device ordinal 0, or gpu: N to pick another.","title":"Workers"},{"category":"section","location":"node_config/#Device-pinning-(the-models:-map)","page":"Node Configuration","text":"models:\n  resnet50: [worker0, worker1]   # hot on both GPUs\n  vsq_coral: [worker0]           # hot on worker0 only\n\nEvery worker loads (and can serve) every bundle in model_repo; the gateway discovers what each worker serves and schedules requests across them. The optional top-level models: map is a per-model override that pins the named models to device memory on the listed workers for the lowest latency. It translates into scheduler.models.<name>.residency: device on those workers, merging into any per-model block already set under global: (an explicit residency wins; the map only fills an unspecified one). Unlisted models stay system-pinned in host RAM and load to the device on demand. Omit the block entirely to keep every model on-demand.\n\nTo load only a subset of the repository on a worker, the resolved config also supports a models_include allowlist (empty means load all); set it with the INFERENCE_SERVER_MODELS_INCLUDE override below.","title":"Device pinning (the models: map)"},{"category":"section","location":"node_config/#Per-model-scheduler-overrides","page":"Node Configuration","text":"Tune individual models under scheduler.models, which builds the ModelSchedConfig entries:\n\nscheduler:\n  models:\n    resnet50:\n      weight: 2.0                  # relative compute share (default 1.0)\n      residency: device            # keep weights GPU-resident for the server's lifetime\n      max_batch_size: 8            # cap on rows coalesced per dispatch (default uncapped)\n    yolo:\n      residency: unpinned          # no host floor; re-materialized from disk on each load\n\nweight sets the model's fair share (consulted only by the fair discipline). residency sets the model's residency floor: unpinned, system, or device (pin_to_gpu: true is a back-compat alias for residency: device). When the on-demand weight cache is enabled, models without an explicit residency default to system (weights pinned in host RAM); see On-demand Weights.\n\nmax_batch_size caps how many rows the scheduler coalesces into one dispatch of the model. It does not change compiled shapes: the dispatch sizes come from the batch sizes the bundle was compiled for (a partial fill still pads up to the smallest compiled size), the batch axis comes from the bundle manifest, and a single request larger than the cap is still served in one dispatch because requests are never split.","title":"Per-model scheduler overrides"},{"category":"section","location":"node_config/#Gateway-configuration","page":"Node Configuration","text":"The gateway does not read the node file at all: it is configured by its own standalone gateway.yml, which carries its listen addresses, limits, and a flat endpoints: list of worker host:port addresses that may span any number of nodes (see Multi-GPU Gateway). A gateway.yml looks like:\n\nlisten:\n  grpc: \"0.0.0.0:8001\"\n  metrics: \"0.0.0.0:8002\"\ngrpc:\n  max_recv_msg_bytes: 536870912   # 512 MiB\n  max_send_msg_bytes: 536870912\n  max_concurrent_requests_per_worker: 64  # inbound cap is this x worker count; 0 = uncapped.\n                                          # Sized above the outbound stream limit so a startup\n                                          # burst has headroom rather than being shed early\nworker_client:\n  request_timeout_seconds: 60\n  max_concurrent_streams: 32      # outbound in-flight RPCs the gateway multiplexes to one worker\nlogging:\n  level: \"info\"\n  format: \"json\"\nscheduling:\n  mode: round_robin               # round_robin | least_outstanding | lpt_packing\n  work_basis: compute             # what \"least busy\" measures: compute | items | requests\n  # lpt_packing only; see Multi-GPU Gateway for the full set and the runtime control plane.\n  default_replicas: 1             # GPUs per model (a number, or \"all\")\n  routing_fill_mode: run          # run (default) | spread | inflight\nendpoints:                        # worker host:port addresses, across any number of nodes\n  - \"worker0:8080\"\n  - \"worker1:8081\"\n\nThe gateway is also configured by REACTANT_GATEWAY_* environment overrides (REACTANT_GATEWAY_WORKERS replaces the endpoint list, comma separated). The embedded gateway under the supervisor is launched from defaults plus those synthesized environment variables, so a single node needs no gateway.yml at all. See Multi-GPU Gateway for the scheduling modes, the runtime control plane, and the lpt_packing-specific knobs.","title":"Gateway configuration"},{"category":"section","location":"node_config/#Environment-overrides","page":"Node Configuration","text":"Any worker value can be overridden per process by an environment variable of the form INFERENCE_SERVER_<SECTION>_<FIELD>, for example:\n\nINFERENCE_SERVER_ENDPOINTS_PORT=9100\nINFERENCE_SERVER_RUNTIME_BACKEND=cpu\nINFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_FRACTION=0.8\n\nBooleans parse as 1, true, yes, or on (case-insensitive). List-valued overrides (INFERENCE_SERVER_MODEL_DIRS, INFERENCE_SERVER_MODELS_INCLUDE) are colon-separated. Overrides are applied on top of the resolved node config, and the effective configuration, including which overrides were applied, is logged at startup.\n\nVariable Overrides Values\nINFERENCE_SERVER_CACHE_DIR cache_dir path\nINFERENCE_SERVER_MODEL_POLL_SECONDS model_poll_seconds seconds\nINFERENCE_SERVER_MODEL_CONTROL_MODE model_control_mode static | dynamic | explicit\nINFERENCE_SERVER_MODEL_DIRS model_dirs colon-separated paths\nINFERENCE_SERVER_MODELS_INCLUDE models_include colon-separated model names\nINFERENCE_SERVER_RUNTIME_BACKEND runtime.backend cpu | cuda\nINFERENCE_SERVER_RUNTIME_DEVICE_ORDINAL runtime.device_ordinal device index\nINFERENCE_SERVER_RUNTIME_MEM_FRACTION runtime.mem_fraction 0..1\nINFERENCE_SERVER_RUNTIME_PREALLOCATE runtime.preallocate bool\nINFERENCE_SERVER_RUNTIME_ALLOW_CPU_FALLBACK runtime.allow_cpu_fallback bool\nINFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_FRACTION runtime.weight_cache_fraction 0..1\nINFERENCE_SERVER_RUNTIME_WEIGHT_CACHE_WIGGLE_FRACTION runtime.weight_cache_wiggle_fraction 0..1\nINFERENCE_SERVER_RUNTIME_AUTOTUNE runtime.autotune bool\nINFERENCE_SERVER_RUNTIME_AUTOTUNE_CACHE runtime.autotune_cache bool\nINFERENCE_SERVER_RUNTIME_AUTOTUNE_CACHE_DIR runtime.autotune_cache_dir path\nINFERENCE_SERVER_RUNTIME_NUMERICS runtime.numerics f32 | auto | tf32\nINFERENCE_SERVER_RUNTIME_SHARED_HOST_WEIGHTS runtime.shared_host_weights bool\nINFERENCE_SERVER_RUNTIME_SHARED_HOST_WEIGHTS_MODE runtime.shared_host_weights_mode octal string\nINFERENCE_SERVER_SCHEDULER_DISCIPLINE scheduler.discipline fair | fifo | edf\nINFERENCE_SERVER_SCHEDULER_EMA_HALFLIFE_SECONDS scheduler.ema_halflife_seconds seconds\nINFERENCE_SERVER_SCHEDULER_RECENCY_PENALTY_CAP scheduler.recency_penalty_cap 0..1\nINFERENCE_SERVER_SCHEDULER_COALESCING_DISCOUNT scheduler.coalescing_discount 0..1\nINFERENCE_SERVER_SCHEDULER_COST_EMA_ALPHA scheduler.cost_ema_alpha 0..1\nINFERENCE_SERVER_SCHEDULER_MAX_QUEUE_DEPTH scheduler.max_queue_depth integer\nINFERENCE_SERVER_SCHEDULER_DISPATCH_TIMEOUT_SECONDS scheduler.dispatch_timeout_seconds seconds\nINFERENCE_SERVER_SCHEDULER_COMPACTION_INTERVAL scheduler.compaction_interval integer\nINFERENCE_SERVER_ENDPOINTS_HOST endpoints.host address\nINFERENCE_SERVER_ENDPOINTS_PORT endpoints.port port\nINFERENCE_SERVER_ENDPOINTS_METRICS_PORT endpoints.metrics_port port\nINFERENCE_SERVER_ENDPOINTS_MAX_CONCURRENT_REQUESTS endpoints.max_concurrent_requests integer\nINFERENCE_SERVER_GRPC_MAX_RECV_MSG_BYTES grpc.max_recv_msg_bytes bytes\nINFERENCE_SERVER_GRPC_MAX_SEND_MSG_BYTES grpc.max_send_msg_bytes bytes\n\nRemoved keys fail loudly rather than being silently ignored: a batch_policy block or a runtime.residency_mode key is rejected at startup with a migration error naming the replacement (scheduler.models.<name>.max_batch_size and the top-level model_control_mode, respectively), and the same applies to their INFERENCE_SERVER_BATCH_POLICY_* and INFERENCE_SERVER_RUNTIME_RESIDENCY_MODE environment overrides.","title":"Environment overrides"},{"category":"section","location":"","page":"Home","text":"<div id=\"landing\" class=\"landing\">\n<header class=\"landing-hero\">\n  <div class=\"landing-hero__text\">\n    <p class=\"landing-name\">ReactantServer.jl</p>\n    <h1 class=\"landing-title\">Production inference for Reactant-compiled models</h1>\n    <p class=\"landing-tagline\">KServe V2 over gRPC from one GPU to many, with compiled XLA models, Julia-first pre and postprocessing, and the most models per card.</p>\n    <div class=\"landing-actions\">\n<a class=\"landing-btn landing-btn--brand\" href=\"tutorial/\">Tutorial</a>\n      <a class=\"landing-btn landing-btn--alt\" href=\"api/\">API Reference</a>\n      <a class=\"landing-btn landing-btn--alt\" href=\"https://github.com/EnzymeAD/ReactantServer.jl\">View on GitHub</a>\n    </div>\n  </div>\n  <div class=\"landing-hero__image\">\n    <div class=\"landing-hero__image-container\">\n      <img src=\"assets/logo.svg\" alt=\"ReactantServer.jl\" width=\"320\" height=\"320\">\n    </div>\n  </div>\n</header>\n<section class=\"landing-features\" aria-label=\"Features\">\n  <a class=\"landing-feature\" href=\"client/\">\n<div class=\"landing-feature__icon\">⚡</div>\n<h2 class=\"landing-feature__title\">KServe V2, natively</h2>\n<p class=\"landing-feature__details\">Speaks the KServe V2 inference API over gRPC, so standard Triton and KServe clients connect unchanged.</p>\n  </a>\n  <a class=\"landing-feature\" href=\"tutorial/\">\n<div class=\"landing-feature__icon\">🚀</div>\n<h2 class=\"landing-feature__title\">XLA under the hood</h2>\n<p class=\"landing-feature__details\">Models compile ahead of time through Reactant and XLA into device executables; the runtime is device agnostic, CUDA today with CPU for development.</p>\n  </a>\n  <a class=\"landing-feature\" href=\"bundles/\">\n<div class=\"landing-feature__icon\">🧩</div>\n<h2 class=\"landing-feature__title\">Julia-first</h2>\n<p class=\"landing-feature__details\">A bundle's model.jl registers pre and postprocessing in plain Julia, and every convention follows Julia's, column-major with the batch axis last.</p>\n  </a>\n  <a class=\"landing-feature\" href=\"on_demand_weights/\">\n<div class=\"landing-feature__icon\">💾</div>\n<h2 class=\"landing-feature__title\">On-demand weights</h2>\n<p class=\"landing-feature__details\">Weights stay in host RAM and stream to the GPU under an LRU byte budget, so a card serves more models than fit in VRAM.</p>\n  </a>\n  <a class=\"landing-feature\" href=\"scheduling/\">\n<div class=\"landing-feature__icon\">🔀</div>\n<h2 class=\"landing-feature__title\">A coalescing scheduler</h2>\n<p class=\"landing-feature__details\">A deficit-weighted, cost-aware scheduler merges same-model requests into one execution at a compiled batch size.</p>\n  </a>\n  <a class=\"landing-feature\" href=\"bundles/\">\n<div class=\"landing-feature__icon\">🔁</div>\n<h2 class=\"landing-feature__title\">Hot reload</h2>\n<p class=\"landing-feature__details\">In dynamic mode the server watches the model repository and reloads bundles online, with no restart.</p>\n  </a>\n</section>\n</div>","title":"Home"},{"category":"section","location":"#What-it-is","page":"Home","text":"ReactantServer.jl is a production inference server for XLA-accelerated models, compiled through Reactant.jl (StableHLO via XLA today). It speaks the KServe V2 inference API natively over gRPC, so standard Triton and KServe clients connect unchanged; it scales from a single GPU to many from one container; and it balances model memory against compute to squeeze the most models out of each GPU. It is Julia-first throughout: custom pre and postprocessing is plain Julia in a bundle's model.jl, and every convention follows Julia's, column-major with the batch axis last.\n\nThe system is a workspace of packages, split so that talking to a server never pulls in the heavy Reactant/XLA stack: ReactantServerCore is the shared, Reactant-free substrate (dtypes, protobufs, the manifest parser, node config, the codec, shared memory, the staging BufferPool); ReactantServer is the worker, the only package that depends on Reactant; ReactantServerGateway is the multi-GPU reverse proxy; ReactantServerClient is a Reactant-free client; ReactantServerNode is the one-container supervisor. Offline export lives in ReactantServerExport. Every package is on the API page.","title":"What it is"},{"category":"section","location":"#Why-ReactantServer?","page":"Home","text":"The target is static-graph workloads, computer vision and scientific computing, where many models share a GPU and one model executes at a time. That shape rewards a server that is compiled rather than interpreted: models are compiled ahead of time into device executables through Reactant's PJRT bindings, so inference is a single batched kernel launch rather than an interpreter loop, and the runtime is device-agnostic, CUDA today with CPU for development and fallback.\n\nThe design balances two resources that pull against each other. Compute is managed by a deficit-weighted, cost-aware scheduler that coalesces concurrent same-model requests into one execution at a compiled batch size. Memory is managed by the on-demand weights cache, which keeps weights in host RAM and streams them onto the GPU under an LRU byte budget. Both are retunable at runtime, on a worker or across GPUs, through a gRPC control plane, without a restart.","title":"Why ReactantServer?"},{"category":"section","location":"#Install","page":"Home","text":"ReactantServer is not yet registered in the General registry, so installation is from the repository. The one build dependency beyond the workspace, the gRPC server, is sourced from the main branch of github.com/JuliaIO/gRPCServer.jl through the workspace [sources]:\n\ngit clone https://github.com/EnzymeAD/ReactantServer.jl\ncd ReactantServer.jl\nREACTANT_GPU=cuda REACTANT_GPU_VERSION=13.1 julia --project=. -e 'using Pkg; Pkg.instantiate()'\n\nRun the supervisor over a directory of model bundles and it scales to every visible GPU:\n\nCUDA_VISIBLE_DEVICES=0,1,2,3 INFERENCE_SERVER_MODEL_DIRS=/path/to/bundles \\\nREACTANT_NODE_FILE=config/node.default.yaml \\\n  julia --handle-signals=no --project=packages/ReactantServerNode \\\n    -e 'using ReactantServerNode; ReactantServerNode.main()'\n\nOr from pure Julia:\n\nusing ReactantServerNode\nReactantServerNode.supervise(\"config/node.yaml\")   # one worker per GPU (+ gateway if >1)\n\nClients speak KServe V2 gRPC to :8001; health and metrics are on :8002. The first server startup is slow, because every model compiles before the gRPC plane accepts traffic. The server is designed for a trusted network, so read the Deployment page's security notes before exposing an endpoint.","title":"Install"},{"category":"section","location":"#Start-here","page":"Home","text":"The Tutorial: export a Lux model to a bundle, configure a node, serve it, and query it from a client, end to end.\nBundles: the bundle format, the manifest's named-axis notation, and the export frontends.\nNode Configuration: the one typed YAML file that describes a machine.\nScheduling: the cost-aware worker scheduler and batch coalescing.\nOn-demand Weights: host-RAM weights and the LRU byte budget.\nMulti-GPU Gateway: the reverse proxy and its scheduling modes.\nClient Usage: the Reactant-free client and its shared-memory transport.\nMeta Models: chaining models with data-dependent Julia between stages.\nThe worked examples, Object Detection and Transformer Text Models, end to end.\nDeployment: systemd, Docker, monitoring, and the deployment shapes.\nThe API: every documented name, collected automatically.","title":"Start here"},{"category":"section","location":"tutorial/#Tutorial:-Export-to-Inference,-End-to-End","page":"Tutorial","text":"This page walks one model through the whole system: export a small Lux MLP into a bundle with export_bundle(:lux, ...), write the minimal single-GPU node file, run the server with the supervisor (the container launcher) or from pure Julia, and query it with ReactantServerClient over KServe V2 gRPC. The commands target a CUDA GPU host (backend: cuda, a visible GPU). The runtime is device agnostic, so the same steps work on CPU by setting backend: cpu and dropping the GPU flags; that is handy for following along without a GPU.","title":"Tutorial: Export to Inference, End to End"},{"category":"section","location":"tutorial/#How-the-pieces-fit","page":"Tutorial","text":"ReactantServer is a Julia workspace of packages, each with one job. A served model is a bundle: a directory with a manifest.yaml, a compiled StableHLO program (one model.b{N}.mlir per compiled batch size), and a shared weights.safetensors; see Bundles for the format. The worker serves bundles over the KServe V2 gRPC API, one worker per GPU, and the node supervisor runs a whole machine from one process: it detects the visible GPUs, spawns one worker subprocess per device, and embeds a thin gateway when there are two or more workers so clients keep a single endpoint. The client talks to a worker or the gateway from anywhere.\n\nPackage Role\nReactantServerCore Shared, Reactant-free substrate: dtype vocabulary, KServe V2 protobufs, node config, wire codec, shared-memory staging.\nReactantServer The inference worker: model registry, runtime, scheduler, KServe V2 gRPC server. The only package that loads Reactant.\nReactantServerNode The node supervisor: detects GPUs, spawns one worker subprocess per device, embeds the gateway when needed.\nReactantServerGateway Multi-GPU reverse proxy that fronts two or more workers with one endpoint.\nReactantServerClient The inference client (KServeModel, infer_sync, InferInput, InferOutput). Reactant-free.\nReactantServerExport Offline bundle production from Lux or PyTorch models. Not a workspace member; the server never loads it.\n\nThe split is deliberate: only the export step needs the model framework, only ReactantServer needs Reactant, and the client is just another KServe V2 gRPC client.","title":"How the pieces fit"},{"category":"section","location":"tutorial/#Three-environments,-one-flow","page":"Tutorial","text":"Because the packages are split that way, this tutorial runs in three separate Julia environments that are never loaded together. Export, serve, and query each use their own project, and they talk to one another only through files and the gRPC wire protocol.\n\nExport environment. ReactantServerExport is not a workspace member; it carries Lux and PythonCall weak dependencies the server image should not. Use its test project, which has them ready: julia --project=packages/ReactantServerExport/test.\nServer environment. The workspace under packages/ (ReactantServerCore, ReactantServer, ReactantServerNode, ReactantServerGateway), with gRPCServer sourced from its main branch on GitHub through the workspace [sources]. After cloning, instantiate once:\n\njulia --project=. -e 'using Pkg; Pkg.instantiate()'\n\nClient environment. ReactantServerClient depends only on ReactantServerCore and the gRPC layer, so add it to any project on a plain client machine and using ReactantServerClient.","title":"Three environments, one flow"},{"category":"section","location":"tutorial/#Step-1:-Export-a-model-into-a-bundle","page":"Tutorial","text":"Export happens offline, in the export environment. Start it and trace a tiny Lux MLP. The batch dimension is the last Julia axis (the Lux convention), so a 4-feature input is (4, batch):\n\nusing Lux, ReactantServerExport, Random\n\nmodel = Lux.Chain(Lux.Dense(4 => 8, tanh), Lux.Dense(8 => 3))\nps, st = Lux.setup(Random.Xoshiro(0), model)\n\nexample = randn(Float32, 4, 1)            # (features, batch)\nReactantServerExport.export_bundle(:lux, model, ps, st, example;\n    dir = joinpath(\"models\", \"mlp\"), name = \"mlp\", batch_sizes = [1])\n\nexport_bundle(:lux, model, ps, st, example; ...) traces model(x, ps, st) at each batch size and writes models/mlp/: a manifest.yaml, one compiled StableHLO module per batch size (here model.b1.mlir), and weights.safetensors. The input tensor is named input and the output output by default (override with input_name / output_name); the client snippets below use those names. The :lux frontend works for any Reactant-traceable model(x, ps, st); Lux itself is not required by the frontend, only by this example that builds the model.\n\nmodels/ is now a model repository: every immediate subdirectory with a manifest.yaml is a servable model, keyed by its directory name (mlp here). See Bundles for the manifest format and custom pre/post-processing via a model.jl.","title":"Step 1: Export a model into a bundle"},{"category":"section","location":"tutorial/#Step-2:-Configure-a-single-GPU-node","page":"Tutorial","text":"A deployment is described by one node file. The minimal single-GPU node needs only the model repository, a base port, the runtime backend, and one worker:\n\n# node.yaml\nmodel_repo: /var/lib/reactantserver/models\nbase_port: 8080\nmetrics_base_port: 9100\n\nglobal:\n  runtime:\n    backend: cuda         # use \"cpu\" to follow along without a GPU\n  endpoints:\n    host: 0.0.0.0\n\nworkers:\n  - { name: worker0 }     # one worker on the (single) GPU\n\nThe explicit one-entry workers: list works with every run path below, including a bare ReactantServer.serve, which expects the workers list. Under the supervisor you can instead omit workers: and write gpus: auto, and it synthesizes one worker per detected GPU; two or more workers bring up the embedded Gateway. See Node Configuration for the full surface: scheduler tuning, on-demand weights, per-model pinning, environment overrides.","title":"Step 2: Configure a single-GPU node"},{"category":"section","location":"tutorial/#Step-3:-Run-the-node-supervisor","page":"Tutorial","text":"Run the supervisor, pointing it at the repository from Step 1. INFERENCE_SERVER_MODEL_DIRS overrides the node file's model_repo, and REACTANT_NODE_FILE names the node file:\n\nCUDA_VISIBLE_DEVICES=0 INFERENCE_SERVER_MODEL_DIRS=$PWD/models REACTANT_NODE_FILE=node.yaml \\\n  julia --handle-signals=no --project=packages/ReactantServerNode \\\n    -e 'using ReactantServerNode; ReactantServerNode.main()'\n\nThis is the pure-Julia form of the container launcher: in the image, docker/entrypoint.node.sh runs the same entry point, detects the visible GPUs, spawns one worker subprocess per device, and restarts children that die.\n\nWith a single GPU the node runs one worker and no gateway: the worker serves the KServe V2 gRPC API on localhost:8001 and metrics/health on localhost:8002 (/readyz, /healthz, /metrics). The first start compiles every model before accepting traffic, so give it a moment; curl localhost:8002/readyz returns 200 once it is serving.","title":"Step 3: Run the node supervisor"},{"category":"section","location":"tutorial/#Step-4:-Or-serve-from-pure-Julia","page":"Tutorial","text":"Two entry points, differing only in which ports are exposed. First the supervisor, which behaves exactly like the container launcher docker/entrypoint.node.sh: one worker (no gateway) on the public ports 8001 (gRPC) and 8002 (metrics):\n\nusing ReactantServerNode\nReactantServerNode.supervise(\"node.yaml\")\n\nSecond, a single bare worker with no supervisor, which serves on the node file's own ports (gRPC on base_port, 8080, metrics on metrics_base_port, 9100):\n\nusing ReactantServer\nReactantServer.serve(\"node.yaml\")          # blocks; Ctrl-C to stop\n\nserve loads the node file, brings up the runtime, compiles the worker's bundles, starts the ReactantServer.Scheduler, and finally starts the gRPC server so traffic is accepted only once models are live. Pass blocking=false to get a ReactantServer.RunningServer you can stop!:\n\nserver = ReactantServer.serve(\"node.yaml\"; blocking = false)\n# ... issue requests ...\nReactantServer.stop!(server)\n\nsupervise is the right choice for deployment (it is what the launcher runs, and it scales to many GPUs unchanged); a bare serve is convenient for a quick single-worker REPL session.\n\nStart Julia multithreaded so per-request preprocess/postprocess hooks overlap the GPU execution: julia --threads=auto,1 gives a default pool for the hooks plus one interactive thread for the GPU dispatch loop. Set this yourself only for a bare serve; under the supervisor each worker is instead sized to its share of the host (min(cores / workers, 16) compute threads plus the interactive one, overridable via REACTANT_WORKER_THREADS), so co-located workers do not oversubscribe the CPU. With a single thread the server still works, just without the overlap.","title":"Step 4: Or serve from pure Julia"},{"category":"section","location":"tutorial/#Step-5:-Query-it-with-the-ReactantServerClient","page":"Tutorial","text":"The server speaks KServe V2 over gRPC, so any Triton/KServe client works; this repository ships the Reactant-free ReactantServerClient. From the client environment, point it at the port your server is using (8001 for the supervisor, 8080 for a bare serve) and use the bundle's tensor names input / output:\n\nusing ReactantServerClient\n\nkserve_init()\ntry\n    model = KServeModel(\"grpc://127.0.0.1:8001\", \"mlp\"; max_batch_size = 1)\n    x = Float32[1, 2, 3, 4]                       # one 4-feature item\n    response = infer_sync(model, [InferInput(\"input\", x)])\n    y = InferOutput(\"output\", response, Float32)  # length-3 output\n    @show vec(collect(y))\nfinally\n    kserve_shutdown()\nend\n\nkserve_init prepares the client transport (a staging pool plus dispatch slots) and kserve_shutdown tears it down. KServeModel(url, model_name) binds a model on the server; infer_sync sends one request and returns the raw response; InferInput builds a named input tensor and InferOutput reads a named tensor back as Float32. See Client Usage for batched inference over a dataset, IO validation, and the shared-memory data path.","title":"Step 5: Query it with the ReactantServerClient"},{"category":"section","location":"tutorial/#Next-steps","page":"Tutorial","text":"Bundles: the bundle format, manifest fields, and custom pre/post-processing.\nNode Configuration: the full node.yaml surface and environment overrides.\nScheduling: how the worker picks the next model and coalesces requests.\nOn-demand Weights: serving more models than fit in GPU memory.\nGateway: multi-GPU deployments behind one client endpoint.\nClient: the full client API and data paths.\nMeta Models and the worked examples Object Detection and Transformers.\nDeployment: systemd, Docker, health, and metrics.\nAPI: every documented name, collected automatically.","title":"Next steps"}]
}
