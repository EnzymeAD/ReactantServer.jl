.idea
*.pyc
__pycache__
bundles/
.CondaPkg
.venv
*.mlir
*.safetensors
build
validation/gpu_memory_sharing/results/
private/
# Corporate TLS-inspection CA certs, dropped in locally; never commit them.
docker/certs/*.crt
# Stray generated e2e model (leftover from removed tooling); not tracked.
packages/ReactantServer/test/e2e/models/bit_resnet50/
# Julia resolved-dependency lockfiles: not checked in, resolved fresh per environment (incl. CI)
# so a vendored-fork compat bump (e.g. HTTP requiring a newer Reseau) is picked up automatically.
Manifest.toml
Manifest-v*.toml